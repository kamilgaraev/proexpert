# Реализация единой привязки смет: журнал работы

## Основание

Полный объём и приёмка: [согласованный план](unified-estimate-contract-finance.md).
Ни один этап пока не завершён. Деплой не выполнялся.

## Состояние на 12 сентября 2026

- Backend: ветка `task/unified-finance-20260912`, исходный main `a7c85b3a1`.
- Админка: одноимённая ветка, исходный main `6d90f0d1`.
- Обе рабочие копии изолированы от незавершённых задач в основных каталогах.
- CBM перечислил проекты, но содержательный поиск завершился `Transport closed`; исследование продолжено точечным чтением исходников нового checkout.

## Подтверждённые точки изменения

- `EstimateFinanceService::save` удалял и заново создавал строки распределений при каждом редактировании. Изменено на обновление существующей строки по стабильному ключу с сохранением её ID. Удаление отсутствующих строк пока остаётся старым; защита утверждённого факта ещё предстоит.
- `EstimateFinanceService::projectLinks` читал связи и распределения для каждой позиции отдельно. Чтение сгруппировано в два пакетных запроса; записи пока остаются построчными. Это ещё не доказательство выполнения требования нагрузки на 8000 строк.
- Добавлен регрессионный тест сохранения ID распределения и совместимой связи при изменении цены и повторном запросе.
- `ContractEstimateService::attachItems`, `detachItems`, `syncItems`, `updateCoverageVat` ещё используют самостоятельный путь изменения связей. Их предстоит сделать адаптерами единого сервиса с явным actor и проверками доступа.
- `EstimateFinanceAccess::editContracts` теперь проверяет `contracts.edit` и область организации/проекта для входящих и существующих затронутых связей. В том числе проверяется пустая замена, удаляющая договорные условия. Проверка вызывается в нормализации сохранения и обоих вариантах предпросмотра. Самостоятельные затраты без связей с договорами не требуют `contracts.edit`.
- `SaveEstimateFinanceRequest` ограничивает выбор 1000 целями и не различает освобождение от НДС и ставку 0%. Массовый выбор и явный налоговый режим ещё предстоят.
- История снимков условий реализуется в четвёртом блоке ниже. Редактирование остатка, факт актов/оплат, ручные затраты, перенос, новый единый интерфейс и единые сводки ещё не реализованы в этой задаче.

## Проверки первого блока

- `php -l` для изменённого сервиса и теста: успешно.
- PHPStan для `EstimateFinanceService.php`: успешно.
- `git diff --check`: успешно.
- Целевой `EstimateFinanceTest` через штатный PostgreSQL launcher: успешно, 22 теста, 98 проверок, 4 минуты 17 секунд.
- Для тестового запуска порт в трёх файлах временно согласован с правилами workspace: 55433 вместо старого 45433. Временные изменения возвращены после проверки; CI/CD не меняется.

## Следующая работа

1. Первый блок проверен; документы в `docs/specs` игнорируются правилами репозитория, при коммите включить только два документа этой задачи явно.
2. Составить карту потребителей сумм сметы/договора/проекта/Excel и старых команд привязки; привести к одному расчёту с регрессией из плана.
3. Расширить существующий финансовый контракт: режим НДС, стабильные версии и защита факта, права на договоры, массовый выбор и полноценный предпросмотр.
4. Реализовать единый пользовательский процесс, план/выполнение/деньги и миграцию согласно полному плану, затем штатный релиз и проверку клиента.

## Карта старых команд (проверенные исходники)

- `ContractEstimateItemController`: добавление позиций, удаление, изменение НДС через `ContractEstimateService`.
- `EstimateCoverageService::attachFullCoverage/syncCoverageItems`: синхронизация через `ContractEstimateService`, затем синхронизация журнала через `CompletedWorkFactService`.
- `EstimateCoverageService::detachCoverage`: прямое удаление `ContractEstimateItem`, обход сервиса привязки. Обязательно перевести на общий путь и защитить факт.
- `ContractFromEstimateService::create`: создание договора и привязка в одной транзакции, actor уже передаётся в наружный метод, но не в `attachItems`.
- `JournalContractCoverageService::ensureCoverage`: автоматическая привязка к договору журнала, actor отсутствует в сигнатуре. Потребует явного контекста и проверки финансовых прав, при сохранении совместимости API.
- `EstimateCoverageService::getCoverageForEstimate/getContractCoverageSummary` и `ContractEstimateService::getSummary/calculateContractEstimateTotal` самостоятельно суммируют совместимые связи. Это потребители будущего единого расчёта.
- `EstimateFinanceQuery::allocations` объединяет управляемые распределения и старые связи, причём старые условия возвращает как неизвестные. Перенос должен сохранять эту осторожность и не придумывать налоговый режим.

## Второй блок: права

- Добавлены четыре теста: запрет создания без права договора, запрет удаления пустой заменой, запрет предпросмотра сметных цен без права договора, разрешение собственных затрат без договора.
- PHPStan обоих изменённых сервисов и php -l сервисов/теста прошли.
- Общий запуск: 25 из 26 тестов прошли, один новый тест ошибочно ожидал начальную версию 0 вместо сохранённой версии после создания позиции. Исправлено сравнение с исходной версией; повторный целевой запуск прошёл (1 тест, 2 проверки). Исполняемый код после общего запуска не менялся.
- Временная настройка тестового порта возвращена; diff не содержит изменений тестовой инфраструктуры.

## Третий блок: режим НДС

- Добавлены nullable `vat_mode` и миграция; исходные данные миграция не пересчитывает.
- `EstimateFinanceTax` рассчитывает сумму без налога и с налогом через существующую десятичную арифметику, проверяет соответствие режима, базы цены и ставки. Режим `none` хранится без ставки; налогообложение по ставке 0% сохраняет явную нулевую ставку и отличается от освобождения.
- Старые API без `vat_mode` поддерживаются по исходным полям цены/ставки, без восстановления ставки из сумм. Старые связи с неизвестными условиями остаются неизвестными.
- Калькулятор больше не считает явно выбранное освобождение от НДС отсутствующей ставкой. Предусмотрены тесты независимых ставок сторон, повторного сохранения, освобождения и противоречивых условий.
- Админка: типы, преобразование ответа в форму и существующая форма условий передают явный режим. Выбор освобождения очищает ставку; при возврате к налогообложению ставка не подставляется автоматически. Полный новый процесс привязки ещё предстоит.
- После прерывания процессов результаты налогового блока не сохранились; процессы были проверены как отсутствующие и проверки перезапущены с файловыми логами.
- PHPStan финансового каталога и запроса прошёл; синтаксис нового расчёта и миграции прошёл.
- Backend: 29 тестов, 122 проверки прошли (6 минут 16 секунд, длительность связана с созданием полной тестовой схемы). Временное изменение порта возвращено.
- Админка: Vitest 7 тестов, ESLint и `tsc --noEmit` прошли. Backend-блок сохранён коммитом `4f6b8d380`; изменения админки также сохранены в task-ветке. Деплоя нет.

## Четвёртый блок: история условий

- Добавлены `condition_version` и отдельная таблица снимков до/после создания, изменения, удаления. История не зависит от существования текущей строки распределения; содержит автора, идентификатор операции и общую версию расчёта.
- `EstimateFinanceHistory` записывает историю пакетно внутри транзакции сохранения. Повтор одного mutation ID возвращает прежний результат без повторных версий. После удаления ключ нельзя повторно использовать для новой привязки.
- Необязательная `condition_version` проверяется наряду с общей revision. Отсутствие нового поля сохраняет совместимость старых клиентов; новая админка передаёт версию из ответа.
- Добавлен защищённый финансовыми правами GET `projects/{project}/estimates/{estimate}/finance/history`, постраничная выдача по 100 записей с `after_id`/`next_cursor`. Интерфейс просмотра истории ещё предстоит.
- Проверки: сохранность снимков после удаления, идемпотентность, устаревшая версия при свежей общей revision, запрет чтения без финансового права. PHPStan и синтаксис прошли; DB-набор прошёл: 32 теста, 135 проверок. Временный порт возвращён. Админка: ESLint и TypeScript прошли. Backend-коммит `6a7ee9359`, изменения админки сохранены в task-ветке.
- Попытка fast-индексации новой backend-копии в официальном CBM завершилась `Pipeline failed`. Граф основной копии не использовался как актуальные исходники этой ветки; далее применено точечное чтение.
- Снимки актов пока не связаны с версиями и защита принятого объёма ещё не реализована. Этот блок не закрывает требования плана по неизменности факта.

## Пятый блок: принятый объём (в работе)

- `EstimateFinanceAcceptedVolume` читает утверждённые/подписанные акты в области организации, проекта и затронутых договоров, группирует принятые объёмы по договору и позиции. Аннулированные акты исключены, включая случай сохранившегося флага `is_approved`.
- Источник объёма — строки акта; только для актов без строк используется старая таблица связи с выполненными работами. Это соответствует приоритету источников в `ContractPerformanceAct::recalculateAmount` и не складывает оба представления одного акта.
- Нормализация save/preview отклоняет удаление связи и объём ниже принятого. Документы и их суммы не изменяются.
- Добавлены тесты удаления/уменьшения подписанного объёма, освобождения после аннулирования и отсутствия ограничения от черновика. Общий запуск: 33 из 34 прошли; тест черновика выявил настоящий дефект: старый `is_approved=true` может сосуществовать со статусом draft. Расчёт исправлен на явные статусы approved/signed, флаг не превращает черновик в факт. Повторный запуск трёх относящихся к актам тестов выполняется. PHPStan и синтаксис ранее прошли, PHPStan после исправления перезапущен.
- Обнаруженная зависимость для следующей части: `PerformanceActFinancialBasisService::forCompletedWork` пока берёт цены/НДС из версии сметы, поэтому его нужно перевести на версии договорных условий с совместимостью старого факта.
- `ActReportWorkflowService::approve` блокирует акт, пересчитывает строки и затем блокирует договор. Требуется согласовать порядок блокировок и проверку актуального остатка с изменением привязки. Текущая проверка объёма сама по себе ещё не закрывает конкурентное утверждение.
- Не завершены в этом блоке: расчёт новых цен только на остаток, фиксация версии в акте, отдельная проверка старого представления актов и конкурентный сценарий. Не считать требование неизменности факта полностью выполненным.
- Повторная проверка завершена: `finance-accepted-retest.log`, 3 теста / 10 проверок успешно; PHPStan после исправления успешно.
- Дополнительный тест проверяет два представления одного акта и отдельный старый акт: принято 30, а не 45; чужая организация не получает эти объёмы. Штатный PostgreSQL launcher: `finance-accepted-legacy-tests.log`, 1 тест / 2 проверки успешно. Временный порт возвращён после проверки.
- Индексация task-worktree теперь завершилась успешно: `most-unified-finance-backend`, generation `2026-09-12T13:44:58Z`. Граф обнаруживает классы PHP, но не метод `forCompletedWork`; актуальный фрагмент класса прочитан через `get_code_snippet`. Метаданные покрытия помечены `metadata_changed`, исчерпывающие связи графа не предполагаются.
- Следующий блок: мастер `ActingActWizardService` и `ActingAvailabilityService` получают цену через `PerformanceActFinancialBasisService`. Утверждение `ActReportWorkflowService::recalculatePricedLines` пересчитывает сохранённую основу через `financialTotals->recalculateFromStoredBasis`, поэтому нужны снимок версии договорных условий и проверка её актуальности перед утверждением, а не только подстановка текущей цены в мастере.

## Шестой блок: договорная основа акта (в работе)

- `PerformanceActContractBasisService` читает условия в области организации, проекта, договора и позиции. Цена и НДС берутся из финансовой привязки; неизвестные условия не заменяются молча ценой сметы. Снимок содержит ключ, номер версии и сохранённые условия. Старые позиции без финансовой привязки пока сохраняют прежний путь по версии сметы.
- `PerformanceActFinancialBasisService` использует договорную основу при её наличии. При нескольких привязках требуется явный ключ; новый необязательный `selected_works.*.allocation_key` передаётся мастером в сервис. Полный выбор нескольких условий в интерфейсе и разбивка одного выполнения на несколько условий ещё не реализованы.
- Первый новый тест (8 проверок) прошёл: договорная ставка 5%, новая цена в следующей версии, предыдущий снимок не изменён. Остальные два не стартовали из-за контроля исходников отчёта: изменение мастера требует обновить `AcceptedProductionCandidateContract::SOURCE_HASH`. Контрольная сумма вычислена по указанному в самом контракте порядку файлов и обновлена; проверка целостности не отключалась.
- Повторные DB-тесты: session 41619, `finance-act-basis-retest.log`; PHPStan: session 30412, `finance-act-basis-phpstan.log`. Сначала проверить эти процессы. Порт в трёх тестовых файлах пока 55433, вернуть после завершения launcher.
- До готовности блока обязательны: проверка версии при утверждении и общий порядок блокировок; привязка принятого факта к конкретному ключу; цены только для неисполненного остатка; пакетное чтение условий; единицы в снимке; точное округление сумм; регрессии мастера актов. Частичная реализация пока не готова к выпуску.
- Повторный запуск основы акта завершён: 3 теста / 11 проверок успешно, PHPStan успешно. После этого добавлен `PerformanceActConditionGuard`, вызываемый при утверждении после блокировки договора и до пересчёта. Он отклоняет исчезнувшую/устаревшую версию и старую сметную основу, если позиция уже имеет договорные условия. Сохранение финансов теперь блокирует также существующие договоры, в том числе при пустом удалении.
- Добавлен тест настоящего `ActReportWorkflowService::approve`: изменение и удаление условий должны возвращать 409 без изменения акта/строки. Текущие процессы: DB session 32725 (`finance-act-guard-tests.log`), PHPStan session 85908 (`finance-act-guard-phpstan.log`). Предыдущие 41619/30412 завершены. Контрольная сумма отчёта пересчитана после изменения workflow. Изменения шестого блока ещё не закоммичены, временный порт вернуть после проверки.
- Session 32725 завершилась ошибкой инфраструктуры до проверок (4 теста, 0 assertions): PostgreSQL закрыл соединение, затем исчезла индивидуальная тестовая БД. Подтверждён новый контейнер `a8612128582b` и работающий PHPUnit другой задачи `pir-platform-backend` (PID 17196 на момент наблюдения). Не запускать наш launcher, пока чужой PHPUnit жив: он пересоздаёт общий контейнер. Session 85908 / PHPStan успешно. После этого добавлен дополнительный scope estimate/project в guard; повторная проверка всего пакета остаётся обязательной.
- Чужой PHPUnit завершился (проверены все php.exe с phpunit в командной строке). Повторный запуск нашего пакета: session 77343, `finance-act-guard-retest.log`. PHPStan пакетной загрузки: session 78610 завершена успешно, `finance-act-batch-phpstan.log`.
- Добавлена связь `EstimateItem::financeAllocations` и eager load для мастера и доступных работ. Resolver использует загруженные данные с повторной фильтрацией организации/договора/позиции; одиночные обращения сохраняют запрос. Дополнительно найдено: `Contract::getProjectIds` для multi-project каждый раз выполняет pluck, поэтому для полного отсутствия N+1 нужно использовать предварительно загруженные projects в resolver. Это ещё не исправлено. Общий тест 8000 строк не выполнен.
- Session 77343 завершена успешно: 4 теста / 17 проверок. Ошибки устаревшей/удалённой версии проверены через настоящий workflow, сохранность акта и строки подтверждена. PHPStan и diff-check прошли. Порт возвращён; пакет сохраняется в ветке без деплоя.
- Следующее выявленное ограничение: общий guard принятого объёма по договору/позиции допускает перенос этого объёма между двумя ключами одного договора. Теперь, когда акты сохраняют allocation_key, нужна дополнительная защита именно этого ключа перед сегментацией цены на принятую часть и остаток.
- Пакет шестого блока сохранён коммитом `b6556f8c7`. Полный процесс остатка остаётся незавершённым.

## Седьмой блок: факт конкретной привязки (в работе)

- Дополнен `EstimateFinanceAcceptedVolume`: принятые строки с `basis_type=contract_conditions` агрегируются по ключу привязки, договору и позиции. Ключ с фактом нельзя удалить, заменить другим ключом, перенести на другую позицию/договор или уменьшить ниже принятого объёма. Старый суммарный guard остаётся для совместимости актов без ключа.
- Новый тест: две привязки одного договора, факт на первой; перенос общего объёма на вторую отклоняется, уменьшение первой ниже факта отклоняется, после аннулирования перенос разрешён. Текущий DB launcher session 69007, `finance-allocation-fact-tests.log`. Временный порт 55433 вернуть после завершения. PHPStan запущен отдельно, `finance-allocation-fact-phpstan.log`.
- Проверки завершены: 3 теста / 13 assertions успешно, PHPStan успешно. Временный порт возвращён. Следующая часть — сохранение принятой стоимости и новых условий остатка: метод unit меняет цену оставшегося объёма, метод total задаёт общий итог связи, из которого вычитается принятая стоимость в соответствующей базе цены.

## Восьмой блок: стоимость остатка (в работе)

- Добавлены nullable JSON `accepted_basis`, `condition_basis` и `EstimateFinanceRemainder`. Сервис вызывается при нормализации после защиты объёма. Принятые суммы читаются только из утверждённых/подписанных неаннулированных строк с ключом условий. Для unit новая цена умножается на остаток; для total из общего итога вычитается принятая стоимость в выбранной базе. План содержит принятую стоимость плюс стоимость остатка. Акт читает новую цену из condition_basis, а не делит смешанный итог на весь объём.
- Тест: 20 из 100 приняты по 6000; новая цена 12000 даёт 1080000, повторное сохранение не начисляет заново; общий итог 1000000 оставляет 880000 на 80 единиц, цена нового выполнения 11000. Проверяется неизменность принятой строки и сумма совместимой проекции.
- Текущие процессы: DB session 2605 (`finance-remainder-tests.log`), PHPStan session 8332 (`finance-remainder-phpstan.log`). Порт временно 55433. Блок не закоммичен.
- Требования для дальнейшего завершения: старый факт без ключа требует уточнения/переноса, он пока не сегментируется; UI должен показывать принятую часть и условия остатка; смешанные ставки не представлять как одну ставку всей суммы; аннулирование требует пересмотра сохранённой сегментации; точное распределение округлений при частичном актировании; предел принимаемого объёма при утверждении, конкурентный сценарий; полный регрессионный пакет. Не считать весь остаток готовым по одному тесту.
- Первый PHPStan отметил три лишних аргумента scale у add/subtract (эти методы сохраняют десятичную точность без параметра). Аргументы удалены, повторный PHPStan session 43500; предыдущая 8332 завершена. DB session 2605 остаётся текущей.
- DB session 2605 завершена успешно: 2 теста / 20 проверок. PHPStan session 43500 успешно. Синтаксис сервиса/миграции и diff-check прошли. Порт возвращён перед сохранением блока. Это подтверждает unit/total и повторное сохранение в проверенном сценарии; остальные ограничения выше остаются открытыми.
- Backend-блок сохранён `dd205ae19`.

## Девятый блок: объяснение остатка в форме (в работе)

- Admin CBM `most-unified-finance-admin` успешно проиндексирован, generation 2026-09-12T14:22:19Z. В форму добавлены типы accepted_basis/condition_basis и краткая сводка сохранённой принятой части. Названия полей уточнены: общий объём/общая сумма привязки; поясняется применение цены единицы к остатку.
- Сводка пока использует сохранённый accepted_basis из ответа. После первого появления факта до редактирования привязки необходимо отдавать актуальную принятую часть в report/preview; это ещё требуется реализовать. Визуальная браузерная проверка и выбор нескольких условий в мастере актов не выполнены.
- ESLint session 98045 прошёл (только уведомление старого Browserslist, зависимости не обновлялись), tsc session 64566 / `finance-remainder-ui-tsc.log`. Изменения UI не закоммичены.
- Согласована независимая задача 01a095fb-70ad-7ca0-b5b6-abba579cdad0: она исправляет импорт labor_hours из is_not_accounted ресурсов и пустые Chip EstimateTableView/estimateItemHelpers, выпускается отдельно. Эти участки пока не менять, её правки затем принять из main. Сейчас тестовый PostgreSQL занят этой задачей; наши DB-запуски завершены. Не запускать launcher до сообщения об освобождении или проверки живых процессов.
- TypeScript session 64566 завершена успешно. После неё добавлены optional can_view_execution и пояснение недоступности факта; повторный ESLint запущен. Для актуальной принятой части report теперь читает acceptedFacts только при act_reports.view и маскирует accepted_basis/condition_basis без права. История также скрывает эти поля. При наличии права факт виден до первого редактирования привязки. Добавлены проверки актуального факта, отсутствия запросов к актам при запрете и маскировки истории; DB-запуск ещё требуется после освобождения контейнера. PHPStan session 51740 / finance-execution-access-phpstan.log.
- PHPStan 51740 успешно, ESLint 14055 успешно. Admin Vitest session 65129 / `finance-remainder-ui-vitest.log` выполняется. В задачу 01a095fb-70ad-7ca0-b5b6-abba579cdad0 отправлен запрос освободить контур после её DB-пакета; не пересоздавать контейнер в промежутке между её проверками без ответа.
- Vitest 65129 завершён: 7 тестов успешно. UI сохранён в task-ветке. Соседняя задача подтвердила, что её повторный DB-тест ещё работает; ждём явного освобождения. Наш backend-блок доступа/актуального факта пока не закоммичен и DB-проверки не пройдены.
- UI-коммит `1eb4cd8e`. Соседняя задача передала контейнер на один запуск и ждёт возврата для финального backfill-теста. Наш DB session 93708 / `finance-execution-access-tests.log` запущен, временный порт 55433. По завершении немедленно сообщить в задачу 01a095fb-70ad-7ca0-b5b6-abba579cdad0 и вернуть порт; до её финального теста больше не запускать launcher.
- Подтверждена карта старых attachItems через CBM trace inbound depth1: JournalContractCoverageService::ensureCoverage, ContractEstimateItemController::attach, ContractEstimateService::syncItems, ContractFromEstimateService::create (4 callers, без усечений). Актуальные snippets attach/sync/updateCoverageVat показывают прямое изменение совместимой проекции, не финансового источника. Адаптация этих путей остаётся следующим крупным обязательным блоком; не считать их объединёнными.
- Session 93708 завершена успешно: 1 тест / 39 assertions, в том числе актуальный факт до редактирования, маскировка метаданных без act_reports.view, отсутствие SQL-запросов к актам при запрете, маскировка истории. Контейнер передан обратно соседней задаче для её финального backfill. Порт возвращён перед коммитом.

## Десятый блок: адаптер старых команд (в работе)

- Backend доступа сохранён коммитом `5b2706219`. Соседняя задача закончила DB и освободила контейнер. Она сообщила main backend aa804fea82e6b54cc4d54c19fed6106273b14f7a (PR691) и admin 68078fdfdbcb6333334e1cbef34aaa8f986ce3f1 (PR374); перед нашим выпуском принять эти изменения из main. Сейчас ещё не подтягивались.
- `ContractEstimateFinanceAdapter::updateVat` преобразует старую команду в save финансового сервиса. Ставка — явно переданная либо уже известная на привязке; ставка сметы не читается. Сохраняются остальные распределения выбранных позиций, ключи управляемых привязок и общая защита прав/факта. Старые связи преобразуются с legacy_link_id, неизвестные условия не подтверждаются автоматически.
- `ContractEstimateService::updateCoverageVat` больше не пишет суммы проекции самостоятельно; требует явного actor. Контроллер передаёт пользователя и необязательный vat_rate, request проверяет ставку 0..100. Старый запрос без ставки допустим только при уже известной ставке; иначе нужна новая форма/уточнение. Её клиентская адаптация ещё не выполнена.
- Тест команды: независимость от ставки сметы, повторное включение без повторного начисления, сохранение allocation/link IDs, неизменная сумма другой стороны, выключение налога, запрет actor-less вызова. Процессы: PHPStan 17638 (`finance-vat-adapter-phpstan.log`); DB 65740 (`finance-vat-adapter-tests.log`), порт временно 55433. Блок не закоммичен.
- Открыты адаптеры attach/detach/sync, клиентские параметры НДС, передача mutation_id/revision и идемпотентность истории старой команды, полная проверка legacy/ресурсов/исключённых строк и старых тестов updateCoverageVat. Это пока только первый путь, не объединение всех API.
- PHPStan 17638 успешно. DB 65740 завершена ошибкой AuthorizationException до assertions: ContractEstimateService зарегистрирован singleton и был создан до mock AuthorizationService. Тест теперь forgetInstance перед получением сервиса; повтор DB ещё требуется. Порт возвращён. Контейнер передан соседней задаче для срочного guard-теста после сбоя её production backfill: production имеет approved_estimate_structure_is_immutable, её новая миграция исключает утверждённые/запечатанные сметы. Наш деплой не запускать до разрешения её выпуска, полный план всё равно ещё не готов.
- Admin EditCoverageVatDialog больше не выводит НДС из разницы сумм и не берёт ставку сметы: требует явного выбора none/exclusive и ставки (0 допустим). Сервис передаёт vat_rate. Независимый от принятого факта предварительный расчёт суммы в браузере удалён, показывается текущая сумма. Клиентские изменения не закоммичены и требуют проверок.
- Повтор команды НДС завершён успешно: finance-vat-adapter-retest.log, 1 тест / 13 assertions. Клиентские тесты EditCoverageVatDialog: 2 успешно, ESLint успешно. Восстановлена прежняя проверка соответствия организации/проекта в updateCoverageVat; тест чужой организации передаёт явного пользователя и ставку. Текущая проверка изоляции: session73700, finance-vat-isolation-tests.log. Проверка типов admin: session43393, finance-vat-typecheck.log. Остальные старые VAT-тесты пока требуют адаптации фикстур к явным условиям и actor, не считать весь пакет совместимости пройденным.
- Соседний выпуск завершён по сообщению его задачи: backend main c8e719b613461e32df9c940a61a4a0e3293374f8, deploy34700511852 success; admin deploy34699981932 success. Перед нашим выпуском всё равно сверить актуальный main и production. Его исправление миграции сохраняет approved/sealed сметы; не снимать этот guard в нашем переносе.
- Проверка изоляции завершена: 1 тест / 1 assertion успешно (finance-vat-isolation-tests.log). Временные порты возвращены; DB launcher завершён.
- Admin tsc --noEmit завершён успешно (session43393, finance-vat-typecheck.log). В старый API НДС добавлен необязательный revision и передача в общий save, чтобы клиент мог получить 409 при устаревшей версии. Расширен регрессионный тест: устаревшая версия не меняет сумму/режим/ревизию, актуальная сохраняется. DB session49266, finance-vat-revision-tests.log; порт временно55433. Клиент пока не передаёт revision, обязательное получение версии и mutation_id/idempotency старого API остаются открытыми.
- Проверки revision завершены: finance-vat-revision-tests.log — 1 тест / 17 assertions успешно; finance-vat-revision-phpstan.log — без ошибок. Временный порт возвращён. Следующий шаг: передача версии из UI и идемпотентность адаптера, затем адаптация оставшихся старых тестов и attach/detach/sync. Полный выпуск не готов.
- Связана версия UI/API: getContractCoverageSummary возвращает finance_revision вместе с существующей сводкой; EditCoverageVatDialog отправляет её в updateVat, без дополнительного запроса финансового отчёта. При отсутствующей версии сохранение блокируется с предложением обновить страницу. Тип поля optional для совместимости остальных потребителей. Тесты формы расширены на передачу версии и блокировку без неё; session52705, finance-vat-dialog-revision-tests.log. ESLint четырёх файлов и PHP syntax успешно. Идемпотентность legacy-команды, старые fixtures, остальные адаптеры остаются незавершёнными.
- Проверка формы завершена: 3 теста успешно (finance-vat-dialog-revision-tests.log). Последний полный tsc был до добавления поля revision; повторить вместе со следующим завершённым UI-блоком.
- Legacy VAT idempotency: API принимает mutation_id; адаптер использует ту же запись estimate_finance_mutations и атомарно дописывает adapter_command_hash в changes. Повтор проверяет actor, hash исходной команды и текущие финансовые/договорные права до возврата. Отличающиеся условия с тем же id дают409. UI хранит id по сигнатуре условий через useRef, при сетевом сбое повторяет его. Без mutation_id старый клиент по-прежнему получает новый id.
- PHPStan без ошибок. UI 4 теста успешно, включая повтор после сетевого сбоя. Первый DB тест упал на предположении revision+1: общий save и существующие обновления повышают revision больше одного раза. Исправлена проверка на неизменность ревизии и condition_version именно между первым сохранением и повтором. Повторный DB session79947, finance-vat-replay-retest.log; временный порт55433. Полный tsc запущен отдельно. Двойное повышение revision само по себе не нарушает optimistic lock, но единый контур ещё требует общего аудита наблюдателей.
- Replay НДС: повторный DB успешно 1 тест / 20 assertions (finance-vat-replay-retest.log); admin tsc session43057 завершён без ошибок. Порт остался55433 для следующего DB-блока.

## Одиннадцатый блок: старое удаление через единый сервис (в работе)
- Добавлен ContractEstimateFinanceAdapter::detach: выбранные условия текущего договора удаляются через finance.save, другие договоры сохраняются, общий guard принятого объёма и история применяются. ContractEstimateService::detachItems требует явного actor, проверяет организацию, загружает сметы по организации/проекту в транзакции и вызывает адаптер. Контроллер передаёт actor и пропускает стандартные ошибки прав/валидации/HTTP вместо500.
- PHPStan трёх файлов успешно. DB session2739, finance-detach-tests.log: новый тест сохранения другого договора и расширенный тест запрета удаления принятого объёма. Порт временно55433. Старый ContractEstimateServiceTest::detach ещё требует адаптации actor/fixture/проекций. Прямая EstimateCoverageService::detachCoverage, sync/attach и ресурсы ещё не переведены. Версия и идентификатор операции для detach ещё не передаются. Не считать все операции унифицированными.
- Первый detach DB успешно: 2 теста / 12 assertions, finance-detach-tests.log. Следующий путь EstimateCoverageService::detachCoverage также переведён на detachItems с явным actor; EstimateContractIntegrationService и HTTP-контроллер передают его, поиск договора ограничен организацией/проектом. Старое изменение журнала работ при отвязке удалено: финансовая команда не переписывает выполненный факт. Это требует общей регрессии интеграции журналов при финальном аудите.
- Выявлена сохранённая нулевая проекция без allocation: countedInCoverage учитывал её как позицию. Добавлен фильтр действующей связи для finance_managed через EXISTS allocations; идентификатор остаётся в таблице. Тест расширен проверкой count0 и отвязкой второй стороны через detachCoverage. Новый DB session96449, finance-unlink-tests.log. Порт временно55433. PHPStan integration3files успешно; модель синтаксически проверена. Общий UI/мобильные читатели проекции и остальные legacy tests ещё требуют аудита.
- Второй unlink DB успешно: 2 теста / 14 assertions (finance-unlink-tests.log). Старые тесты адаптированы: явный actor, авторизация, известные стороны двух договоров для VAT; ставка20 передаётся явно. При detach сохраняются2 строки совместимости, но countedInCoverage=0. Новая DB проверка3сценариев session14119, finance-legacy-compatibility-tests.log; порт55433.
- По текущим исходникам resolveChildrenForDetach захватывает один уровень parent_work_id и не r:ресурсы. Это остаётся отдельным незавершённым требованием при переводе legacy composition; не считать поддержку удаления всей ресурсной структуры законченной.
- Legacy compatibility завершена успешно: 3 теста / 11 assertions (finance-legacy-compatibility-tests.log). Порт возвращён45433. Блок VAT/replay и первые два пути detach готов к локальному коммиту; весь план и остальные legacy adapters не завершены.

## Двенадцатый блок: полный вложенный состав (в работе)
- resolveChildrenForDetach заменён обходом adjacency map всех parent_work_id в сметах договора. Один запрос дерева, далее очередь/seen в памяти, без запроса на каждую строку и без зацикливания при повреждённом графе. Расширен прежний тест третьим уровнем: три строки совместимости сохраняются, активное покрытие0.
- DB session70068, finance-nested-detach-tests.log; PHPStan session89536, finance-nested-detach-phpstan.log. Порт временно55433. Это покрывает вложенные EstimateItem; отдельные r:ресурсы в адаптере ещё нужно включить в состав и проверить подтверждение изменений состава.
- Вложенные позиции: DB успешно 1 тест / 3 assertions (finance-nested-detach-tests.log), PHPStan успешно. Добавлены r:ресурсы в detach по estimate_item_id с обязательным включением родителя в target_keys и confirm_resource_changes. Поиск смет дополняется financial allocations для ресурсной привязки без старой проекции. Тест: подрядчик только на ресурсе, заказчик на родителе; удаляется ресурсное условие, доход сохраняется. DB session97281, finance-resource-detach-tests.log; PHPStan запущен finance-resource-detach-phpstan.log. Порт55433. Проверка принятого факта именно ресурсного состава и вложенных смет с resource-only links требует дальнейшего аудита.
- Ресурсный detach DB успешно: 1 тест / 2 assertions (finance-resource-detach-tests.log), PHPStan успешно. Полная отвязка теперь собирает item IDs также из allocations; обход дерева определяет сметы объединением проекций и allocations, включая resource-only договор. Тот же тест переведён на публичный detachCoverage для проверки полного пути. DB session66270, finance-resource-unlink-tests.log; PHPStan session72257, finance-resource-unlink-phpstan.log. Порт55433, блок пока не коммитить до результата.
- Полный ресурсный unlink DB успешно: 1 тест / 2 assertions (finance-resource-unlink-tests.log), PHPStan успешно. Порт возвращён45433. Проверенный блок вложенных позиций и ресурсов сохраняется отдельно.
- Следующий attach/sync: граф inbound attachItems depth1,4 production callers (без truncation): JournalContractCoverageService::ensureCoverage, ContractEstimateItemController::attach, ContractEstimateService::syncItems, ContractFromEstimateService::create. ensureCoverage автоматически вызывает attach при отсутствии связи без actor. При переходе на единый процесс нельзя автоматически угадывать цены/НДС/направление из журнала; требуется сохранить поведение регистрации факта без создания молчаливых подтверждённых условий. Граф callers ensureCoverage неполон (0), нужен ограниченный source-поиск вызывающих обёрток и фактической цепочки.

## Тринадцатый блок: создание через финансовый сервис (в работе)
- ensureCoverage: ограниченный source-поиск по всему app нашёл только определение, явных вызовов нет. Не считать автоматическую регистрацию журнала доказанным активным путём; динамические вызовы не проверены. ContractFromEstimateService::create имеет actor и атомарный dossier replay guard.
- Добавлен adapter.attach: существующие условия сохраняются, новые корневые позиции получают выбранные сметные цены как договорную базу и явный none/exclusive VAT; неизвестная ставка при includeVat требует ввода. Исключённые строки пропускаются, включённые дочерние строки не добавляются второй раз. Отдельно выбранный дочерний элемент требует уточнения состава. Все финансовые права и квоты общего save сохраняются. Повтор attach существующей позиции не меняет цены/версии. Метод пока НЕ подключён к старым attach/sync/controller/create; это следующий обязательный шаг.
- PHPStan успешно, DB session47284 finance-attach-tests.log: сохранение договорной цены после изменения сметы и повторного attach. Порт55433. Неизвестные legacy стороны/налоги, размеры запросов, UI-предпросмотр и атомарные обе стороны остаются в полном плане.
- Adapter attach DB успешно: 1 тест / 5 assertions (finance-attach-tests.log). Теперь ContractEstimateService::attachItems полностью делегирует адаптеру и требует actor, больше не создаёт проекции напрямую. Контроллер передаёт actor/vat_rate и не превращает validation/auth/HTTP ошибки в500; request принимает vat_rate. ContractFromEstimateService передаёт actor и новый optional vatRate, но вызывающий DTO/controller ещё должен передать ставку.
- Тест переведён на публичный attachItems: session98542 finance-attach-api-tests.log. PHPStan session45649 finance-attach-api-phpstan.log. Порт55433.
- ВАЖНО: переход ещё НЕ завершён и не готов к коммиту/релизу. syncItems пока вызывает attachItems без actor и независимо удаляет проекции; нужно заменить на адаптер и протянуть actor/rate через EstimateCoverageService, EstimateContractIntegrationService и HTTP. Неиспользуемый ensureCoverage и многочисленные legacy тесты также требуют явного actor/новых ожиданий либо удаления недействующего пути. Прежние зелёные legacy тесты после смены attach больше не являются доказательством текущей совместимости.
- Публичный attachItems DB успешно1/5 (finance-attach-api-tests.log), PHPStan успешно. syncItems заменён: собирает удаляемые item IDs из проекций/allocations, вызывает общий detach и attach внутри одной транзакции с actor/rate. attachFullCoverage/syncCoverageItems/linkToContract/HTTP протягивают actor/rate. DB новый rollback test: при неизвестном НДС после попытки удаления прежние условия/ревизия сохраняются, при явной20% замена даёт240000. session DB записать по результату tools; finance-sync-tests.log. PHPStan session2079 finance-sync-phpstan.log. Порт55433.
- Открыто: старый backfillLegacyCoverage вызывает attachFullCoverage без actor; его нужно перевести в новый повторяемый перенос, а не угадывать пользователя. Неиспользуемый ensureCoverage и старые тесты ещё не адаптированы. Передача vatRate в создание договора из API пока не завершена. Полный набор legacy test после замены attach/sync не пройден.
- DB sync rollback: session79414 (finance-sync-tests.log), продолжать наблюдение за этим процессом, не запускать второй launcher до завершения.
- Sync rollback DB успешно: 1 тест / 5 assertions (finance-sync-tests.log), PHPStan успешно. Порт возвращён45433. CreateContractFromEstimateRequest принимает coverage_vat_rate отдельно от полей договора, controller передаёт её в сервис и пропускает стандартные validation/auth/HTTP исключения; PHPStan session86523 finance-create-vat-phpstan.log. UI создания договора ещё должен передавать поле.
- Открытый риск идемпотентности создания dossier: проверить, входят ли itemIds/includeVat/coverageVatRate в существующий request hash. Сейчас вызов create переиспользует dossier replay guard, который может не учитывать состав привязки. Не считать эту защиту завершённой без отдельного теста изменения состава/НДС при том же ключе.
## Проверка идемпотентности создания договора (следующий блок)
- Подтверждено source чтением ContractDossierCreationService::create: сначала возвращает любой ContractDossierSource с organization/source_type/source_id, затем договор по dossier_creation_key; сравнения запроса нет. Controller задаёт source_type=estimate/source_id=estimateId. Это мешает нескольким новым договорам из одной сметы и может считать изменённые условия повтором.
- ContractDossierCreationInput readonly содержит contract, key, document metadata/sourceLinks/sourceidentity; hash/context отсутствует. Не менять глобальную semantics всех источников без необходимости. Нужна защита именно создания из сметы: идентичность операции отдельно от идентичности исходной сметы, snapshot/hash состава и НДС, повтор с отличающимися условиями409, прежние источники/документы сохранить. Использовать существующий финансовый журнал команд либо минимальное расширение, без второго финансового контура.
- В tests/Feature/Contract/ContractDossierCreationServiceTest.php найден единственный targeted test_invalid_estimate_items_do_not_create_a_partial_contract (204–233 в индексе), мокирует ContractFromEstimateService dependencies, проверяет отсутствие частичного договора. Добавить сценарии повторов/другого состава/нескольких договоров, не выдавать этот тест за покрытие идемпотентности привязки.
## Четырнадцатый блок: операция создания договора из сметы (в работе)
- ContractFromEstimateService теперь принимает FinanceAccess, проверяет финансовые права и фиксирует запрос в estimate_finance_mutations (детерминированный uuid5 от estimate/key). Hash включает DTO договора, отсортированный состав, includeVat/ставку, документные параметры. Источник dossier для новых операций estimate_contract_creation/operationUUID вместо общей estimate/id; sourceLinks исходной сметы сохраняются. Разные ключи могут создавать разные договоры из одной сметы. Старые источники/документы не меняются.
- Повтор сравнивает actor/hash, права изменения договора и contract_id из receipt. Старый dossier replay без новой receipt возвращает409: состав прежнего запроса неизвестен, молча считать его совпавшим нельзя. После успешного создания/привязки receipt пишется в той же транзакции. PHPStan finance-create-replay-phpstan.log успешно. Нужны targeted DB тесты повторов, других условий, разных ключей, rollback и доступа. Старый тест invalid items обновлён под4й constructor dependency, ещё не запущен.
- Блок не проверен функционально и не готов к коммиту. Проверить также вызовы new ContractFromEstimateService за пределами известного теста, поддержку нового sourceType внешними читателями и прошлые ключи/перенос. Порт сейчас45433, активных DB запусков нет.
- Create invalid-input test успешно1/2 finance-create-invalid-tests.log. Найден и обновлён второй direct constructor в ContractSourceIntegrationTest. Интеграционный тест с настоящим PG receipt/dossier source (мок записи самих финансовых условий) успешно1/8 finance-create-replay-tests.log: перестановка items и20/20.0 повторяют договор, изменение состава/ставки конфликтует, другой ключ создаёт второй договор. Это не заменяет full end-to-end проверку создания фактической привязки/архива.
- Соседней задаче01a08688-81ff-7af0-8b69-da897d430cf8 передан DB-контур после завершения всех моих процессов. До сообщения об освобождении не запускать DB. По явной координации добавлен общий mutex Local\MostPostgresTests в launcher вокруг reset/up/tests с finally Release/Dispose, без CI изменений. Parser проверен, конкурентную проверку самогоmutex ещё провести. Порты возвращены45433.

## Вложенный состав старого sync (следующий блок, не завершён)
- Предыдущий goal-turn дал прогресс: DB проверки создания/replay сохранены. В этом продолжении обнаружен resolveWithChildren только первого уровня; заменён на один запрос id/parent_work_id и обход очередью с seen для всех уровней. PHP lint и APP_ENV=testing PHPStan по ContractEstimateService успешно (finance-nested-sync-phpstan.log). Без APP_ENV testing bootstrap падает storage_configuration_invalid; не подставлять фиктивные credentials.
- Новый test_sync_keeps_existing_conditions_of_deeply_nested_positions в EstimateFinanceTest пока НЕ ПРОШЁЛ. Первый запуск ошибался fixture: пытался менять parent_work_id после allocation, DB guard корректно отклонил. Второй запуск (session41792 завершён exit2) выявил запрет revenue для дочернего target (EstimateFinanceService::normalize примерно364). Fixture теперь создаёт иерархию до save и использует contractor cost для root/leaf, confirm_resource_changes=true. Эту последнюю версию ещё запустить. Не выдавать тест за успешный, не деплоить блок.
- DB окно передано задаче01a08688-81ff-7af0-8b69-da897d430cf8 под7регрессий ПИР. Все наши DB процессы завершены, порты45433 восстановлены. Mutex Local\MostPostgresTests фактически использован двумя нашими launcher, иных инфраструктурных ошибок не было. Дождаться освобождения соседней задачи или безопасно поставить очередь черезmutex после согласования.
- Дополнительно обнаружен старый calculateItemsTotal: суммирует все resolveWithChildren через calculateAmount, который не исключает parent_work_id и использует VAT сметы. Trace inbound единственный production caller EstimateCoverageService::validateContractAmount. Нужно перевести этот preview/validation на общий расчёт, иначе после расширения иерархии независимое суммирование ещё заметнее. Это обязательный незавершённый потребитель этапа1, не утверждать согласованность всех сумм.

## Общий предварительный расчёт привязки (в работе)
- ContractEstimateFinanceAdapter::rootKeys общий для attach и initialAmount. calculateItemsTotal теперь делегирует initialAmount: денежная арифметика FinanceDecimal, EstimateFinanceTax, только корневые неизключённые строки, явная ставка НДС вместо ставки сметы. Старый calculateAmount остаётся неиспользуемым и ещё требует удаления после проверки вызовов.
- vat_rate протянут в ValidateEstimateContractAmountRequest/controller/integration/coverage. UI потребителей validate ещё адаптировать; include_vat=true без явной ставки для новой привязки теперь422. Существующие привязки берут сохранённые суммы. Права preview и неизвестные исходные цены остаются в полном аудите.
- PHPStan всех6изменённыхproductionPHP успешно finance-preview-phpstan.log; php-l7файлов иgitdiffcheck успешно.
- Предыдущий nested test32770 завершён с composition guard: target_keys не включал непосредственного родителя leaf. Fixture теперь добавляет child key и сохраняет cost conditions. Новый тест initial_amount проверяет родителя/ребёнка/исключённую строку, VATсметы5 vsявная20, совпадение attach1200000, missingVATreject. Текущий общий DB session69250 finance-preview-tests.log (2теста), продолжать наблюдение; порты55433 пока НЕ восстанавливать до окончания. Не считать проверки успешными до результата.
- Пользователю честно сообщено: приблизительно60–70% полного плана ещё впереди, раньше темп был оценён оптимистично; полногоUI/факта/денег/переноса/деплоя нет.
- DB session69250 завершён успешно: 2tests/8assertions, 02:04.772, finance-preview-tests.log. Вложенные условия/ID/ревизия сохранены при sync; preview/attach суммы совпали, исключённая строка и включённый ребёнок не добавлены повторно; явныйVAT20 независим отVATсметы5. Порты45433 восстановлены, своих DB процессов нет. Следующая задача ПИР ставит QualityControlMobileTest черезmutex. Полный блок и весь выпуск всё ещё не готовы: UI vat_rate, старые fixtures, перенос, полныйплан открыты.

## Передача НДС создания договора в админке
- В admin CreateContractFromEstimateDrawer добавлены явные none/exclusive и поле ставки, без подстановки из estimate.vat_rate. При missing/nonfinite/outside0..100 отправка блокируется; 0% отправляется как exclusive/rate0, none как false/null. CreateProjectContractFromEstimate payload типизирован дополнительными include_vat/coverage_vat_rate. Старый validateContractAmount API-клиент принимает необязательный vatRate и передаёт параметр; текущий ContractAmountValidator всегда использовалfalse, не требует нового поля для просмотра сохранённого покрытия.
- Тест формы расширен: blankrateблокирует,0%передаётся, повторпослеошибки используеттотжеkey, обычныйnoneпередаётnull. Vitest2/2 успешно finance-create-tax-tests.log. ESLint4файлов успешно finance-create-tax-lint.log. TSC session28472 finance-create-tax-types.log ещё наблюдать. UI не закоммичен.
- ВАЖНО: это совместимость старой формы, не вся новая единая форма. Здесь пока только none/exclusive как в старом API; included в общемFinanceDrawer есть, объединениеещёвпереди. CreateContractFromEstimateDrawer всё ещё сам суммирует selectedItems для initialData/notes (обнаруженоgraphread); нужно заменить сервернымpreview, чтобы выбранные работа+ресурсы не увеличивали начальнуюсуммудоговора. Полныйe2e создание+финусловия+архив не выполнен. Backendне деплоить отдельно от завершенияcompat.
- TSC session28472 завершён exit0: finance-create-tax-types.log пустой, ошибокнет. Проверки текущих4adminфайлов завершены:2Vitest,eslint,tsc,diffcheckуспешно. СвоиDB/Nodeпроцессы завершены. Следующий приоритет: заменить клиентскую сумму selectedItems сервернымpreview и продолжить единуюформу, затем остальныеполныеэтапы.

## Серверная исходная сумма создания договора
- Новый EstimateFinanceSelection объединяет rootKeys/amount для adapter.attach/initialAmount и finance.preview. Операция source_amount расширяет существующий preview, принимаетitem_ids<=20000, возвращает netamount/count/currency/revision. Новый PreviewEstimateFinanceRequest отдельно отSaveRequest: save не принимает этуоперацию. ПроверкаFinanceAccessedit остаётся в сервисе. Проекции/условия не записываются.
- Удалён клиентский reduce selectedItems и выдуманная разбивка notes из CreateContractFromEstimateDrawer. estimateFinanceService.sourceAmount запрашивает существующий preview. Форма появляется после успешного расчёта; ошибки даютRetry, сменаselectionотбрасываетстарыйответ, безpreviewсозданиеблокируется. Начальнаясуммавалидируетсяfinite/RUB/nonemptycount. УсловияНДСпривязкиотдельны.
- Backend PHPStan5файлов успешноfinance-source-preview-phpstan.log,phplint5успешно,diffcheckуспешно. DB95838завершён:1test/8assertions,01:45.384,finance-source-preview-tests.log. Провереноsourcepreviewnet1m/count1беззаписей, совпадениеattachсНДС20%1.2m,исключениеchild/excluded. Это сервисныйтест,неfullHTTP/e2e.
- Frontend3Vitestуспешноfinance-create-preview-tests.log (serveramountvslocal9m,ошибка/retry,НДС0/repeatkey,existingdossier),eslintуспешноfinance-create-preview-lint.log. TSC93212finance-create-preview-types.log ЕЩЁ выполняется; продолжатьэтотhandle, не перезапускать. СвоиDBзавершены,порты45433восстановлены;контурпередансоседнейПИРзадаче.
- Открыто: HTTPnegative/sourcesaveguardtests, неизвестныесметныецены(вqueryпокаdefault0), целостныйsnapshotусловийприизменениисметымеждуpreview/create, типизированныйединыйUIсобеимисторонами/включённымНДС,весьостальнойплан. Не считать20000itemIdsрешениемвсех8kограничений: основныесохранениявсёещёограничены1000targets/5000lines.
- TSC93212 завершёнexit0,finance-create-preview-types.log пустой. Все запущенные проверки данного блока завершены; DB/node активных нашихпроцессовнет. Измененияbackend/admin пока не закоммичены. Следующийблок: полноценнаяединаяпривязкавместоразрозненныхформ,аудитстарыхfixtures/потребителей,акты/оплаты/перенос и весь remaining scope.

## Массовая подготовка обеих сторон в финансовом окне
- Добавлен admin FinanceBindingSetup в существующий EstimateFinanceDrawer: два договора по server-side side(revenue/cost), независимыеnone/exclusive/included/rate/подтверждениесостава. Создаёттольконовыеrootлинии,пропускаетexcluded/children/ужеимеющийсятотжедоговор. Серверный preview estimate_prices принимаетоднимзапросомвсе additions вместе с сохранённымиlines; приошибке local linesнеизменяются. Денежныхклиентскихвычисленийнет. Итоговыйsaveостаётсяобщиматомарнымsave.
- Это подготовка полных объёмов новых root-позиций по сметным ценам. Частичныеобъёмы/общаясумма/изменениеценредактируютсяниже существующим UI. Уже распределённые другим договорам объёмы проверяетсервер, при превышении previewотклоняется. Удобнаямассоваячастичнаяпривязка и массоваяцена/НДСостатков ещёне закончены.
- Новые2теста проверяютобестороны/0vsnone,сохранениесуществующейстроки,исключениеchild/excluded,unknownVATблокировку. Вместес7Workspaceтестами9/9PASS finance-binding-setup-tests.log. ESLint3файлаPASS finance-binding-setup-lint.log. TSC71853 finance-binding-setup-types.log ещёжив,дождаться.
- Затем workspace defaultbasis заменённаwithout_vat,действиепереименованоПривязатькдоговору,drawerПривязкакдоговорам/Сохранитьпривязки. Повторworkspace6/7прошёл,1ожидалстарыйзаголовокwithVAT. Fixtureобновлена:initialПлановаямаржа,послепереключенияРазницасНДС. Узкийповторsession40534 finance-binding-basis-tests.log. ESLintworkspace/drawer/testPASS finance-binding-workspace-lint.log. Не считатьповторпрошедшимдозавершения.
- DBнеиспользовалась. СоседняяПИРзадачасообщилаотретьемlauncherбезmutex worktreeestimate-revisions-backend;онасвязываетсясвладельцем. Не перезапускатьконтейнервручную,толькособщимmutex/координацией.
- TSC71853 завершёнexit0;узкийbasis40534успешен1/1(остальные6неперезапускались,онипрошливпредыдущемпакете). Всеактивныеfrontendпроверки завершены. Scopeполнойцелиоткрыт: договорнаявкладкаещёнеиспользуетединыйeditor,связьвыбораосновнойтаблицысbulkformнеполная,план/факт/деньги/миграция/деплойне завершены.
- Admin checkpoint commit8eec27e9 сохранёнлокально:10файлов,сервернаясуммасоздания/явныйVAT/подготовкаобеихсторон/defaultwithoutVAT. Непушен,неPR,недеплой. Backendизмененияостаётсянезакоммиченными;неоткатывать.

## Редактирование привязки из договора
- ContractEstimatesTab заменяет узкийEditCoverageVatDialog на ContractEstimateBindingEditor через «Изменить привязку». NestedProtectedAction требуетcontracts.edit+finance.view+finance.edit. НовыйeditorдополнительноchecksusePermissionsпередHTTP,загружаетобщийfinance.reportwithoutVAT,выбираетallocationsтекущегодоговора+financeSelectionrelatedparents/resources,сохраняявсеlinesпрочихдоговороввэтихrows. ПередаётданныевтотжеEstimateFinanceDrawer. onsavedзакрываетeditor/деревоиперезагружаетsummary,чтобыприраскрытиидеревозагрузилосьзаново.
- Loading/errorRetry/empty/nopermissionsпоказанывDrawerShell. AbortControllerотбрасываетстарыеответы. can_edit=falseсерверногорепортаоставляетобщийeditorreadonly. Полнаясервернаяпроверкаправназатронутыедоговорывsaveостаётся.
- Новые2MSWтеставыборки/сохраненияother-link/norequestприnopermission+старый1ContractEstimatesTabtestуспешны(3/3,finance-contract-editor-tests.log). ESLintнетошибок,1ранееимевшеесяwarninguseEffectloadSummarydependencyвContractEstimatesTab(finance-contract-editor-lint.log). TSCsession92167finance-contract-editor-types.log ещёжив;дождатьсятого жеhandle. DBнет,контейнерне трогал.
- Крупныеостаткинеуменьшать: нетendtoendизэтойкнопкичерезнастоящийsave/утверждённыйакт,общаявкладкасуммпокаlegacySummary,основнаявыборкасметыещёнесоединенасbulkeditor,8kUI/saveлимитыоткрыты,план/исполнение/деньги/перенос/деплойне завершены.
- TSC92167exit0,finance-contract-editor-types.logпустой;своихпроцессовнет. Editorдоговорнойвкладкипроверенлокально,неproduction. Измененияadminсохраненыотдельнымлокальнымкоммитом(смgitlog);непуш/неPR/недеплой.

## Выбор основной таблицы сметы → общая привязка
- EstimateDetailPage больше не открывает AttachToContractFromEstimateDialog по выбраннымпозициям: удаленыимпорт/состояние/старыйhandlerсобходомлокальныхitems/старыйbutton/modal. selectedItemIds иonBindingSavedпередаютсявEstimateFinanceWorkspace. ОбщаяtoolbarврежимеestimateпоказываетПривязатькдоговору(N)толькоприreport.can_edit;открываетfinanceSelectionselected IDsсродителями/ресурсами. Послеsavecallbackочищаетвыбор иперезагружаетсмету. Создание новогодоговораоставленоотдельнымдействием.
- AddedWorkspaceMSWtest проверяетвыборi2+childi3безпостороннегоi1,PUTpayloadtarget_keys,callbacksaved. Первичный8testspackage7PASS/1FAILиз-заPOSTmockвместоPUT;mockисправлен,узкийповтор93429 finance-table-binding-retest.log. Общийлогfinance-table-binding-tests.logоставитькакнеуспешныйпервичныйзапуск. ESLint3файловуспешноfinance-table-binding-lint.log. TSC79528finance-table-binding-types.log ещёнаблюдать.
- НетDBзапусков. Всеостальныетребованияполнойцели остаются: частиобъёмов/договорныесводки/платежи/факт/миграция/права/нагрузка/deploy. Не считатьтестпустьemptylinesсохраненияполнымe2eдоговора;этоинтеграциявыборасобщимредактором,финансысохраняетсуществующийsave.
- Узкийповтор93429PASS1/1;предыдущие7тестовнеперезапускалисьбезизмененийихлогики. TSC79528exit0. Кодсохранёнлокальнымadminкоммитом(смgitlog),непуш/недеплой. Активныхнашихпроцессовнет.

## Совместимость старых тестов привязки с едиными условиями
- ContractEstimateServiceTest передаёт пользователя в attach/fullCoverage, задаёт явное направление договора и ставку НДС. Проверки НДС используют ставку сметы 5% и договорную 20%, без неявного наследования.
- Включённый ресурс больше не ожидается как отдельная оплачиваемая проекция. Сумма работы 10 000 остаётся 10 000, ресурс сохраняет родителя в смете. Разные стороны договора допускают привязку одной позиции одновременно.
- Исторические вложенные связи для проверки удаления создаются напрямую; идентификаторы проекций остаются, countedInCoverage исключает удалённые. Исключённая старая строка с суммой 90 млн создаётся напрямую и не влияет на сводку/видимые позиции/валидацию.
- PostgreSQL launcher с Local\MostPostgresTests: finance-legacy-adapter-tests.log — 12 из 13 прошли, первый остановился на доступе к проекту. Fixture уточнён: is_archived=false, all_projects, сервис разрешается после actingAs. Целевой повтор finance-legacy-parent-retest.log успешен: 1 тест, 4 проверки. Остальные 12 не повторялись. PHP syntax и git diff --check пройдены. Порт профилей восстановлен на 45433, своих процессов нет.
- Следующий конкретный риск: EstimateCoverageService.attachFullCoverage и syncCoverageItems вызывают CompletedWorkFactService.syncJournalEntriesForContractEstimateCoverage. Тот вызывает syncFromJournalEntry, пересохраняющий/удаляющий CompletedWork. Нужно убрать побочный пересчёт исторического факта из команд условий и добавить регрессию; пока код этих вызовов НЕ изменён. CBM generation 2026-09-12T13:44:58Z, metadata_changed; точный snippet показал текущие вызовы, syncCoverageItems дочитан ограниченно.
- Новые изменения не выпущены. Полная цель, включая план/факт/деньги, миграцию, 8k, права, сверку production и штатный деплой, остаётся открытой.
## Защита выполнения от побочного пересчёта при привязке
- Удалены два вызова syncJournalEntriesForContractEstimateCoverage из attachFullCoverage/syncCoverageItems и неиспользуемая зависимость CompletedWorkFactService. Сервис журнала не изменён: обычные операции журнала сохраняются.
- Добавлен EstimateFinanceTest.test_coverage_commands_do_not_resynchronize_existing_execution: запрещает побочный вызов, проверяет неизменность атрибутов существующего confirmed CompletedWork после обеих команд и стабильность проекции.
- PHP syntax пройден. PHPStan первоначально не загрузил storage config в обычном окружении; штатный APP_ENV=testing повтор finance-coverage-history-phpstan-retest.log успешен, ошибок нет.
- Первый PostgreSQL тест finance-coverage-history-tests.log остановился на авторизации, до проверок поведения. Исправлена подготовка: сбрасываются ранее разрешённые singleton ContractEstimateService и EstimateCoverageService после тестового mock авторизации.
- Повтор ЕЩЁ ЖИВ: exec session63889, PowerShell PID27792, finance-coverage-history-retest.log. Launcher ждёт общийmutex; не перезапускать! На 20:38 подтверждён владелец другого активного прогона: php28504, launcher23016 estimate-rollback-backend. Соседний ПИР launcher1884 тоже ждёт. ПИР подтвердил движение очереди, не зависание. Проверить этот же handle до любых новых DB запусков.
- Временные профили сейчас55433 до завершения wrapper, finally восстановит45433 исходного checkout. При интеграции актуальногоmain привести к каноническому55433 без отдельной новой инфраструктуры. Полная цель и деплой не завершены.- Повтор finance-coverage-history-retest.log завершён exit0: 1 тест, 5 проверок, 01:44.927. Exec63889 терминальный, mutex освобождён, wrapper восстановил исходные порты. Первоначальная ошибка была в singleton тестовой подготовки. Проверка сохранности выполнения теперь подтверждена локально.
- Начата сверка чтения сумм: ContractEstimateService.calculateContractEstimateTotal использует SQL sum(amount) по countedInCoverage, getSummary — collection sum(amount); EstimateCoverageService.getContractCoverageSummary — collection sum(amount/amount_without_vat) проекций. Это ещё не прямой общий read model финансовых условий. Требуется объединение потребителей с учётом неизвестных условий и совместимости старых связей; никакой замены этих методов в этом пакете ещё нет.
## Суммы договора из финансовых условий
- Добавлен ContractEstimateItem.scopeWithFinancialAmounts: один grouped join по allocation projection ID, точные SQL SUM с сохранением NULL при неизвестной цене. Если allocations отсутствуют, используются старые значения проекции. Явная выборка колонок не допускает двойных amount aliases в SQL подзапросе.
- Подключено к getItemsForContract, calculateContractEstimateTotal (SQL SUM из подзапроса), getSummary, getCoverageForEstimate и getContractCoverageSummary. Чтение не пишет в БД и сохраняет ID. Финансовая quantity пока берётся из совместимой проекции.
- Новая регрессия test_contract_reads_financial_conditions_instead_of_stale_projection_amounts: финансовая сумма 1 млн, старая проекция 180 млн; все пять путей показывают 1 млн, ID тот же, физическая проекция остаётся 180 млн. PostgreSQL finance-summary-source-tests.log PASS 1 тест/7 проверок, 01:53.563, exec30046 завершён. PHPStan finance-summary-source-phpstan-retest.log PASS (первичный запрет collection sum исправлен SQL aggregate).
- Ограничения следующего пакета: старые сериализаторы sum/float ещё могут превращать неизвестные суммы в 0; нужно переносить полноту данных/null на API и UI, а не объявлять финансовую сводку полностью готовой. Требуются проверки legacy fallback, нескольких распределений/валют, bounded organization/project и нагрузки grouped join на 8k. Остальные потребители проекта/Excel ещё не все объединены. Новые изменения пока не коммит/не деплой. Своих процессов нет; портwrapper восстановлен.
## Неизвестные суммы в сводке договора
- Добавлен EstimateFinanceAmounts.sum: десятичное накопление и распространение null. getContractCoverageSummary использует его для gross/net и общего итога; неизвестный итог даёт null также для процента/остатка/перекрытия/средней/максимальной суммы. Количество позиций остаётся известным.
- Admin ContractEstimateSummary допускает null в денежных показателях. ContractEstimatesTab показывает «Требуется оценка», скрывает денежный progress при неизвестном проценте и не выводит ложное превышение. Старый EditCoverageVatDialog безопасно показывает неизвестную сумму и блокирует сохранение без базовой цены.
- PostgreSQL finance-summary-null-tests.log PASS2/19, включая предыдущую регрессию stale180млн и новуюunknown/nullvszero. PHPStan finance-summary-null-phpstan.log PASS. Frontend ContractEstimatesTab2/2PASS finance-summary-null-tests.log; EditCoverageVatDialog5/5PASS finance-vat-null-tests.log. TSC первоначально нашёл nullable в старомVATdialog, после исправления finance-summary-null-types-retest.log exit0. ESLint3основныхфайлов0ошибок,прежнийwarningloadSummarydependency. Все свои процессы завершены.
- Граница: null обработан в contract coverage summary и его UI. getCoverageForEstimate/getSummary/calculateContractEstimateTotal и другие потребители всё ещё требуют аналогичной обработки неизвестных сумм, UI дерева и общий проект/Excel не завершены. Legacy fallback и 8k нагрузка remain. Не считать полный план выполненным; деплоя нет.
## Неизвестное покрытие на стороне сметы
- getCoverageForEstimate использует EstimateFinanceAmounts.sum. validateContractAmount сохраняет неизвестную сумму вместо float0 и возвращает valid/difference/percentage_difference=null с локализованным сообщением о заполнении цен и налоговых условий.
- Admin nullable типы EstimateContractValidation/ContractEstimateCoverage. EstimateContractSection показывает «Требуется оценка». ContractAmountValidator выводит info без нулевых чипов/ложного расхождения. Старый LinkContractDialog показывает info, не предлагает подтвердить неизвестное значение как отклонение и не отправляет такое сохранение.
- PostgreSQL finance-estimate-null-tests.log PASS1/19; PHPStan finance-estimate-null-phpstan.log PASS. Frontend новый ContractAmountValidator1/1PASS, LinkContractDialog3/3PASS. TSCfinance-estimate-null-types.log сначала выявил4nullableместа в старомLinkDialog; после исправленияfinance-estimate-null-types-retest.log exit0, exec56476 завершён. Lint5начальныхфайлов0ошибок/прежнийwarningeffectvalidateAmount. Свои процессы завершены, diffcheckPASS, production не трогали.
- Ещё открыты getSummary/calculateContractEstimateTotal unknown handling, список/tree serializers, источники исполнения/денег, миграция, нагрузка и полный deploy. Этот пакет не означает завершения цели.
## Неизвестные суммы списка позиций и договорная цена
- ContractEstimateService.calculateContractEstimateTotal возвращает nullablefloat и SQL CASE COUNT/SUM, getSummary применяет общий null-preserving decimal sum. ContractEstimateItemResource сохраняет amount=null и добавляет contract_unit_price из суммы/объёма привязки через FinanceDecimal, не из сметной цены.
- ContractEstimateItemsTree сохраняет неизвестные итоги списка/раздела, копейки складывает целыми. Строка «Цена по договору» использует contract_unit_price (для старого API — сумму/объём), больше не показывает исходный item.unit_price как договорный. ContractEstimateItemLink.amount nullable.
- PostgreSQL finance-items-null-tests.log PASS2/32: известная договорная цена/неизвестный ресурс/nullable service summaries. При проверке resource установлен estimateItem=null, чтобы изолировать денежный контракт; это НЕ тест прав и полного ресурса с выполнением. PHPStanfinance-items-null-phpstan.log PASS, TSCfinance-items-null-types.log exit0. Frontend1known testPASS; unknown первоначально слишкомточныйtextmatcher, после regex целевой finance-items-null-retest.logPASS1, первый не повторялся. Lint0errors/priorloadeffectwarning.
- Важный следующий участок: ContractEstimateItemResource.toArray для каждого item вызывает getActualVolume/getCompletionPercentageForPlannedQuantity/resolveActingQuantities/contracts_count query. Пока не убраны per-row queries, не добавлены полноценная проверка прав факта и отдельный financial execution/cash report. Снимки CBM это подтвердили; следующее изменение требует batch read model и проверки no-forbidden-queries. Не выдавать current monetary tests за выполнение требований 8k/прав/факта.
- Своих живых процессов нет, DBmutex свободен, deploy отсутствует. Полный scope остаётся.
## Пакетная загрузка оперативного выполнения и права
- Новый ContractEstimateOperationalProgress.prepare: проверяет организацию и доступность проекта, принадлежность links договору, отдельно contracts.completed_works.view и contracts.performance_acts.view; закрытый источник не запрашивается. Разрешённые CompletedWork суммируются по item одним запросом, строки актов с документами загружаются общим пакетом. Аннулированные/отклонённые освобождают объём, approved/signed в актировании, остальные в резерве. Возможность действий проверяется contracts.edit / contracts.performance_acts.create.
- Index/attach контроллера готовят прогресс перед Resource; index не превращает AuthorizationException в500. getItemsForContract eager withCount countedInCoverage; attach eager section/unit/children/count. Resource больше не делает getActualVolume/resolveActingQuantities/contracts_count per-row queries; старый private resolveActingQuantities удалён. Без подготовленного прогресса отдаёт null/нет прав, без fallbackSQL. UI показывает Недоступно и info о правах, без ложных процентов, типы nullable.
- Сохраняется ОПЕРАТИВНЫЙ смысл CompletedWork.effectiveForSchedule (вне журнала включает draft/pending/in_review/confirmed, журнал толькоapproved). Это НЕ реализация финансового представления Выполнение, где требуются только утверждённые документы. Отвязанные от договора CompletedWork не назначаются автоматически по единственной связи; нужен будущий явный нераспределённый факт.
- PostgreSQL finance-progress-tests.log PASS2/19, exec99023 завершён: отсутствие SQL к закрытым sources, 30позиций, не более5queries подготовки и0при сериализации; confirmed20/approved5/draftreserve3/annulled7=>available12. Нетпроверки8k/глубокихsections/всехстарыхпотребителейResource. Финансовые accepted snapshots/legacyactpivot отдельные и не заменены этим reader.
- PHPStan4filesPASSfinance-progress-phpstan.log (после него удалён только неиспользуемый private метод/импорты); TSCfinance-progress-types.log exit0; frontend3/3PASSfinance-progress-ui-tests.log; lint0errors/priorloadwarning. Все свои процессы завершены. Новые изменения безproduction/deploy. Остальной полный план открыт.
## Проверка чтения 8001 позиции
- Добавлен test_large_estimate_reads_without_queries_per_position: пакетная подготовка 8000 новых позиций + исходная, 8001 legacy проекция, загрузка списка, оперативного прогресса, Resource сериализация, сводка договора и finance.report.
- finance-large-read-tests.log PASS1/7, 02:33.843 с подготовкой схемы, peak424MB, exec50979 терминальный. Проверено8001строк в списке и отчёте; сериализация не добавилаqueries; весь измеряемыйreadпакет<=40queries; сумма1,800,000/8001позиций правильна. Это synthetic fixture, is_manual=false, без полногоExcelimport/browserDOM/массовогоsave, без всехвариантовиерархий/ресурсов. Не выдавать за всю8kприёмку.
- Следующий обязательный участок: SaveEstimateFinanceRequest ограничивает target_keys1000, lines5000 (также prepare_resource_items/resource_mappings1000). Простое повышение лимита без проверки Laravel wildcard/distinct validation и массовыхwrites недостаточно. EstimateFinanceService.validateInput использует in_array(target_keys) в цикле — для большого сохранения нужен аудит сложности/пакетов. Лимиты пока НЕ изменены.
- EstimateFinanceQuery.targets использует items.contains(id) для представления каждого ресурса и повторный pendingResources/hash по ресурсу; потенциальный CPU N² на ресурсных сметах, хотя в данномfixture нетэтихресурсов. Графgenerationстарый; точные snippets были получены и coverage metadata_changed отмечено.
- Своихпроцессовнет,DBmutexсвободен. Цельне завершена,production/deploy отсутствуют.
## Продолжение: построчная валидация больших привязок
- FinanceInputValidation проверяет массивы по одной строке без глобального разворачивания wildcard-правил. Уникальность проверяется множествами (UUID без учёта регистра, integer ID в числовой форме). Ошибки сохраняют путь конкретной строки; при первой ошибочной строке запрос отклоняется. Непроверенные поля вложенных объектов удаляются.
- FormRequest validator/validated/safe и EstimateFinanceService preview/save используют общий механизм. Source preview также проверяет большие списки без wildcard distinct. Лимиты target_keys подняты до 20000, lines/total_line_keys до 40000. prepare_resource_items/resource_mappings пока по 1000: массовое преобразование ресурсов ещё не завершено.
- finance-input-validation-tests.log PASS4/17, 01:50.118 вместе с подготовкой схемы, peak220MB: 8001 позиция/16002 строки условий, сохранение condition_version, удаление лишних полей, некорректная конечная строка, UUID-дубликат другого регистра, malformed/oversized payload, FormRequest/source preview. После добавления safe() выполнен узкий finance-input-safe-tests.log PASS1/6, 01:26.575, peak158MB. Оба launcher-процесса терминальные; профили восстановлены на исходный порт45433.
- PHPStan3 изменённых production-файла PASS (finance-input-validation-phpstan.log, APP_ENV=testing). Первый запуск без APP_ENV остановился на production StorageServiceProvider bootstrap; конфигурация хранилища не менялась. git diff --check PASS.
- Это проверка входного контракта, НЕ доказательство сохранения 8001 позиции в БД. Следующий участок: N² in_array в normalize/save, пакетная запись allocations/history/projections и реальный большой save с атомарностью/повторным запросом. Полный финансовый факт, перенос, приёмка и деплой остаются обязательными. Цель не завершена.

## В работе: пакетная запись привязок и триггер версии
- НЕ завершено и НЕ закоммичено: EstimateFinanceService записывает/удаляет allocations пакетами500, новые записи через insert (не upsert неизвестного ключа), существующие через upsert с сохранением id/created_at. Проекции contract_estimate_items upsert по contract_id+estimate_item_id с сохранением id/notes/created_at; ссылки allocations проставляются одним коррелированным update. В normalize/save ключи выбранных позиций и общей суммы проверяются множествами.
- FinanceInputValidation.sanitize приводит UUID mutation/line/total keys к нижнему регистру: keyBy результатов PostgreSQL UUID теперь совпадает с вводом другого регистра и не позволяет обойти проверку чужой сметы. Новые тесты rollback-on-history-error и uppercase-other-estimate прошли во втором прогоне.
- Выявлен row-trigger guard_estimate_finance_links: на каждую проекцию UPDATE estimates finance_revision++ (16002 лишних изменений одной строки). Добавлена новая миграция2026_09_12_160000_batch_contract_estimate_revision_trigger.php: statement-level AFTER INSERT/UPDATE/DELETE с transition tables, distinct estimate IDs и упорядоченным обновлением. Остальные source guards не изменены. down восстанавливает прежний row-trigger. В central save итоговая revision теперь locked initial+1, внутренние изменения входят в одну операцию.
- Первый finance-large-save-tests.log завершён exit1: большой save прошёл ограничение<300queries, но assertion revision=1 ошибочен; остальные2 (split/replay, accepted remainder price)PASS. Второй finance-large-save-retest.log завершён exit1: на прежней схеме получено24004 вместо ожидаемых8002 (8001 начальная +16002 проекции+1). Малые rollback/uppercase проверкиPASS. После этого подготовлены финальные service revision+trigger изменения; не считать прежние прогоны проверкой новой миграции.
- PHPStan final3files PASSfinance-large-save-phpstan-final.log; php-l миграцииPASS. Новая проверка прямого bulk insert/update/delete проекций ожидает ровноодно увеличение revision на statement.
- АКТИВЕН launcher exec session66779: finance-large-save-final-tests.log, фильтр6тестов test_large_atomic_save|test_batch_save_rolls_back|test_uppercase_allocation|test_legacy_projection_batches|test_new_price_applies_only|test_customer_and_split_cost. Последний лог: ожидание общего PostgreSQL-контура другой задачи. Это живой процесс; НЕ перезапускать. Временные3профиля сейчас55433, wrapper finally сам восстановит45433. Проверить этот session черезwrite_stdin до новых DBзапусков/commit. Никаких своих другихпроцессовнет.
- Следом подтвердить весь большойsave (8001позиция/16002условия, повтор, invalid-last-line, обновление с сохранениемIDs/history), затем commit толькоуспешныйблок. Полныйфинансовыйфакт, migrationстарыхданных,8kresources/frontend,productionregression,доки/деплой всё ещёне завершены.
- CBM targets в EstimateFinanceQuery актуальныйsnippet15-67 показывает contains(id) на каждый ресурс и повторный pendingResources+hash; после текущего блока заменить на индекс id и однократныйhashнаitem с отдельнойпроверкой. Query пока НЕ менялся.

## Проверка пакетной записи завершена
- finance-large-save-final-tests.log: PASS6/70, 06:34.353 с подготовкой схемы/фикстуры и несколькими большими командами, peak534MB. exec66779 завершён exit0. Три временных профиля восстановлены на45433. Своих работающих процессов нет (обнаруженный после завершения другой php23196 не принадлежит этому launcher; не трогать).
- Доказано:8001позиция/16002условия обеихсторон реально записаны; первыйsave<300SQLqueries, всеprojectionIDs проставлены; replayбезновыхhistoryrows; превышение объёма в последнейстроке не меняетrevision/history; updateсохраняет16002projectionIDs и создаётследующуюверсию условий. В отдельныхтестах ошибка непосредственно послеSQLзаписиhistory откатываетallocations/projections/history/revision/receipt; UUIDдругойсметывверхнемрегистре отклоняется безизмененияисточника.
- Новая migration statement-trigger проверена: прямые bulkINSERT/UPDATE/DELETE проекций увеличиваютrevisionна1заstatement; central save увеличиваетrevisionна1завсюоперацию. Регрессии splitvolume/replay и accepted-remainderpricePASS нафинальномкоде. PHPStan3filesPASS и gitdiffcheckPASS.
- Время6:34 не является временем одногоHTTPsave: включаетbootstrap,8000bulkfixtureстроксисходнымitemrowtrigger,несколькокрупныхsave/replay/ошибку и5малыхсценариев. ОтдельнуюпроизводительностьHTTP/браузера не измеряли. Большаяфикстураплоская,is_manual=false,безреальногоExcelimport иресурсныхиерархий; полная8kприёмка остаётсянеподтверждённой.
- Блокготовклокальномуcheckpoint. Production/PR/deployне выполнялись. Следующее:ресурсы Query.targets (индексitems иоднократныйsourcehash); затемнезавершённыеплан/факт/оплаты/миграцияданных/приёмка/документация/деплой.

## Ресурсы: устранены повторные обходы
- EstimateFinanceQuery.targets: индекс IDs всехпозиций вместоcontainsдлякаждогоресурса; pendingResources/hash вычисляютсяленивоодинразнаработу ииспользуютсядляеёресурсов. Ресурсы безsourcehashневызываютвычисление; разныеработыимеютраздельныеданные.
- finance-resource-query-tests.log PASS3/16,01:31.948сbootstrap,172MB: независимыехеши двухработ,устаревшие/актуальные/nullhashресурсы,явнаяподготовкалегасиресурсов иизменениеисточника,запретзакупкимиррорногоресурсадоразрешенияпредставления. PHPStanQueryPASS, gitdiffcheckPASS. exec6492терминальныйexit0,профиливосстановлены.
- Следующийкрупныйэтапфинансовогофакта: CBMнайдены PaymentTransaction(immutable,statusCOMPLETED,amount,currency,reverses_transaction_id,payment_document_id,org/project,refunds) и PaymentDocumentEstimateSplit(document_id,item_id,amount,quantity...). PaymentDocumentService.processEstimateSplits создаётплановыеsplitдокумента,аНЕдетализациюконкретнойтранзакции. Ихнельзяскладыватьскаждойоплатойпополнойсумме. ExistingPaymentDocumentEstimateLifecycleTest.test_refund_reverses_estimate_item_payment_projection показывает1000оплата/400возврат=>paid_amount600 иitem.actual_quantity6: actual_quantityэтогоконтураНЕсчитатьутверждённымвыполнением.
- Графpaymentsкандидатыпрочитанычерезsnippet+coverage(metadata_changed): app/BusinessModules/Core/Payments/Models/{PaymentTransaction,PaymentDocumentEstimateSplit}.php; Services/PaymentDocumentService.processEstimateSplits(1397-1445), tests/Feature/BusinessModules/Core/Payments/PaymentDocumentEstimateLifecycleTest(174-205). Требуетсяпродолжитьtraceпотребителей/прав/поляPaymentDocument иfactreport; покаэтонавигационноеисследование,финансовыйфактнеподключён. Полнаяцельне завершена,деплоянет.

## Выполнение: первый серверный слой источников
- Добавлен EstimateFinanceExecution.report и существующий finance.report/show расширен view=plan|execution (поумолчаниюplan; cash ещёнеподдерживается). Executionвозвращает available/rows/documents. Planполяпокаостаютсявобщемответе: frontendпереключатель,агрегатывыполнения/остатки,проект/Excel ЕЩЁ НЕ реализованы. Не выдавать за готовыйэтапфакта.
- Источники: ContractPerformanceAct approved/signed, annulled_at null, contract org/project и act project проверены. Подгружаютсястрокиактасitem; старыеcompletedWorks/pivot используютсятолькокогдасовременныхlinesнет. Вrowsтекущейсметы попадаюттолькострокис еёitemID; фактыдругихсметнепереносятся. Документотдаётсвоюсумму,суммутекущейсметы,остатокбезлюбойпозициисметы; отрицательныйостатокилиразныевалюты помечаютсяneeds_review. Строкиразныхвалютсохраняютсяотдельно,безпересчёта.
- Net строки извлекаетсяизcontract_conditions base_unit_price толькоеслисовпадаютcontract/item/estimate/currency, значениевалиднаядесятичнаястрока; иначеnull. СуммаактабезНДСберётсяизегоявногоненулевого amount_without_vat (либо0приобщемнуле), илиизполностьюизвестныхснимковстрокприсовпадениисуммы; историческийdefault0приненулевомgrossневыдаётсязаизвестныйnet. Известныйnetшапкиавтоматическинастрокинеразносится.
- EstimateFinanceAccess.canViewExecution теперьпринимаетprojectId, проверяет OR contracts.performance_acts.view / act_reports.view сproject+orgcontext. Это соответствует routes/api/v1/admin/project-based.php performance-actsreadmiddleware и ActReportingModule permission. report/history обновлены. Безправ rows/documents=null available=false и никакихзапросовкактам. Старыйtestaccepted-remainder callbackобновлёнзапрещатьобаактоправа (самэтотстарыйтествэтомпрогоненеисполнялся).
- finance-execution-report-tests.log PASS2/21 (ранняяверсия); финальный4testнаapproved/draft/annulled/unallocated/rights/projectcontext/legacyfallback/mixedcurrency PASS4/31 finance-execution-report-final-tests.log,01:53.215,174MB. Послеуточненияnetшапкиузкий finance-execution-header-net-tests.log PASS1/10,01:25.625,164MB. PHPStan4filesPASSfinal,послепоследнейправкиExecutionодинфайлPASSheader-net. gitdiffcheckPASS. Всеexecпроцессытерминальные,профиливосстановлены45433.
- Остаётсянаэтомслое: агрегатырядов/разделов/валют иостатки,ссылкиUIнаисточник,доступныеконтракты/снимкисогласованностьприконкурентныхактированиях,старыйis_approvedбезканоническогостатуса(неучитываемавтоматически),ручное распределениеостатковидругиетестыизоляции/нетдетализации/нетповторногоподсчёта. Неизвестноедействительно остаётсяnull. Общаяцельне завершена; cash/owncosts/migrationolddata/real275436/docs/deployвпереди.
- CBM backendgen13:44:58старый: новыйExecution, Remainder иActing/PerformanceActContractBasisService неtracked; snippetsнеполны. Использованыточечныечтенияпослепустогопоиска иcoverage. Нужнаоднаиндексациязатронутогоbackendworktreeпослепакета; неиндексироватьвсеосновныепроекты.

## Итоги выполнения по позициям, разделам и валютам
- Добавлен EstimateFinanceExecutionSummary: агрегаты источников с устранением повторов, раздельные валюты, итоги родительских разделов, остатки и превышение объёма по каждой связке договора и позиции. Неизвестные цены, направления и объёмы сохраняются как отсутствие данных; известная часть суммы доступна отдельно.
- Отчёт execution использует агрегатор только при наличии права просмотра актов. Чтение источников сохраняет неизвестный объём как null.
- Проверки: finance-execution-summary-tests.log PASS 2 теста / 23 утверждения; finance-execution-summary-integration-tests.log PASS 5 / 75; finance-execution-summary-final-phpstan.log PASS. git diff --check PASS. Тестовые профили восстановлены (не входят в diff).
- Следующий блок: отображение выполнения в админке. Деньги, собственные затраты, ручное распределение факта, перенос, полная сверка потребителей и деплой ещё не завершены.

## Контракт выполнения в админке
- В типы добавлен discriminated union available для выполнения, документы, строки, агрегаты валют/разделов/позиций и остатки. estimateFinanceService.report принимает пятым аргументом view (plan по умолчанию, execution явно), сохраняя существующий AbortSignal.
- MSW-проверка src/services/estimateFinanceService.test.ts PASS 1: параметры view/basis и отсутствие доступа без ложных нулей. ESLint трёх файлов PASS; npx tsc --noEmit PASS (exec27253 терминальный exit0). Коммит админки feat[admin]: описать контракт отчёта выполнения.
- UI ещё НЕ подключён. CBM FinanceWorkspace найден, snippet содержит текущую основную функцию, но конец обрезан из-за смещения строк. Добавлять выбор План/Выполнение внутри режима Доходы и расходы, типизированный компонент выполнения с детализацией и проверенными переходами к актам. Excel пока поддерживает только план: нельзя выдавать плановый экспорт за выполнение. Старые разделы cash/owncosts/migration и остальные условия полной цели остаются обязательными.

## Экран подтверждённого выполнения
- Подключено План/Выполнение в FinanceWorkspace к report(view). Новый EstimateFinanceExecutionPanel показывает итоги по валютам, явные неизвестные значения, остатки по договорам и позициям, поиск, фильтр разделов с подразделами и их агрегаты, акты/нераспределённые суммы и диалог исторических строк с condition_version. Объёмы по 50 строк на страницу (проверено 8001), без запросов на строку.
- Без права выполнения выводится unavailable, не нули. Переход /acts/:id только при act_reports.view (проверен App.tsx guard и permissions.ts); имеющим только просмотр актов договора доступны строки разрешённого отчёта без запросов к общей карточке.
- Целевые Vitest PASS12 (3 новых component +9 workspace, включая MSW переключение view/возврат к plan); ESLint4files PASS; npx tsc --noEmit финальный exec55850 PASS exit0, предыдущий12818 тоже terminal PASS. gitdiffcheck PASS. Проверка в реальном браузере ещё не проводилась.
- Excel execution пока отключён в UI, потому что экспорт поддерживает только plan. Это НЕ готовый выпуск: следующий шаг общий execution export. CBM нашёл EstimateFinanceExport.workbook/download (class snippet актуален, метод контроллера export найден); download пока вызывает report безview, workbook толькоplan. Надо расширить тот же экспорт: summary/positions/sections/documents/sources/remaining без независимого пересчёта, права, unknown=null, недоступные данные явные. Не создавать второй финансовый контур. Существующий тест workbook находится EstimateFinanceTest.php:1218. Остальные обязательные cash/owncosts/manualfact/migration/clientregression/docs/deploy не завершены.
- Ограничения текущего UI для дальнейшей доводки: таблица документов и диалог источников пока без пагинации; выбор позиции показывает источники всех её договоров, требует ясных подписей стороны/договора; детальное действие ручного распределения пока не реализовано. Финальный экспорт и проектные потребители должны быть согласованы до релиза.

## Экспорт выполнения сметы
- EstimateFinanceExport.download/workbook принимают view=plan|execution. Тот же маршрут контроллера передаёт view; выполнение получает finance.report(..., view), без независимого пересчёта. Шесть листов: итоги, позиции, разделы, объёмы, акты, строки актов. Сохраняются null, валюты, остатки, ID источников/allocation_key/condition_version, формулоподобный текст явно строковый. Нет доступа — явная запись без строк/документов. НДС выбранной базы подписан, gross/net источников отдельными колонками.
- UI Excel теперь передаёт текущие view/basis, имя файла выполнения отдельное; без available кнопка отключена. Сервисный MSW-тест проверяет параметры, Blob URL, имя и освобождение URL. Vitest service+workspace PASS11; ESLint3files PASS; tsc exec34116 PASS. Админка коммит feat[admin]: выгружать выбранное представление выполнения.
- Backend workbook tests PASS2/20 finance-execution-export-tests.log, 01:38,164MB (exec50002 terminal). Интеграция реального отчёта актов + старый plan/project/Excel regression PASS2/25 finance-execution-export-integration-tests.log,01:40,174MB (exec13013 terminal). Профили после launcher восстановлены45433. PHPStan PASS (exec37210 terminal), запуск APP_ENV=testing php -d memory_limit=2G vendor/bin/phpstan ... --memory-limit=2G: без APP_ENV ошибочно загружалась production-проверка конфигурации хранилища; без memory_limit упиралось в128MB. Никакие ключи хранилища не подставлялись, никакие production-команды не выполнялись. php -l4changed PHP files PASS; diffcheckPASS.
- Ограничение до следующего блока: execution export для project без estimateId пока возвращает validation error (не подставляет plan). projectReport пока толькоplan. Надо расширить проектный отчёт единым выполнением с корректной агрегацией по валютам/источникам и недублированием много-сметных актов; затем разрешить project execution export. Отдельно требуется одинаковость контрактного экрана и сводок.
- Полная цель по-прежнему не завершена: cash/returns/advances/manualundistributed/owncosts, повторяемый перенос вместо broken backfillLegacyCoverage, фактическая регрессия275/436, полная8kресурсы/большие UI документы, документацияworkflow, интеграцияmain/штатныйдеплой/клиентскаяпроверка. Ничего не деплоилось, не отмечать цель выполненной.

## Проектное выполнение и общий акт нескольких смет
- EstimateFinanceProjectExecution объединяет отчёты по уникальному ключу строки act_id/source_type/source_id, договорные условия по allocation key, цели/разделы по ID. Акт один на проект с estimate_amounts; повтор отчёта не увеличивает объём/сумму. Общие итоги через тот же EstimateFinanceExecutionSummary, не сложение произвольных UI сумм.
- projectReport(..., view) и проектный HTTP-метод принимают execution. Для агрегации сначала доступны строки/разделы, затем удаляются из кратких ответов; plan сохраняет прежнее раннее освобождение строк. Нет доступа — available=false, rows/documents=null, включая пустой проект.
- Проектный Excel execution теперь разрешён: использует projectReport(execution), добавляет общий итог; лист актов заполняется из объединённых документов, общий акт одной строкой со списком смет. Отдельная смета сохраняет прежние листы.
- Проверки: финальный комбинированный unit/workbook PASS3/30 finance-project-execution-final-tests.log,01:37,168MB; интеграция реального projectReport(execution) + старого plan/project/Excel PASS2/29 finance-project-execution-integration-tests.log,02:12,174MB. PHPStan4files PASS finance-project-execution-final-phpstan.log. php-l4files PASS, diffcheckPASS. Все backend launcher terminal, временные профили восстановлены45433.
- В админке подготовлены project(view) и типы execution/estimate_amounts; MSW3tests и ESLint3files PASS. tsc обнаружил implicit this в тесте скачивания из прошлого блока; исправлен this:HTMLAnchorElement, повтор сейчас exec6692 (проверить перед frontend commit). UI ProjectEstimateFinance пока толькоplan. CBM snippet этой функции прочитан целиком и актуален (29–233): компонент Accordion с загрузкой service.project, tax switch, plan-таблицей/expandcontracts и download. Следующий UI шаг — выбор view, выполненные итоги/акты из data.execution, переход к деталям сметы, экспорт с view. Типы/сервис уже поддерживают.
- Незавершённые риски: согласованность чтения одного акта при конкурентном аннулировании между чтениями разных смет, округление итогов на границах нескольких смет, дополнительные проверки project rights/no-act-query. Остальные полные требования cash/owncosts/manualfact/migration/клиент275436/docs/deploy остаются, ничего не выкатили.
- Cash groundwork: CBM PaymentTransactionService.refundPayment snippet136–269 актуален, coverage metadata_changed. Возврат — отдельная COMPLETED транзакция с отрицательной amount, reverses_transaction_id и теми же payment_document_id/org/project/currency; не переписывает original transaction. Проверяет сумму доступного возврата, уменьшает document.paid_amount и запускает synchronizeFinancialProjections. Плательщик/получатель в новой refund строке не копируются: направление надо определять через документ/исходную транзакцию/стороны договора, не по пустым полям refund. Отрицательную сумму нельзя повторно инвертировать. Этот код только прочитан, не менялся.
- Финальный tsc после исправления тестового this PASS exec6692 exit0. Frontend project API/types зафиксированы; все собственные процессы завершены. Следующий шаг — UI ProjectEstimateFinance с view и источниками выполнения (описание/snippet выше), затем cash. Обязательные остальные требования остаются активными; деплой не выполнялся.

## Проектный экран выполнения
- ProjectEstimateFinance переключает view plan/execution при чтении/экспорте, сохраняет plan-таблицу, execution отображается отдельным ProjectExecutionTable. Итоги смет и проекта берутся из execution.summary; общий акт одной строкой, список смет, общий/распределённый/нераспределённый gross. Документы по50настраницу. Диалог показывает части смет и строки источника; общая карточка акта только act_reports.view, ссылки смет только PERMISSIONS.ESTIMATES.VIEW (проверены реальные App.tsx маршруты /acts/:actId и /projects/:projectId/estimates/:id). Недоступный execution не показывает plan или ложные нули, Excel отключён.
- Финальный Vitest ProjectEstimateFinance PASS2, включая MSW переключение API, две сметы/один общий акт/проектный итог и запрет доступа. ESLint3files PASS. tsc exec49095 terminal PASS. diffcheckPASS. Новый UI в реальном браузере пока не осмотрен. Все свои процессы terminal. Коммит admin feat[admin]: показать выполнение по сметам проекта.
- Дальнейшая UI доводка: источники в диалогах пока без пагинации; ProjectExecutionTable делает поиск имени сметы в data.estimates на источник (заменить индексом при оптимизации крупных документов); конкретный вид execution не сохраняется в ссылке на смету. Обязательные cash/owncosts/manualfact/migration/права/клиентскаярегрессия/доки/деплой всё ещё не завершены.
- Cash groundwork дополнен: ContractPaymentDocumentService.createPaidContractPayment создаёт документ через PaymentDocumentService.createFromContract, затем registerPayment; metadata.contract_payment_type различает advance/другие. getPaymentsForContract выбирает PaymentDocument invoiceable_type=Contract::class и invoiceable_id=contractId, но для нового finance reader обязательны явные org/project scopes. PaymentDocument.transactions — hasMany по payment_document_id. Эти snippets актуальны, только прочитаны. Дальше проверить createFromContract и источники документов по актам (invoiceable polymorphic), канонические права чтения платежей и транзакций, сохранить отрицательный знак возвратов.

## Денежные источники связанных договоров
- Новый EstimateFinanceCashSources выбирает документы по invoiceable Contract/ContractPerformanceAct, строго в org/project; дополнительно проверяет реальные contract IDs в области estimate. Транзакции только completed в той же org и project (legacy nullproject допустим при проверенном документе), пакетная загрузка. Pending/чужие транзакции не учитываются. Возврат сохраняет отрицательную amount/reverses_transaction_id; валюты отдельно. Документ paid_amount не подменяет подтверждённые транзакции. Legacy paid_amount>0 без истории отмечается payment_history_missing.
- Направление из договора; противоречие invoice.direction incoming/outgoing со стороной договора даёт unknown+direction_requires_review. Не гадаем по автору. Cash report scope=linked_contracts, НЕТ автоматического распределения суммы на смету/позиции. Для чтения требуются одновременно payments.invoice.view и payments.transaction.view с org/project context. Маршруты Payments/routes.php подтверждают оба базовых права; запрещённый report не делает payment queries.
- EstimateFinanceService.report поддерживает view=cash (проект и export пока cash НЕ поддерживают). Добавлены cash payload и канонический type в adminservice. Источники из document invoiceable act прочитывают только act.id/contract_id для контекста платежа, не суммы/содержимое акта.
- Backend tests cash PASS3/18 finance-cash-sources-tests.log,01:39,172MB: частичные400+200-100=500 при recorded_paid600, отдельные USD10, исключены pending/другаяorg/другойproject; актовая связь/unknown direction/legacymissing; denied payments no queries. PHPStan3files PASS finance-cash-sources-phpstan.log; php-l3files PASS; diffcheckPASS. exec20509 дождался общегоmutex и завершёнexit0, профили45433 восстановлены. exec93695 terminalPASS.
- Админка: report(view=cash) typed sources/documents, MSW service PASS4 и ESLint3filesPASS, tsc47058PASS до UI. EstimateFinanceWorkspace добавляет Деньги; новый CashPanel показывает договорные источники/документы, аванс/возврат, предупреждение отсутствующей истории,50строк/страница. НДС-база пока не меняет денежный факт; Excel cash отключён до реализации. Важно: это только список договорных источников, НЕ законченный денежный анализ/распределение. Workspace Vitest PASS10 inclcash, ESLint3UI filesPASS; финальный tsc54239 ещёидёт (проверить передfrontendcommit).
- Дальше обязательно: денежные агрегаты с дедупликацией транзакций и раздельными валютами, платёжные привязки по позициям с cap оплаченной суммы/точным округлением/возвратами/повторами/историей; ручное распределениенеразнесённогофакта через общийpreview/save, project/export; собственные затраты и различие expense vs money. Existing PaymentDocumentEstimateSplit — план документа, его полная сумма не фактическая оплата; synchronizeEstimateItemsPaymentProgress использует paid_amount/document.amount ratio, обновляет procurement actual_quantity (НЕ выполнениеработ). Нельзя использовать эту проекцию как принятые объёмы.
- Cash риски для доводки: explicit flags для расхождения recorded_paid_amount/ledger, валюты документа/транзакции и malformed refund; доступность документов напрямую по estimate без contract и другие invoiceable типы; детальные ссылки на платёжный документ (App.tsx только /payments, внутренний механизм открытьdocumentещёнепроверен), распределение legacy splits без переписывания истории. Полная миграция/275436/8k/доки/деплой и прочие критерии цели остаются обязательными, ничего не выкатили.
- Финальный frontend tsc54239 завершён PASS exit0; UI/API источников денег зафиксирован. Все свои процессы завершены, ожидания тестов нет. Следующий шаг — денежные агрегаты и проверяемое распределение фактических сумм (включая возвраты) в общем finance preview/save; текущий cash — договорные источники, не считать закрытием всего денежного этапа. Полная цель активна.

## Денежные итоги и начало сводки проекта
- Backend cc29e4a35: EstimateFinanceCashSummary считает поступления/выплаты/денежную разницу и возвраты отдельно по валютам и договорам, дедупликация transaction_id. Возврат заказчику входит в выплаты, возврат подрядчика — в поступления. Неизвестная сумма/сторона/валюта даёт null полного итога и явные known_* частичные суммы. Scope остаётся linked_contracts, автоматического разнесения на позиции нет.
- Проверки: summary PASS2/17 finance-cash-summary-tests.log; PHPStan PASS finance-cash-summary-phpstan.log; php-l3files PASS. Интеграция cash report PASS3/26 finance-cash-summary-integration-tests.log,01:39,174MB, exec20277 terminal0, профили восстановлены. Реальные завершённые транзакции400+200-100 дают receipts600/payments100/difference500, USD отдельно, права без SQL платежей.
- Admin fc073077: таблица итогов по подтверждённым транзакциям, null как прочерк и предупреждение, валюты отдельно; исправлена подпись направления возврата. Vitest workspace PASS10, ESLint3files PASS, tsc66978 terminal0 PASS. Первичная вставка JSX попала в guard и дала синтаксическую ошибку; исправлена до успешных проверок/коммита. Никаких push/deploy.
- Сейчас добавляется project cash через EstimateFinanceProjectCash.combine: дедупликация договорных транзакций/документов между сметами; linked_estimate_ids только указание связей, не распределённые суммы. EstimateFinanceService.projectReport разрешает cash и возвращает сводку. Эти изменения ещё НЕ закоммичены/НЕ подтверждены: PHPStan exec72938 и PG tests exec88254 (finance-project-cash-tests.log, фильтр test_project_cash|test_cash_report_does_not) запущены. Тест создаёт две сметы с общим договором, проверяет один денежный итог300 вместо600 и отсутствие платежных запросов без прав. После завершения проверить профили45433. Frontend проекта/Excel cash ещё не подключены.
- Полная цель по-прежнему не достигнута: ручное распределение факта, собственные затраты, перенос данных, реальные275/436, большие данные, все потребители/права/документация/штатный деплой остаются обязательны.
- Project cash backend завершён в c56775cd2. PG PASS2/14 finance-project-cash-tests.log,01:27,172MB, exec88254 terminal0; PHPStan PASS finance-project-cash-phpstan.log exec72938 terminal0, php-l2files PASS, diffcheckPASS. shared contract two estimates paid400-refund100 yields300 once; linked_estimate_ids preserves both estimate references; no automatic money allocation. Denied project makes no payment queries. Profiles restored45433.
- Project cash admin подключён к общему CashPanel (Pick cash/contracts), API project accepts cash, ProjectFinanceReport cash typed. Project UI uses server summary, never adds duplicate per-estimate cash totals. Excel cash still disabled pending implementation. Vitest workspace+project PASS13 finance-project-cash-ui-tests.log, ESLint5files PASS, tsc26333 terminal0 PASS; diffcheckPASS. All own launched checks terminal, no live handles remain.
- Следующий доступный шаг: общий cash Excel (смета/проект) из уже рассчитанного report, разрешить view=cash в EstimateFinanceExport.download/workbook, отдельные листы итогов/документов/транзакций, scope linked_contracts явный; project summary единожды, null/валюты/права/источники сохранять. CBM search/get_code_snippet download выполнены: download сейчас разрешает plan|execution, передаёт workbook5params(project.execution); актуальный source длиннее indexedlines20-38, coverage metadata_changed. Для workbook требуется boundedread текущего тела; не повторять общий поиск. На фронте export union/filename и guards cash в Workspace/Project нужно обновить послеbackend. Переключатели НДС пока видимы в cash, на реальные деньги не влияют; убрать в денежном режиме при доводке UI. Ручной факт/собзатраты/миграция и остальная цель не закрыты.

## Общий денежный Excel
- Backend 1f8657c41: EstimateFinanceExport.download/workbook принимает cash, project.cash передаётся шестым аргументом. cashWorkbook строит 3 листа из report: Деньги, Платёжные документы, Транзакции. Проект использует ровно один агрегированный пакет; суммы смет не складываются повторно. Подписана область связанных договоров целиком, без распределения по позициям. Null остаётся пустым; известная часть отдельно; документы с отсутствующей историей помечены; возврат со знаком и reverses_transaction_id; денежная разница без трактовки как маржа. НДС-база не меняет денежные суммы. Текстовые ячейки явно string, идентификаторы не переводятся в числа Excel.
- Backend PG tests PASS3/35 finance-cash-export-tests.log,01:28,176MB, exec57370 terminal0; project two-estimate sharedcontract300 one summary/doc and2transactions; denied empty sheets; unknown direction null summary. PHPStan PASS finance-cash-export-phpstan.log exec65541 terminal0, php-lPASS/diffcheckPASS. Export controller найден черезCBM, принимает Request и передаёт view в download; его snippet актуален по содержимому, coverage metadata_changed связан с предшествующими правками.
- Admin export union cash + filename Движение денег.xlsx; доступный cash Excel включён в смете и проекте, запрещённый остаётся disabled. Переключатели НДС скрыты в cash. MSW service tests расширены execution/estimatecash/projectcash, параметры/filename/BlobURL revoke. Vitest3files PASS19 finance-cash-export-ui-tests.log; ESLint6files PASS; tsc40449 terminal0 PASS; diffcheckPASS. Все свои процессы терминальны.
- Следующая крупная часть НЕ реализована: ручное распределение неразнесённого выполнения/денег на позиции через общий financial preview/save, ограничения остатка/дублей/возвратов/версий; собственные затраты с документом или подтверждённой ручной записью. Current cash scope только связанные договоры. Нет утверждения о готовности всего факта или полного выпуска. Далее также повторяемый перенос, actual275436, полная приёмка/права/8k/доки/CI/деплой остаются обязательны. Никаких push/deploy.

## Основа хранения распределённого денежного факта
- Добавлена migration2026_09_13_010000_create_estimate_finance_cash_allocations: estimate_finance_cash_allocations со stable UUID key, org/project/estimate, FK allocation_id на существующие EstimateFinanceAllocation, FK payment_transaction_id, currency, signed amount20,2, version/source_hash/source_snapshot/actor/timestamps. Unique(transaction,allocation) не допускает повторную связь. Restrict FKs сохраняют финансовую привязку и исходную транзакцию. Отдельная estimate_finance_cash_versions хранит before/after, version,mutation_id,finance_revision,actor; unique(cashallocation,version), restrict удаление связи с историей. Positiveversions/currencyformat CHECK. Таблицы пока НЕ подключены к пользовательскому сохранению/отчётам; это фундамент, не выполненный manual cash workflow.
- Test test_cash_allocation_storage_preserves_signed_amount_and_prevents_duplicate_transaction_target PASS1/7 finance-cash-ledger-schema-tests.log,01:19,164MB, exec47309 terminal0; дубликат пары/удаление транзакции/финансовойпривязки/истории блокируются DB constraints, -100.00 сохраняется. PHPStan migration PASS finance-cash-ledger-schema-phpstan.log exec78886 terminal0; php-lPASS/diffcheckPASS. Профили возвращены45433. Нет активных процессов.
- Следующая реализация: отдельная операция cash_distribution В СУЩЕСТВУЮЩИХ finance preview/save, общий receipt estimate_finance_mutations и finance_revision, transactional source lock/document lock + sorted allocation/estimate locks, доступ finance.edit+canViewCash+contracts.edit. Подтвердить native transaction completed/org/project/currency/contract через invoiceable document/act; передавать expected sourcehash и rowversion; cap суммы по транзакции между ВСЕМИ сметами, signed refund с привязкой исходной transaction, сохранять/редактировать версии без переписывания source. Должен быть read-back распределено/остаток/источники по позициям, общий calc/project/export/UI; после cash факта guard удаления финансовойпривязки обязан давать понятнуюошибку, а не FK SQL. Обнуление распределения как явная новая версия, историю не удалять. Не считать миграцию готовностью процесса и не выкатывать без service/UI/обязательныхпроверок.
- Исследовано: PaymentDocumentEstimateSplit хранит quantity/unit_price_plan/actual/amount/percentage, это план документа, не ledger оплаченного факта. SaveEstimateFinanceRequest.inputRules сейчас только conditions (revision/mutation_id/target_keys/lines); PreviewEstimateFinanceRequest имеет только source_amount специальную ветку. EstimateFinanceService.preview specialsource thenstandard; save starts access/validate/hash/TX estimate lock/receipt replay/revision/items/preparation/normalize/writes/projection/history/receipt. В конце save finance_revision+1/cacheinvalidate. Для нового operation ветвление validator+service надо согласовать, не пропускать неизвестные поля в существующую normalize и не стирать плановые lines.
- CBM backend project most-unified-finance-backend gen2026-09-12T20:04:13Z: save snippet offsets stale (на самом деле начинается на хвосте preview), выполнено boundedread save355-455/preview205-270. Coverage metadata_changed Access/Request и финансового Service, graphmodelsnippet PaymentDocumentEstimateSplit актуален по содержимому. Не переиндексированы проекты. Полная цель всё ещё активна; cash distribution/execution manual/owncosts/migration/client275436/8k/acceptance/docs/deploy остаются обязательны. Никаких push/deploy.

## Операция сохранения распределённой оплаты
- Добавлен EstimateFinanceCashDistribution.handle и ветка operation=cash_distribution существующих FinanceService.preview/save. SaveRequest.cashRules/PreviewRequest ветвятся по operation. Требуются revision/mutation_id/transaction_id, lines allocation_key/condition_version/version/signedamount; save требует source_hash, preview возвращает его и source_amount/distributed_amount/remaining_amount. Подтверждение источника через CashSources.report (родной document/contract/act org/project/scope/completed), права finance.edit+cashview+contracts.edit. Получение targets исключает excluded и несовпадающие договор/валюту.
- TX estimate lock → document lock → sorted native document transaction locks, текущий ledger всех этих транзакций загружается пакетно. Учитывается allocated всей транзакции между ВСЕМИ сметами. Одинаковый mutation_id+hash+actor даёт replay без новой строки/истории. Sourcehash/condition_version/cashrowversion/revision conflicts409. Amount sign должен совпадать с source. Refund only up to positive original allocation minus other allocated refunds; уменьшение оригинальной разнесённой оплаты ниже возвратов блокируется. Изменение mapping не меняет native payment/act. Пакетные upsert cashlines/history по500; shared finance_revision и estimate_finance_mutations. UUID/id сохраняются приредактировании.
- FinanceService.save/normalize защищают cashlinked financeallocation от удаления/смены договора/валюты/source/target; понятное cash_linked вместо rawFKошибки. Миграционный FK restrict остаётся последней защитой. При preview стандартныхусловий guarddeletedидов находитсявsave (какраньше частьудалений), UI должен корректнопоказатьошибку.
- Tests first PASS2/17 finance-cash-distribution-tests.log; после batchwrite+guards+sourcehash/permissions финальный PASS3/21 finance-cash-distribution-final-tests.log,01:28,172MB, exec24378 terminal0. Реальный payment400 allocate300/refund-100, исходную allocation уменьшить50 нельзя,401 нельзя,200 можно сstableUUID/version2; replay count1; badsecondline rollsback; sharedsecondestimate200 overremaining100 rejected; forbiddencontracts.edit rejected; sourcehash changed409; удалить cashlinkedconditions нельзя. PHPStan4files PASS finance-cash-distribution-final-phpstan.log exec13498 terminal0; php-l4files/diffcheckPASS. Профили45433, все свои процессы терминальны. No push/deploy.
- ЕЩЁ НЕ ГОТОВО: read-back CashSources/report с per-position ledger/versions/остатками и source_hash для формы; UI редактирования/preview/save; project/Excel распределённых денег, manualexecution/owncosts, общаямиграция/реальныерегрессии/8k/concurrency/права/доки/deploy. cash_rules preview lines currently required min0, Laravel required rejects emptyarray — для первичного probe при разработкеформы потребуется present вместоrequired либо непустой выбранный состав; не выдавать отсутствиепробызааппрув. Сервис загружает CashSources целойсметы для проверкиоднойtransaction; noqueryperrow, но окончательный8k/cashloadcheck ещёнет. Refund malformednativechain/currency требуетсяполнаяфинальнаяпроверка; concurrent payment workflow lockorder проверить integration. Финансовыелинии сcashисторией могутправитьоставшиесяусловия, но acceptedmoney не равноacceptedwork; эти показатели не смешивать.

## Read-back распределённых оплат и остатков
- Backend8a248918c: CashLedger.report делает один join ledger→allocation→estimate для проверенных native transaction IDs, сверяет org/project/estimate/contract/currency и скрывает несогласованные scope строки. source hash рассчитывается по исходному CashSource, distribution не встраивается в сам source (не меняет writer hash/selfsnapshot). Global allocated/remaining каждойтранзакции учитывает все сметы; estimate_allocated отдельно. Возвращает cash.distribution(scope allocated_positions): sources с hash/remaining/review, allocations со stableid/key/allocation_key/condition_version/version/target_key/amount/source_changed, totals и positions. Договорные cash.summary суммы остаются отдельно, не подменяются распределением.
- Changed native context/hash/sign либо overallocated ledger помечаются requires_review; remaining/allocated null, соответствующий позиционный/проектный подтверждённый итог null. Никаких native transactions изменений; test первоначально пытался update immutable PaymentTransaction и был исправлен: меняет direction документа, имитируя изменившийся контекст. Коррупция ledger amount500 приnative400 отдельно тестируется толькотестовым DBwrite, остаток/позиционныйитог/проектныйитог null, nativecashsummary400 остаётся.
- ProjectCash.combine дедуплицирует distributionsource bytransaction, allocation byid, positions byestimate+target; project totals изuniqueallocatedfacts, не из суммы повторныхдоговорных reports. В проектномsource удаляется estimate_allocated_amount (нет единственнойсметы). CashRules preview исправлен present lines, допустим пустой probe; save min1. Writer sourcehash unchanged and source query now includes readledger extra singlejoin.
- Backend финальный PG PASS5/56 finance-cash-ledger-final-tests.log,01:34,174MB, exec29508 terminal0. Успешны writer/replay/caps/readsourceglobal/ownamount/projectdedup/stalecontext/scope/deny. Initial run5/45 had immutablemodeltesterror, scoped retest1/6 passed, final5/56 fullypassed afteroverallocatedguard. PHPStan4filesinitialPASS, final2changedfiles PASS finance-cash-ledger-final-phpstan.log exec2757 terminal0; php-l3files/diffcheckPASS. Profiles45433. Backend own processes terminal.
- Admin добавлены FinanceCashDistribution/Command/Preview types, previewCash/saveCash в ТЕХ ЖЕ endpointsPOSTpreview/PUTfinance. Service MSW PASS7 finance-cash-distribution-api-tests.log, tsc29736 PASS; ESLint3PASS. CashPanel показывает изdistribution распределено по позициям всехсмет/нераспределено и warningrequiresreview; источник и валютасоседнихколонок сохраняются. Workspace/project Vitest PASS13 finance-cash-ledger-panel-tests.log; послеassertionsреальных250/150/warning workspace PASS10 finance-cash-ledger-panel-final-tests.log. ESLint5filesPASS, tsc93958PASS дофинальнойправкитестовыхassertions. Финальный tsc послеtestedit запущен сейчас в finance-cash-ledger-panel-final-types.log — handle получить из текущего toolresult. Frontend ещё НЕ закоммичен доэтого tsc. Других livehandlesнет.
- Следующийшаг UI: CashDistributionDialog для выбранной транзакции из CashPanel, редактируемыестроки текущих FinanceRow.allocations поcontract/currency/notlegacy/notexcluded, версииизdistribution; previewCash подтверждаетостаток/sourcehash, saveCash с темжемutationid повторяетсябез дубля приnetworkfailure, обновитьreport. Workspace имеет report.can_edit и hasPermission, кнопки толькоfinanceedit+contracts.edit+cashavailable. CashPanel sharedwithproject props Pick<FinanceReport,cash|contracts>, лучшеonEdit callbackвWorkspace; проектныйредакторпокаоткрыватьнужносконкретнойсметой. В CashPanel нетимёнпозиций: readerallocation target_key технический, его невыводить пользователю; имена братьFinanceRows в диалоге. 50строкнастраницу+поиск для8000строк, neveryqueryperrow. Далее распределённыйcashExcel/sections, manualexecution/owncosts/миграция/всяприёмка/доки/deploy остаются обязательны. Не выкатили, полнаяцельне завершена.
- Финальный frontend tsc83899 завершён PASS exit0, finance-cash-ledger-panel-final-types.log пуст. Frontend readback/APItypes/остатки закоммичены. Все свои процессы терминальны, можно начинать CashDistributionDialog без повторения этих проверок до новыхправок.

## Форма редактирования распределения оплаты
- Admin новый CashDistributionDialog: источникtransaction, текущие FinanceRow.allocations matching contract/currency, nonlegacy/condition_version>0/notexcluded. Поискпо всейсметe и50строкнастранице. Текущиеamount/version изcash.distribution.allocations; отправляет толькоизменённыеlines allocation_key/condition_version/version/amount (comma→dot). Sourcehash берётсяизdistribution/preview. Явнаякнопка Проверить→previewCash, saveCash доступентолькопослеуспеха, изменениеинпута сбрасываетpreview/command. commandref сохраняет mutation_id приnetworkfailure/retrysave; 409 блокируетформу+Обновитьотчёт,422/403/networkпонятные тексты безrawSQL. Busy блокируетполя/закрытие. Нолькакноваяверсияснимаетсумму, отрицательнаядляrefund. onSaved закрывает/перезагружаетreport.
- CashPanel onEdit callback и actioncolumn; неизвестныйsource/amount/direction disabled. Workspace передаётonEdit только report.can_edit && contracts.edit; диалогвWorkspace, проектнаясводкаостаётсяreadonly (выборконкретнойсметыдляредактированиянауровнеprojectещёнужнодовести). Внешнеsource_changed/invaliddistribution можноисправлятьдляknownsource черезперепроверку, nativeunknown запрещён.
- Tests initial dialog+workspace PASS12 finance-cash-dialog-tests.log. Новыеtests проверяютpreviewsave gating, retry500 sameexactmutation/body, conditionversions, 8001candidates≤50DOMrows/searchlast/excludedfilter. Послеadding409/resetpreview/emptysearchmessage dialog PASS3 finance-cash-dialog-final-tests.log; ESLint4filesPASS finance-cash-dialog-final-eslint.log; formattingPrettiernew2files done. Initial tsc66364 PASS доnewtests, финальный tsc90060 сейчасожидается (finance-cash-dialog-final-types.log). НоваялогикаUI ещёНЕкоммититьдоэтойпроверки. Backend неизменёнэтотблок. No push/deploy.
- Обязательные дальнейшиеработы: распределённыеcashитоги/строки иостатки вExcel иsections; детализациясуммпоконтрактам/позициям иnativeплатёжнымдокументам (сейчасsource IDs/table только), UIисторииversion, project cash перейтинаконкретнуюсмету; manualexecution/owncosts, переносданных/real275436/полные8k/concurrency/прав/доки/deploy. Это НЕзакрытие всейцели. ТекущаяCashDialog пагинацияlabelDisplayedRowsрусская, ariaнавигациипокаlibrarydefault — довестиприUIpolish. Source snapshot nativepaymentsimmutability соблюдается. Продолжатьтотжеплан.
- Финальный tsc90060 PASS exit0; CashDistributionDialog/интеграцияCashPanel/Workspace закоммичены. Все свои проверкитерминальны, активныхожиданийнет.

## Распределённые оплаты в Excel и проверка источников собственных затрат
- Backend 4c394453d подтверждён текущим git log. Выгрузка cash теперь содержит пять листов: исходные денежные суммы, документы, транзакции с распределением/остатком, сводку распределённых денег, оплаты позиций. Версии условий и распределений, стабильные идентификаторы и исходные суммы для сверки сохранены. Проект использует общий пакет без повторного сложения одной транзакции. Неизвестные подтверждённые значения пустые; сохранённое распределение с ошибкой отдельно видно для сверки. Формульные имена позиций записываются строкой.
- Повторно прочитаны конечные результаты, проверки не перезапускались: finance-cash-position-export-tests.log PASS 6 tests / 91 assertions, 01:39.899,178MB; finance-cash-position-export-phpstan.log No errors. Нет действующих процессов этого блока. Деплой не выполнен.
- Исследован существующий источник собственных расходов: AdvanceAccountTransaction(type=expense, reporting_status=approved). approveTransaction устанавливает approved_at/approved_by; deleteTransaction запрещает удалять отчитанные/утверждённые записи; updateTransaction защищает amount/type/user/org после pending. Однако реально используемый контроллер импортирует Api/V1/Admin/AdvanceTransaction/UpdateAdvanceTransactionRequest: он допускает изменение description/document_number/document_date без проверки approved. Одноимённый старый Request в Http/Requests/AdvanceTransaction имеет более строгую ветку approved, но этот контроллер его не использует. Поэтому основание нужно сохранять снимком и сверять свежесть; неизменность всех реквизитов предполагать нельзя. Посторонний workflow не изменялся.
- Категории уже представлены CostCategory с organization_id/is_active. В AdvanceAccountTransaction нет отдельной валюты: нельзя автоматически подставлять RUB или выводить налог из разницы сумм. Кандидат на ссылку — только утверждённый расход той же организации/проекта; выдача аванса и возврат не становятся признанным расходом. Канонический permission-кандидат advance_transactions.view присутствует в RoleDefinitions; фактическую проверку AuthorizationService ещё нужно связать с новым сервисом и проверить тестом, а не полагаться на show контроллера.
- Следующая реализация: собственный расход с подтверждённой ручной записью либо ссылкой на утверждённое основание, дата/сумма/валюта/категория/основание, стабильный ID и снимок. Один источник организации повторно не создавать; распределение по собственным финансовым условиям с общим пределом между сметами. Предпросмотр/сохранение через существующие finance endpoints и receipt/revision. Расход отдельно от движения денег; отсутствие налоговых данных не превращать в ноль. Сначала реализовать хранение и сервис с тестами ручного подтверждения, прав, чужой организации/проекта, дубликатов, изменённого основания и распределения сверх остатка; затем отчёт/Excel/UI. Это НЕ реализованный блок и не основание закрывать этап 3.
- CBM most-unified-finance-backend, фактический worktree, generation2026-09-12T20:04:13Z. Поиск и snippets использованы для методов AdvanceAccountService, модели allocation и обоих Request. Coverage metadata_changed; snippet normalize смещён после новых правок — для оставшейся части 474–525 использовано ограниченное чтение. Импорты контроллера и подключение маршрутов прочитаны отдельно. Отсутствие результатов раннего поиска Expense не означает отсутствие других источников затрат. Полная цель, перенос/реальная регрессия275/436/приёмка/документация/деплой остаются открыты.

## Хранение собственных затрат
- Backend390525fb0: migration2026_09_13_020000_create_estimate_finance_own_costs добавляет источник расхода estimate_finance_own_costs, историю источника estimate_finance_own_cost_versions, распределения estimate_finance_own_cost_allocations и их историю estimate_finance_own_cost_allocation_versions. Источник проектный, не принадлежит единственной смете: source_type manual/advance_expense, уникальный nullable advance_transaction_id, category/date/basis/currency/grossamount/netnullable/VAT, confirmed actor/time, stableUUID/version/status/sourcehash/snapshot. Распределение связывает расход с existing EstimateFinanceAllocation и сметой, хранит source/condition/version; уникальная пара расход/условие. Restrict FKs и отдельная история защищают связи; source может быть voided новой версией, нулевая сумма распределения допускает явное снятие без удаления истории.
- CHECKs: источник/nullableFK согласованы, положительный расход, неотрицательное распределение/net не больше gross, версия>0, ISO-подобная currency3 uppercase, непустое основание; unknownVAT требует NULL rate/net; none требует NULLrate и net=gross; included/exclusive требуют явные net/rate0..100. Нет дефолта валюты и автоматической связи с оплатой. Совпадение ставок с формулой, org/project/caps/права должен проверять сервис — одна схема этого не доказывает.
- test_own_cost_storage_preserves_sources_history_and_unknown_tax PASS1/12, finance-own-cost-schema-tests.log,01:23.061,164MB, exec93800 terminal0. Проверены UNKNOWN NULLnet, частьрасхода60 из120, ошибки валюты/отрицательногозначения/noneбезnet/источникабезFK, запретудаления расхода/условий/распределения систорией, дубльпары, дубльподотчётногоисточника, сохранениеUUID. PHPStanmigration PASS finance-own-cost-schema-phpstan.log,exec18723 terminal0; php-l обоихфайловPASS/diffcheckPASS; триPGпрофиля восстановлены45433. Все процессы терминальны.
- Пока это только хранилище: НЕ подключено к finance preview/save/report/export/UI. Следующее: операция own_cost в существующих FormRequests и EstimateFinanceService dispatch, общий estimate finance_revision/mutation receipt, stablecostkey/version + source snapshot/hash, explicit confirmed manual, advance viewpermission+approved expense+sameorg/project, category scopeactive, source currency confirmation. Источники/распределения блокировать при записи; общий лимит по расходу между сметами; возврат/аннулирование отдельной историей, не переписывать исходные документы. Снимок изменения исходного документа помечает review. Затем reader/execution own costs vs cash отдельно, UI и Excel. Не закрывать цель/этапфакта по этому тесту.
- CashDistribution class отсутствует в текущем CBM generation (содержательный search выполнен, total0), поэтому при продолжении можно ограниченно прочитать её текущий handle как шаблон receipt/locking. FinanceAccess class snippet подтверждает estimate/project/org+finance checks, но обрезан перед новым canViewCash: читать только необходимый хвост при изменении. Не запускать переиндексацию всех проектов. Полный план и ранее перечисленные открытые пункты сохраняются. Push/deploy не выполнялись.

## Регистрация подтверждённого собственного расхода через общий API
- Backend747c8257b: операция own_cost подключена к существующим Save/PreviewEstimateFinanceRequest и EstimateFinanceService.preview/save, новый EstimateFinanceOwnCost.handle. Параметры revision/mutation_id/cost_key/confirmed(accepted), manual|advance_expense, nullable onlyadvance sourceID, category/date/basis/currency/amount/VAT mode+basis+rate; source_hash обязателен при сохранении. Ввод проверяется через FinanceInputValidation и существующий EstimateFinanceTax, нет второго денежного расчёта. Amount при exclusive — исходная сумма без НДС, snapshot.amount — полученный gross; unknown gross равен явно введённой сумме, net NULL.
- Finance edit/project/org проверяются прежде источника; advance требует advance_transactions.view. В транзакции блокируется смета, проверяется shared receipt actor/hash и finance_revision; category active sameorg lock; source approved expense sameorg/project lock, approved_at required, nativeamount=gross и category совпадает еслиуказана. Source snapshot содержит native реквизиты/статус/дату/утвердившего и explicitcurrency+налоговыеусловия; source_hash сравнивается preview/save. Несовпадение/ужеиспользованныйUUID конфликт, повторnativeID понятнаяошибка own_cost_duplicate. Сохраняет source/history/receipt и увеличиваетfinance_revision, повторrequest возвращаетcost_key/replayed. Исходный документ/платежи/распределения не меняются.
- Tests final PASS2/26 finance-own-cost-register-final-tests.log,01:24.779,170MB, exec98570terminal0. Покрыты preview100+20%=120, беззаписи preview, registration/replay1row+1history, changedpayload409, unknownnetNULL, mandatoryconfirmation/currency/category/positiveamount/basis, pending/foreignorg/otherproject/issue rejected, changednative document_number afterpreview409, duplicate native source rejected, amountnative120 unchanged, deniedsourcepermission no query advance_account_transactions, cashledgerempty. Initialrunfailed2/0 because test revision hardcoded0; fixedto freshactualrevision, successfulretest. PHPStan4productionfiles PASS finance-own-cost-register-phpstan.log exec46948terminal0; php-l5filesPASS/diffcheckPASS. Profilesrestored45433; no livehandles.
- Только регистрация! Следующий блок: own_cost_distribution в техжеpreview/save, lock costsource иledger, allocation own/source/sidecost/currency/scopes/conditionversion, global acrosssmets cap и exactnetrounding, sharedreceipt/history; delete/changeallocation guard costlinked. Затем reader отчёта снераспределённымипроектнымирасходами, актуальностьsnapshot, void/correction workflow, source/category lookup, UImanual+sourceconfirmation+distribution, executioncomparison/Excel. Nativeexpensecurrencyсампосебенеизвестна — explicitchoice остается обязательным. Проверить реальноеsourcepermissionrouting/rolecatalog иUI доclaimsдоступности. Concurrent sameUUID manualcreation fromdifferentestimates можетдойти доDBunique вместоfriendly409; обработатьtargetedunique conflict прифинальномconcurrencyблоке. Source native lock сериализуетduplicateadvance регистрации. Нетдоказательствавсегопроцесса/productionготовности, цельв полномобъёмесохраняется.

## Распределение собственных расходов по условиям позиций
- Backend98631069d: own_cost_distribution подключена в existing preview/save и FormRequests. EstimateFinanceOwnCostDistribution.handle проверяет finance/project/org, блокирует смету и scoped owncost; advance source требует advance_transactions.view и совпадения текущего native document snapshot с сохранённым. Sharedreceipt actor/hash/revision replay, source_version/source_hash, confirmed status. Распределяет только allocation source=own/side=cost/no contract/currency match, nonexcluded target текущей сметы; line condition/version conflicts; editContracts проверяет связанные договоры целевых позиций. Все записи расхода блокируются под cost lock, общий лимит между сметами. Запись500bulk, stableID/key, version/source_version/condition_version/history/sharedreceipt.
- Net округление без изменения остальных записей: из исходных gross/net вычитаются все неизменяемые распределения всех смет; оставшийся net разносится FinanceDecimal.allocate по новым grossвесам выбранных строк и нераспределённому остатку. Итог точный, неизвестныйnet сохраняетсяNULL; приполномраспределении0/0 использован0 безделения. Уже сохранённые другиестроки не пересчитываются молча. Ноль — новаяверсияснятия суммы, stableID/history остаются. Существующий FinanceService теперьдаётfriendly own_cost_linked приdelete/change source/contract/currency/target cash-likeguard; ценыусловиймогутменяться безпереписывания расхода.
- PG test_own_cost_distribution_caps_all_estimates_and_keeps_exact_net_and_history PASS1/15 finance-own-cost-distribution-tests.log,01:24.310,164MB,exec25282terminal0. Три gross0.01 по2сметам исходныйgross0.03/net0.02; exactnet0.02, untouchedfirstrowequals, replaynohistoryduplicate, caperror, staleversion409, unlinkguard, explicitzero сохраняетUUID иversion2/history4. PHPStan4productionfilesPASS finance-own-cost-distribution-phpstan.log exec79143terminal0; php-lnewservice/testPASS/diffcheckPASS; profilesrestored45433. Нетlivehandles.
- Этоwriter, неготовыйэкран/весьрасходныйфакт. Следующее: reader own_costs в report/project с projectunallocated, batch source freshness/access checks (без forbidden nativequeries), allocation totals/sections+source drilldown; корректировки/аннулирование source с историей; source/category lookup, UIregistration/distribution; Excel+execution owncost отдельнопоказателем, не сложение сcash. Дляdistribution отдельно добратьtests source changed/denied/currency/org/target/unknownnet, источникрасходаvoided, масспакет/конкурентность/ошибочныйlegacyledger. ПрямойduplicateUUIDregisterrace изпредыдущейзаписи ещё открыт. Полнаяцель/перенос/реальные275436/права/8k/CI/доки/деплой сохраняются. Не push/deploy.

## Чтение собственных расходов в отчётах
- Backend6c5bfff4c: EstimateFinanceOwnCostReport.report(actor,project,estimateId|null,basis) scopedfinanceaccess, batchcosts/project, batchnativeonlywhenadvance_transactions.view, ledgerjoin allocations/estimates/items/resources. reportEstimate own_costs толькоviewexecution; projectReport получает отдельныйprojectown_costs непосредственноreader сestimateIdnull, не суммируетповторныеисточникиперсметам. Publiccontract: basis/source_scope=project/allocation_scope=estimate|project, sources/projectsource_totals, rows/allocated_totals/positions. Sources имеютsavedgross/net, selectedbasisamount, allocated/remaininggross/net, sourcehash/version/category/date/basis/VAT/state; rows stableID/keys/current+recordedconditionversion иgross/netдиагностику. Проектныеисточники и распределённаячастьсметы разведены.
- Изменённыйnative document snapshot, stale sourceversion, ledgerwrongscope/ownsource/currency, превышениеgross/net помечаютсяrequires_review, подтверждённыеamountNULL. Deniedadvance source не запрашивает native table, скрываетbasis/amount/source реквизиты и возвращаетavailablefalse; еслиестьраспределенияввыбраннойобласти, allocated_totals[''] unknown вместоfalsezero. Voidedsources/rows видныисторически, исключеныизитогов. Всеunallocatedsourcesпроекта виднывsource totals; стоимостьнеавторазноситсяпо сметам. Ресурсноеназвание берётсяresource.name scopedпоitem, отдельныеqueryperrow отсутствуют. SectionsrollupпокаНЕдобавлен.
- Тестпервогоreader выявил JSONB-orderbug: PHPjson_encode native object versus decodedJSONBстрока отличаютсяпорядкомключей. Исправленыreader И distribution на сравнение normalized decodedarrays по содержимому, беззависимостиотпорядка. Дополнительно протестирован реальныйdistribution зарегистрированногоadvance expense100 из120: раньшеэтотпутьложноконфликтовал; теперьсохраняется, источниксумма120/allocated100, изменениеdescriptionпослерегистрации даётNULLобоимитогам, deniedнетnativequeryиNULLunknown.
- Финальный PG PASS2/44 finance-own-cost-report-final-tests.log,01:27.297,172MB,exec46655terminal0. Перваяпопытка2/34имела1failureнаunchangedsourceNULL, исправлено каквыше. Провереныprojectdedupgross/net2smets иостаткипослеzero, historyunchanged, positive/changed/deniedsource, нетложныхнулей. PHPStanreader+Service initialPASS; финальныйreader+distributionPASS finance-own-cost-report-final-phpstan.log exec57746terminal0; php-lPASS/diffcheckPASS, profiles45433. Все свои процессыterminal.
- Следующее: UI/Excel owncost (types/report metadata, readable owncostsource/table/form registration+distribution, lookupcategory/approvedsource, без forbiddenrequests), sections, void/correction workflow/historydrilldown, owncostfact сопоставлениеcapproved revenue без смешиванияcash; покрытьreadlegacycorruption/unknownVAT/currency/resource/excludedhistorypolicy/scale/concurrency. own_cost_report.source_totals всегдаproject, НЕЛЬЗЯсложитьихмеждусметами; allocated_totals толькоrequestedestimate/project. В reported rows recorded_condition_version сохранён отдельноотcurrentcondition_version дляправильногоредактирования/истории. Остальнаяполнаяцель/миграция/реальнаярегрессия/CI/права/8k/доки/deploy всёещё открыты. No push/deploy.

## Собственные расходы в интерфейсе сметы и проекта
- Admin OwnCostPanel и типы ownCostFinance.ts, optional own_costs в FinanceReport/ProjectFinanceReport. Панель встроена отдельноотактов во вкладкуexecution обоихэкранов: наличиефинансовыхрасходов невыдаётдоступк актам. Дваявноподписанныхитога: все project owncost sources ираспределённаячастьтекущейсметы/проекта, backendtotals несложены повторно. Источникитаблица25настраницу, поискпооснованию/категориивовсеммассиве, максимальнаяширинапоиска460. Суммы/остаток выбираютсяпоbasis; unavailable/unknown — тире/недостаточноданных, запретоткрытияоснования. Состояниявидны.
- Карточкаоснования: полноеbasis pre-wrap/anywhere, ручнаязаписьилиномерподотчётногорасхода, категория, savedgross/net длясверки, предупреждениеrequires_review, детализацияпозиционныхраспределенийвыбраннойобласти50настраницу. Русскиеaria/пагинация, неизвестноеназвание не заменяетсятехключом. No nativeAPIrequests вreadonlyкомпоненте, используетexistingreport. Пока неткнопкирегистрации/измененияраспределения, nativeдокументнеоткрываетсяотдельнойстраницей, толькооснованиекарточкой; никакого утвержденияполнойготовностиowncostUI.
- Vitest3files PASS16 finance-own-cost-panel-tests.log (OwnCostPanel3/Workspace10/Project3), 15.59s,exec74124terminal0. Проверен8001source→25DOMrows/searchlast, full100linebasis и51allocation→50DOMrows/next, hiddenbasisнеткнопки/unknownnotzero, existingflowsрегрессия. ESLint6filesPASS finance-own-cost-panel-eslint.log terminal0; npx tsc --noEmit PASS finance-own-cost-panel-types.log exec51987terminal0; Prettier3newfilesdone/diffcheckPASS. Все своипроцессыterminal. Source/tableкраткоеоснованиепокаНЕпоказываетсярядомсобщейкнопкой (категория/дата/суммыесть): приformUX добавитькороткийpreviewдляразличенияпохожихрасходов; вдиалогеlabel«сналогами» заменитьна«суммарасхода»дляunknownVATточности. No build/devserver/push/deploy.
- Следующее: APItypes+services own_cost/own_cost_distribution, registrationanddistributiondialogs сpreview/save/retry409/revision/hash иправами; нуженlookup orgcostcategories/approvednativeexpenses сpermission ипроектом(текущиеfinanceAPIещёнеотдаютoptions; использоватьсуществующиеилиузкорасширитьpreview). Затем Excel/sections/voidhistory/сопоставлениеowncostфакта; остальныеоткрытыепунктыполнойцелинеизменны. CBMadmingeneration2026-09-12T14:22:19Z: interface/workspace snippets былисмещены/неполны, выполненоboundedчтение; новыеfilesnottracked, coveragechecked. Backendвэтомблокекоднеизменён.

## Выбор оснований и клиентские команды собственных расходов
- Backendab66ebf63: preview_operation=own_cost_options(kind categories|documents, query<=200, after>=0) в existingPreviewRequest/FinanceService. EstimateFinanceOwnCostOptions.search требуетfinanceedit/projectscope. Категорииactiveorganization; документыsameorg/project approved expense approved_atnotnull/amountpositive иnotexists owncost.sourceID(включаявойдедисторию). Документыбезadvance_transactions.view возвращаютavailablefalse/data[]/nextnull ДОnativequery. Поискname/code илиdescription/documentnumber черезescapedliteralILIKE (проценты/underscore/backslash), cursorID+limit51→50data/next. Документ отдаётid/number/date/description/amount/categoryID/currencyNULL(неугадана).
- BackendPG PASS2/30 finance-own-cost-options-tests.log,01:26.493,170MB,exec82047terminal0: 52owncategories50+2/nooverlap, чужая/неактивнаяисключены, literal%findone; docsforeignorg/otherproject/issueexcluded, unknowncurrencyNULL, alreadyregisteredexcluded, deniednoquery. PHPStan3filesPASS finance-own-cost-options-phpstan.log exec10568terminal0; php-lPASS/diffcheckPASS; profiles45433. No livebackendhandles.
- Admin ownCostFinance typedregistration/preview/distribution/preview/optionsgenerics. estimateFinanceService ownCostOptions AbortSignal/search/cursor, previewOwnCost/saveOwnCost(requiredsourcehash), previewOwnCostDistribution/saveOwnCostDistribution. ВсёчерезexistingPOSTpreview/PUTfinance. NativeoptioncurrencytypedNULL. MSWinitial9tests1failure: genericunwrap повторноизвлекал businessdata[] из нормализованногоapi response итерялavailable/next_cursor. ПоCBM прочитан normalizeStandardApiSuccessResponse: добавляетmetadata, сохраняетbusinessdata. МетодownCostOptions теперьпервымпринимаетструктуруkind+availableцеликом, иначеunwrapwrapper. ОбщийapiResponseнеизменён.
- AdminfinalMSW PASS9 finance-own-cost-api-final-tests.log,3.71s; initial8pass1fail сохранёнвfinance-own-cost-api-tests.log. ESLint3filesinitialPASS ипослефиксаservicePASS finance-own-cost-api-final-eslint.log; финальныйtsc45377PASS finance-own-cost-api-final-types.log,initial39027PASSдоправки; Prettiertypesdone/diffcheckPASS. Всеprocesshandlesterminal. Команды+типы закоммичены, формыПОКАнеподключены. Нетpush/deploy.
- Следующийшаг: OwnCostRegistrationDialog с ручнымподтверждением/date/basis/category/currencyexplicit/VAT, optionsпоискdebounce/abort/loadmore, preview→saveсsameUUIDretry/sourcehash/409reload; OwnCostDistributionDialog ownallocations/sourceversions/globalremaining иtable50/search. КнопкивWorkspaceтолькоreport.can_edit; projectflowвыбираетконкретнуюсмету. OwnCostPanel actioncallbackssimple, source readonlyкарточкуулучшитькороткимbasispreviewиlabelgrossнейтральным. ДляsourcecategoryIDможетнепопастьвпервые50категорий: передUIрешитьlookupselectedID илиотдаватьscopedcategorynameвdocumentoptions; неугадыватьназвание/валюту. Нынешнийsource registration допускает толькоactivecategory; явнообработатьисторическийдокументснеактивнойкатегорией вUX/сервисе. ДальнейшиеExcel/sections/voidhistory/fullplanfact/manualexecution/migration/275436/8k/CI/docs/deploy остаютсяобязательными; полнаяцельНЕготова.

## Форма регистрации собственного расхода
- Backend7428f68d3: documentoptions включаетscoped category_name/category_active batchlookup, поэтомукатегориядокументанеобязанабытьвпервых50options. Registrar manual требуетactivecategory; advance разрешаетнеактивнуюкатегориюТОЛЬКОеслиэтоименносохранённаякатегорияnativeдокумента, orgпроверен. АктивнаяноваякатегорияприnativecategoryNULL какраньше. Softdeletedcategory всёещёнеподдержана(скрытачерезSoftDeletes), невыдаватьзаабсолютнополныйисторическийслучай. PG2/42 PASS finance-own-cost-category-tests.log,01:49.514,172MB,exec59132terminal0; sourcecategoryinactivefixture сохраняется/optionsname+falseверны. PHPStan2filesPASS finance-own-cost-category-phpstan.log exec41951terminal0/php-lPASS. Manualinactiveguard явный, отдельныйnegativeassert добавитьприобщейприёмке.
- Admin OwnCostRegistrationDialog встроенизOwnCostPanel onCreate толькоreport.can_edit вWorkspace; key=report.revision, onSaved/reload/close. canViewDocuments изadvance_transactions.view: безправдокументынепредлагаются/не запрашиваются. HookuseOwnCostOptions generic: 250mssearchdebounce, AbortControllercleanup/late resultguard, cursorloadmore, dedupIDs, retry, error/available. category select с поиском/загрузитьещё; документ approvedoptions иcategoryиздокумента readonly, втомчислеinactive. Исходныйдокументнеизменяется. Currencyinitialempty explicit3letters, VATinitialempty explicitnone/included/exclusive/unknown, requiredrateдляналоговыхрежимов, amount/date/basis иcheckboxconfirmed. Nativeamount автозаполняетсякакdocumentgross, приexclusiveподписано«СуммабезНДС»иитогдокументавиден; пользовательвводитсоответствующуюбазу, серверпроверяетgrossmatch.
- Preview→save gating, любоеизменениеполейсбрасываетpreview/ref; samecostKeyнасеанс, mutationIDприновойпроверке, приnetworkretrysave тотжепакет/sourcehash. 409blocksform+reload,422/403/networkчеловеческиетексты; busyinputs/closeblocked, saveвалидируеттакжетекущиеправа/valid. Компонентpropsreport толькоPickestimate_id/revision. OwnCostPanel buttonleft, нерастянут. Новые3файлаprettier.
- InitialVitest3files15tests:14PASS,1timeout5sвдлинномручномсценарииприодновременныхPG/PHPStan/typechecks; nativecasepassed. Scopedretest сexplicit15stimeout толькоэтих2многошаговыхcase PASS2 finance-own-cost-registration-ui-final-tests.log (2.575s/1.392s), 13.12soverall,exec10095terminal0. Сценарииmanualconfirm/preview/editinvalidates/retry500samebody+UUID/nodocqueries; nativecategoryID53notinoptions/currencyrequired/sourceparams/409reload. Workspace10+OwnCostPanel3passedinitial, новоеповедениепослеретеста затронулотолькоdialogsaveguard. ESLint6filesPASS finance-own-cost-registration-ui-eslint.log (толькоcaniuseoutdatedwarning, не обновлялибиблиотеки). Finaltsc60396PASS finance-own-cost-registration-ui-final-types.log; initial53371PASSраньше. Все своиprocessesterminal, no builds/devserver/push/deploy.
- Следующийблок: OwnCostDistributionDialog с источникомизown_costs.sources available/confirmed/notreview, собственными FinanceRow.allocations/currency иledgerrowversions, table50/search, preview/save/retry409; интеграцияonDistributecallbackOwnCostPanel/Workspace. Дальшеprojectchooseestimatepath, owncostExcel/sections/voidhistory, manualexecutionиостальнаяполнаяприёмка/миграция/275436/8k/docs/CI/deploy. РегистрациярасходапокаНЕвыполняетавтораспределение, после неёисточниквиденнераспределённымкакзадумано. Неотмечатьполнуюцельготовой. Sourcecard shortbasispreview инейтральныйgrosslabelвсёещёвtodo. Дополнительныетестыhookrace/loadmore/revoke иnativeunknowncategoryследуютприкомплекснойприёмке.

## Распределение собственных расходов в админке
- Admin d3bb813f: OwnCostDistributionDialog подключён через OwnCostPanel и EstimateFinanceWorkspace в представлении выполнения. Источник доступен только при can_edit, available, confirmed и отсутствии requires_review. Выбор из собственных условий текущей сметы с известной версией и совпадающей валютой; исключённые строки и legacy пропускаются. Поиск по всему составу, 50 строк в DOM. Полная сумма и остаток по всем сметам показаны отдельно; ввод gross, net возвращает сервер. Ноль снимает сумму с сохранением истории через ранее проверенный серверный сервис.
- Preview/save передают revision, mutation_id, source_version/hash, condition_version, ledger version. Изменение поля сбрасывает предпросмотр; повтор сохранения после 500 отправляет тот же пакет, 409 блокирует форму до обновления. Успех закрывает форму и перезагружает отчёт. Это не автоматическое распределение расходов и не изменение документа оплаты.
- Vitest PASS16 (dialog3, panel3, workspace10), finance-own-cost-distribution-ui-tests.log, exec23797 terminal0. Дополнен отдельный тест права/confirmed/review/voided: panel PASS4, finance-own-cost-distribution-rights-tests.log, exec76122 terminal0. ESLint изменённых файлов PASS, finance-own-cost-distribution-ui-eslint.log и finance-own-cost-distribution-rights-eslint.log. tsc --noEmit PASS, finance-own-cost-distribution-ui-types.log, exec51444 terminal0. Diff check PASS. Все свои процессы завершены, сборки/серверы/push/deploy не запускались.
- Осталось: собственные расходы в Excel и разрезах разделов/сравнении выполнения; изменение/аннулирование источников с историей; распределение неразнесённого выполнения; перенос старых данных, аудит всех потребителей и реальные 275/436, права и нагрузка, документация, интеграция main и штатный деплой. Полная цель остаётся незавершённой.

## Собственные расходы в Excel выполнения
- Backend 7fecbdbee: EstimateFinanceExport передаёт project.own_costs отдельным последним необязательным аргументом workbook. В представлении execution добавлены листы «Собственные расходы», «Основания расходов», «Расходы позиций». Проектные источники и их source_totals берутся один раз из общего отчёта, для одиночной сметы из её project-scope own_costs. По сметам выводятся только allocated_totals/rows; источники не суммируются повторно. Итоги не пересчитываются в экспортёре, используются значения общего расчёта. Расходы отделены от актов и денег.
- Сохранённые gross/net отделены от подтверждённой суммы; неизвестное остаётся пустым. Недоступный источник раскрывает только ключ и статус, без основания/сумм. Версии строк и исторических условий присутствуют, аннулирование/review явно подписаны. Листы используют существующий явный тип ячеек, фильтры и фиксированные заголовки. Права проверяет finance report перед экспортом.
- PostgreSQL16 test_own_cost_distribution_caps_all_estimates_and_keeps_exact_net_and_history PASS1/32, finance-own-cost-export-tests.log,01:24.661/168MB,exec68259 terminal0: источник .03 gross/.02 net распределён тремя строками между двумя сметами, в XLSX источник один, строки три, итог .02, остаток0, historicalcondition1. PHPStan PASS finance-own-cost-export-phpstan.log,exec57116 terminal0; php-l PASS/diffcheckPASS. Три PG профиля восстановлены, других tracked изменений нет. Денежные/актовые регрессии и негативные кейсы unknown/denied/formula-safe для новых листов нужно расширить при общей проверке экспорта; отдельный текущий тест не доказывает весь экспорт.
- Дальше: разделы и сравнение собственных расходов с выполнением, исправление/аннулирование источников с историей, ручное распределение выполнения; перенос старых данных/все потребители/275436/8k/права/документация/main/CI/штатный деплой. Полная цель не завершена. Админка остаётся d3bb813f, backend код 7fecbdbee. Все свои процессы завершены; push/deploy не выполнялись.

## Негативные проверки Excel собственных расходов
- Расширены реальные PostgreSQL сценарии регистрации ручного расхода и доступа к подотчётному источнику: неизвестный НДС сохранён, сводка net=NULL при известной части100/unknowncount1; gross100 сохранён отдельно, net/confirmedamount пустые. Основание =SUM(1,2) имеет TYPE_STRING. Недоступный native source экспортирует ключ/статус, колонки B–M пустые и распределённых строк нет. Тесты reader по-прежнему подтверждают отсутствие native-запросов при запрещённом advance_transactions.view.
- PASS2/64, finance-own-cost-export-access-tests.log,01:35.310/174MB,exec14128 terminal0. php-l теста и diffcheck PASS. Production-код в этом блоке не менялся. PGпрофили восстановлены. Все свои процессы завершены.
- Навигация для следующего блока: EstimateFinanceOwnCostReport не отслеживается графом (coverage not_tracked); bounded source17–48,97–158 прочитан. Сейчас report выдаёт rows.section_id и positions, но sections ещё нет. Добавление разреза разделов требует scoped batch дерева estimate_sections, подъёма к родителям без двойного прибавления и проверки циклов/чужого estimate; денежные результаты и неизвестное брать из того же add. Требуется интеграция с UI/Excel и отдельная проверка вложенных разделов/валют/недоступных данных. Полная цель, миграция/факт/275436/права/8k/docs/деплой остаются незавершёнными.

## Серверный разрез собственных расходов по разделам
- EstimateFinanceOwnCostReport теперь отдаёт sections: estimate_id/section_id/name/parent_section_id/hierarchy_requires_review/totals. Одним scoped join-запросом получает дерево только org/project и estimate_id доступных строк; путь к родителям кешируется по estimate+section. Каждая исходная строка добавляется один раз в свой раздел и каждый уникальный родитель; allocated_totals по-прежнему считаются непосредственно по строкам. Валюты и неизвестные суммы используют общий add. Voided пропускается. Позиции без раздела попадают в bucket null. Цикл/чужой раздел/отсутствующий родитель обрывают путь с hierarchy_requires_review, закрытое имя не раскрывается.
- PG16 тест own_cost_distribution_caps_all_estimates_and_keeps_exact_net_and_history расширен parent/child и второй сметой без раздела. PASS1/36, finance-own-cost-sections-tests.log,01:19.315/168MB,exec7527 terminal0. Child=.01 и parent=.01, общий итог сметы=.01 и проекта=.02 сохранены; старые проверки точности/лимита/версий/Excel прошли. PHPStan PASS finance-own-cost-sections-phpstan.log,exec95189 terminal0;php-l/diffcheckPASS. Все процессы завершены, PGпрофили восстановлены.
- Разрез пока только в серверном контракте: следующий шаг тип sections в ownCostFinance, таблица разделов в OwnCostPanel с пагинацией/переходом к позициям, отдельный лист Excel. Прямые регрессии cycle/foreign-parent/unknown/несколько валют и нагрузка дерева ещё не написаны; защита в коде не считается их проверкой. Сравнение собственного расхода с принятым выполнением, коррекции источников, ручное распределение выполнения, перенос, 275436,8k,права,docs/main/CI/deploy остаются обязательными. Полная цель не завершена.

## Разделы расходов в интерфейсе сметы и Excel
- Admin d240e510: optional sections в OwnCostReport, OwnCostSections внутри OwnCostPanel. Для allocation_scope=estimate таблица серверных итогов (валюты отдельно, unknown не ноль), 50 разделов/страница; диалог включает строки раздела и потомков через дерево с visited guard, 50 строк/страница и переход в существующий диалог основания расхода. Аннулированные строки не включаются. Подпись предупреждает, что родитель уже включает потомков. Показан hierarchy_requires_review. Проектный экран пока НЕ показывает этот компонент — нужно дополнить именами смет и переходами.
- UI Vitest PASS5 (новый тест вложенного раздела с51строками/unknown/source navigation + panel4), finance-own-cost-sections-ui-tests.log,exec3928 terminal0. tsc PASS finance-own-cost-sections-ui-types.log,exec81581 terminal0. ESLint initial warning о fallbackarray устранён useMemo; finalfinance-own-cost-sections-ui-final-eslint.log PASS. DiffcheckPASS. Последняя memoправка не меняет контракт/поведение теста.
- Backend Excel: дополнительный лист «Расходы разделов» с серверными confirmed/known/unknown, валютой, областью учёта и состоянием иерархии. Не суммирует родительские/дочерние строки. PG16PASS1/40 finance-own-cost-section-export-tests.log,01:22.073/168MB,exec13891 terminal0: child иparent по.01, третья строка безраздела второй сметы; источник/общий итог остаются прежними. PHPStan PASS finance-own-cost-section-export-phpstan.log,exec92947 terminal0;php-l/diffcheckPASS. PGпрофили восстановлены; все процессы завершены. Push/deploy не выполнялись.
- Следующие обязательные блоки: проектный разрез с именами смет; сопоставление owncost с выполнением/полнотой оценки; коррекции источников с историей; ручное распределение неразнесённого выполнения; перенос старых данных/потребители/275436/нагрузка/права/workflowdocs/main/CI/штатныйдеплой. Прямые тесты повреждённой иерархии и большой глубины остаются в приёмке. Полная цель не достигнута.

## Проектный разрез разделов собственных расходов
- OwnCostSections теперь работает и при allocation_scope=project. ProjectEstimateFinance передаёт через OwnCostPanel map estimate_id→номер+название из того же разрешённого отчёта; подпись сметы показана в строке раздела и в диалоге. Поиск дочерних строк уже изолирован estimate_id, одинаковые названия разделов не объединяются. Если метаданных сметы нет, выводится «Название сметы недоступно», а не выдуманный номер.
- Исправлена доступность Excel в Workspace и ProjectEstimateFinance: запрет актов не блокирует экспорт собственных расходов, если own_costs получен. Если недоступны и акты, и расходы — прежний запрет остаётся. Серверное разграничение источников не ослаблялось.
- Vitest PASS15 finance-own-cost-project-sections-tests.log: project4 (новый сценарий одинаковых разделов двух смет и Excel при запрещённых актах), sections1,workspace10. exec26518terminal0. tsc PASS finance-own-cost-project-sections-types.log,exec1670terminal0. ESLint PASS finance-own-cost-project-sections-eslint.log;diffcheckPASS. Все свои процессы завершены. Backend productioncode в этом блоке не менялся; push/deploy не запускались.
- Остались обязательные крупные блоки: owncost+acceptedexecution comparison и полнота, коррекции/аннулирование источника с историей, ручное распределение выполнения, перенос legacy с dry-run и сохранением ID, полная сверка потребителей/275436/8k/конкурентность/права, workflowdocs/main/CI/штатныйдеплой. Цель остаётся активной, не завершённой.

## Проверка старых связей перед переносом
- Аудит обнаружил: EstimateCoverageService.backfillLegacyCoverage всё ещё вызывает attachFullCoverage без actor. Его НЕ использовать для переноса: новый адаптер требует actor; вызов пересоздаёт состав целиком и не удовлетворяет dry-run/истории/сохранению условий. Метод пока не заменён. CBM exactsnippet368–383 подтверждён, Query.allocations legacy ветка прочитана snippet, coverage metadata_changed.
- Исправлено чтение legacy в EstimateFinanceQuery: eager relation contract scoped organization_id/project_id текущей сметы. Недоступный/несогласованный договор не определяет side/currency. Currency неизвестная пустая строка, не фиктивный RUB. Legacy link ID, amount и quantity сохранены; composition остаётся неподтверждённой. Существующие новые allocations не изменялись.
- PostgreSQL16 PASS1/10 finance-legacy-read-scope-tests.log,01:18.270/164MB,exec26052terminal0: чужая организация→unknown/emptycurrency, иной project(null)→unknown/emptycurrency, исправленная область→EUR/cost, stablelegacykey и amount123.45 сохранены. PHPStan PASS finance-legacy-read-scope-phpstan.log,exec76194terminal0;php-l/diffcheckPASS. Все процессы завершены, PGпрофили восстановлены.
- Следующий конкретный шаг переноса: read-only планировщик миграции с actor/org/project scope и пагинацией по legacyID, snapshot/hash, explicitreasoncodes и исходными полями. Query.allocations сейчас для всех legacy намеренно vat_mode/price_basis unknown и compositionfalse; нельзя объявить их автоматически подтверждёнными по разнице gross/net. Нужно исследовать schema/metadata ContractEstimateItem для явно сохранённых VAT/composition и fact-связей, затем повторяемый apply через единый сервис с исходными IDs, conflict/replay/history и отчётом до/после. Сам dry-run и apply ещё НЕ реализованы. Полная цель остаётся активной; остальные крупные блоки из предыдущих записей сохраняются.

## Предварительный план переноса старых привязок
- Backend f69d889e0 добавлен EstimateFinanceMigrationPlan.report(actor, projectId, estimateId, after=0,limit=100). Проверка EstimateFinanceAccess.estimate; limit1–500, keysetпоlegacyID(limit+1), legacy finance_managed false/null. Contract relation scoped org/project, item scoped estimate. Возвращает read_only=true/revision/rows/next_cursor; исходный ID, quantity/gross/net/notes/updated_at, доступные direction/currency, причины и SHA256snapshot. Отсутствующие/чужие contract/item→blocked. Налоговые условия и состав цены явно не сохранены в ContractEstimateItem, поэтому без вывода ставки по разнице сумм всегда requires_review/confirmed_margin_eligiblefalse; can_preserve_as_unreviewed лишь обозначает согласованную область, не является разрешением apply. Excluded/parent_work_id добавляют причины проверки.
- PG16 PASS1/14 finance-migration-plan-tests.log,01:16.671/164MB,exec11667terminal0: limit1/cursorдвухсвязей, повторный план идентичен, исходные атрибуты не меняются, allocationcount0, суммы120/100 не порождают известныйНДС, изменениеamount→новыйhash. PHPStanPASS finance-migration-plan-phpstan.log,exec32427terminal0;php-l/diffcheckPASS. PGпрофили восстановлены, все процессы завершены.
- Это пока внутренний read-only сервис, без API/CLI/apply. Следующие обязательные дополнения: факт/ссылки на документы и ужеуправляемые связи в отчёте, финальные reason translations/экспорт, сохранение исходных IDs в повторяемом apply через FinanceService; адаптер старого actorless backfill. Проверить прямые scope/permissions/cursorbadvalue/malformed cases и большой объём. Не запускать планировщик вручную против рабочей БД — только тестовый launcher и разрешённый штатный путь production. Полная цель и остальные крупные блоки остаются незавершёнными.

## План переноса через существующий preview
- PreviewEstimateFinanceRequest: preview_operation=migration_plan с after>=0 и limit1..500; EstimateFinanceService constructor внедряет EstimateFinanceMigrationPlan, dispatch валидирует через те же static rules даже при прямом сервисном вызове, затем read-only report. Новых маршрутов/контуров нет; используется existing POST finance/preview. Права/область проверяет планировщик через EstimateFinanceAccess; route middleware не менялся.
- PostgreSQL16 PASS1/18 finance-migration-plan-preview-tests.log,01:18.641/164MB,exec41798terminal0: service.preview возвращает тот же report, invalidlimit-1/0/501 отклоняются, прежние checks no-write/pagination/hashchange сохранены. Это сервисная интеграция, НЕ отдельный HTTP-auth тест. PHPStan2productionfiles PASS finance-migration-plan-preview-phpstan.log,exec31535terminal0;php-l2/diffcheckPASS. PGпрофили восстановлены, все свои процессы завершены.
- Предварительный отчёт пока не имеет UI/экспорта/локализованных причин, проверки факта и ужеуправляемых связей; apply ещё нет. Следующее: дополнить план источниками/согласованностью и сделать повторяемый apply без пересоздания ContractEstimateItem и без изменения актов, затем UI/документы/релиз. Остальная полная цель остаётся незавершённой; push/deploy не выполнялись.

## Применение переноса старых привязок
- Добавлен EstimateFinanceMigrationApply, dispatch existing FinanceService.save operation=migration_apply, SaveRequest.migrationRules revision/mutationUUID/links1..500 distinctIDs+SHA256. Валидация повторяется внутриapply. Plan.report принимает дополнительный внутренний nullablelinkIds для пакетной проверки выбранных IDs без N+1.
- TX: financeedit/estimate scope, estimate lock, receipt actor/hash replay, revision conflict, scopedlinks lock sorted, contract/item locks, повторный plan snapshot для IDs, editContracts, blocked/changedhash→409 beforewrites. Stable allocation UUIDv5 namespaceURL most:estimate-finance:legacy:ID; allocation.sourcecontract/sideизплана/currencyexplicitorunknown, originalquantity/net/legacyamount, grossNULL/vatunknown/pricebasisunknown/compositionfalse. estimate_snapshot хранит migration_source. Existing allocation forlink/key→409. Одна bulk insert, оригинальные ContractEstimateItem изменяют ТОЛЬКОfinance_managed=true (ID/quantity/amount/net/notes/timestamps не переписываются), finance_revision++, history.record created, sharedmutationreceipt сохраняет migrationreport+keys; cacheinvalidate. Нет projectLinks/нормализации/автопересчётастарыхусловий, актов/оплаткоднепишет.
- PG16 PASS1/30 finance-migration-apply-tests.log,01:18.142/164MB,exec62901terminal0: неверныйhash2йстроки откатываетвсё, 2связиперенесены, repeatedsamecommandreplayed/2allocations/2history, originalattrsкромеflagидентичны, legacy130/net100/grossNULL/compositionfalse, oldplanrowsпослепереносапусто. PHPStan4productionfilesPASS finance-migration-apply-phpstan.log,exec58015terminal0;php-l4/diffcheckPASS. PGпрофили восстановлены; все процессы завершены.
- Важные оставшиеся проверки/доработки: настоящие approvedacts/ссылки snapshotдо-после; повтор сновымmutation (сейчас409 наmanagedlinks, дублейнет; нуженпонятныйreportalreadyprocessed), independentconcurrency/rollbackandrights; scope recheck lockedEstimate послеrace; retired deterministicUUID collisionhistory; контрольвсехпотребителейunknownlegacygross (projectionread); enrichedplanfact/managedrows, локализация/UI/экспорт/dryrunallorg изаменастарогоbackfill. Прямой ручной productionapply не запускать, только штатныйразрешённыйрелиз. Сампереноснеобъявлятьполностьюготовым поузкомутесту. Остальные крупные блоки плана остаются обязательными.

## Перенос при подписанном акте и оплате
- Тест подписанного акта выявил реальный сбой после миграции: estimate_snapshot содержал только migration_source, Calculate ожидал quantity/estimate_amount/unit_id и падал Undefinedarrayquantity. Исправлено в MigrationApply: batchlockedItems keyByID используется для снимка исходного target.quantity (quantity_total??quantity), total_amount/unit_id вместе с migration_source. Никаких новых N+1. LockEstimate теперь дополнительно org/project scoped, закрывая race между access и lock.
- Новый тест migration_preserves_signed_act_lines_and_payment_history создаёт legacy link, signed act150/строку120 со старым basis_snapshot, платёжный документ+транзакцию50. Сравнивает полные persisted attributes четырёх документов до/после, execution.rows/documents/summary.totals до/после, replay и ровно1allocationнастарыйlinkID.
- Initial finance-migration-fact-tests.log ERRORquantity (exec49723terminal2) исправлен, не скрыт. FinalPG16 PASS2/36 (fact + предыдущийplan/applyatomic/replay), finance-migration-fact-final-tests.log,01:22.421/172MB,exec30871terminal0. PHPStanfinal PASS finance-migration-fact-final-phpstan.log,exec91916terminal0; initial92304terminal0;php-l/diffcheckPASS. Все свои процессы завершены, PGпрофили восстановлены.
- Перенос пока требует remaining gates: oldlegacyCompletedWorks pivot path, rights/foreignscope/batchconcurrency/newmutationrepeat/managedreport/retiredUUID; UI/локализация/exportreport/overallorgdryrun иactorlessbackfillreplacement. Остальные крупные блоки плана сохраняются: manualexecutiondistribution, owncost corrections/comparison,consumer275436/8k/workflowdocs/main/CI/deploy. На production ничего не применено.

## Права и история повторного переноса
- MigrationApply теперь проверяет estimate_finance_condition_versions для детерминированных allocationUUID наряду с активными allocations: ужеиспользованныйUUID не создаётся заново даже при повреждённом finance_managedflag. Возвращает409 доwrite. Это дополняет запретвозрожденияудалённыхключей обычного редактора.
- PG16 новыйtest_migration_requires_contract_edit_and_does_not_resurrect_retired_conditions PASS1/5, finance-migration-rights-tests.log,01:18.546/164MB,exec35505terminal0. Financialeditбезcontracts.edit→AuthorizationException, allocation0/legacyflagfalse. Затем разрешены права и добавлена retiredhistory:409,allocation0/flagfalse/mutationreceipt0. PHPStanPASS finance-migration-rights-phpstan.log,exec21189terminal0;php-l/diffcheckPASS. Все процессы завершены, PGпрофили восстановлены;push/deployнет.
- Следующие обязательные части переноса остаются: отчёт already_managed/factссылок/до-после, UI/локализация/экспорт/полныйdryrun, legacyCompletedWorkspivot, конкурентныеотдельныепроцессы, устранениеactorlessbackfill. Остальные крупныеплановые блоки изпредыдущихзаписей не отменены. Цель активна, не завершена.

## Повторная сверка обработанных связей
- MigrationPlan.report optionalincludeManaged после internal linkIds; PreviewRequest migration_plan include_managedboolean; Service dispatch. Defaultпо-прежнему толькоfalse/nulllegacy (applyнеизменён). СincludeManaged выдаёт finance_managed записи втойжеkeysetстранице, statusalready_managed/can_preservefalse/confirmed_margin_eligibleNULL, бездогадокоактуальныхусловиях. Еслиscopebroken→blocked. Длявалидныхmanaged reasonalready_managed сссылкойтекстомнафинансовыйотчёт; остальныеreasoncodes прежние, reason_messagesчерезlang/ru/estimate_finance.migration_reasons. Никакихновыхзаписейилиавтосохранений.
- PG16PASS1/35 finance-migration-managed-report-tests.log,01:18.882/164MB,exec14198terminal0: после2migrationinclude_managedвозвращает2обработанныесвязи, no-repeat/correctstatus/eligibilityNULL/Russianmessage; предыдущиеpage/hash/no-write/atomic/replaycheckspassed. PHPStan3filesPASS finance-migration-managed-report-phpstan.log,exec64665terminal0;php-l3/diffcheckPASS. Все процессы завершены,PGпрофиливосстановлены;push/deployнет.
- Remaining: UI/reportexport/названиядоговоровипозиций/ссылкинафакт/полнаясверкадо-после, legacyCompletedWorks,конкурентныепроцессы,старыйbackfill. Считатьalready_managedописаниемуправления, не подтверждениемфинансовыхусловий; confirmed_margin_eligibleNULLдляmanagedпреднамеренен. Остальные крупные блоки утверждённого плана остаются активными.

## Проверка покрытия после миграции
- Расширен signedact/paymentmigration regression реальным EstimateCoverageService.getCoverageForEstimate до/после. withFinancialAmounts при перенесённом unknownVAT возвращает linked_amountNULL (не0), countпозиций прежний; исходная coverage120 совпадает с allocation.legacy_amount и ContractEstimateItem.amount120 сохранён. Это НЕ утверждениеравенстваподтверждённыхgrossдо-после: старыйнеопределённыйgross намереннонеподтверждается. UI ещё должен ясно отделять сохранённуюдиагностическуюсумму от подтверждённой, особенно coveragecard.
- PG16PASS1/10 finance-migration-coverage-tests.log,exec23464terminal0;php-lтеста/diffcheckPASS. Productionкодне менялся, повторPHPStanне требовался. PGпрофиливосстановлены, все процессы завершены.
- Следующий приоритет миграции: UI отчёта и диагностикасохранённойсуммы/локализованныепричины; HTTPauth/полныйdryrun,legacyCompletedWorks,конкурентность,старыйbackfill. Общийпланmanualexecution/owncostcorrections/comparison/275436/8k/docs/main/CI/deployпо-прежнемуне завершён. Нетpush/deploy.

## Контракт переноса в админке
- Добавлены типы FinanceMigrationRow/Report/Command и методы migrationReport/applyMigration существующего estimateFinanceService: preview_operation=migration_plan, страницы 100, курсор, include_managed и AbortSignal; сохранение operation=migration_apply с исходными revision/mutation_id/source_hash. Неизвестные суммы, валюта и eligibility остаются nullable; денежные строки не преобразуются в числа.
- MSW проверяет точные payload, отсутствие подстановки неизвестных сумм, курсор, replay и сохранение HTTP409 для обновления сверки. Vitest PASS11 finance-migration-api-tests.log; ESLint PASS finance-migration-api-eslint.log; tsc --noEmit PASS finance-migration-api-types.log (session8592 terminal0). Все проверки этого блока завершены.
- В backend пока незакоммичено обогащение строк MigrationPlan названиями позиций и номером договора; требует проверки перед коммитом. Окно сверки и его подключение ещё не реализованы. Следующее действие: MigrationDialog с выбором допустимых строк, диагностическими суммами, причинами проверки, пагинацией и защитой повторного сохранения/409, затем интеграция Workspace. Полный утверждённый план остаётся активным, production не изменён.

## Окно сверки старых привязок
- FinanceMigrationDialog подключён в Workspace при report.can_edit + contracts.edit. Показывает сохранённую сумму/нет данных без НДС, валюту, объём, договор и позицию, причины проверки. Не складывает валюты; явно отделяет старую сумму от подтверждённой маржи. Страницы по 100 с cursor/back, режим includeManaged, выбор только can_preserve_as_unreviewed. Смена страницы/режима очищает выбор; AbortController защищает от устаревшего ответа. При сетевой неопределённости сохраняет полный command/mutation UUID и блокирует изменение выбора до повтора/обновления, 409 требует обновить сверку. In-flight guard не допускает двойного запуска и закрытия во время сохранения.
- Backend cf6648268 добавляет contract_number/position_name/position_number из уже загруженных scoped relations, без новых запросов/изменения source_hash. PHPStan PASS finance-migration-labels-phpstan.log (91578 terminal0), php-l/diffcheck PASS. DB-логику этот пакет не меняет.
- Первые тесты и tsc упали на userEvent.setup: установленная версия использует прямой userEvent.click. Исправлено. Final Dialog Vitest PASS4 finance-migration-dialog-final-tests.log (99858 terminal0): replay same UUID, 409, readonly, cursor/reset. Workspace PASS12 finance-migration-workspace-tests.log (77535 terminal0): добавлены доступ/скрытие/отсутствие preview-запроса. tsc PASS finance-migration-dialog-final-types.log (42471 terminal0), ESLint PASS finance-migration-dialog-final-eslint.log, diffcheck PASS. Все свои процессы завершены.
- Оставшийся перенос: отчёт/экспорт полной организации, ссылки на факт и полноценное сравнение до/после, старый backfill, legacy CompletedWorks, независимая конкурентность и production сверка. Остальные пункты полного плана (manual execution distribution, корректировки расходов/история/сравнение, 275/436, 8k+, документация/main/CI/deploy) не завершены. Push/deploy отсутствуют.

## Удаление старого backfill и сохранение legacy-выполнения
- CBM search/snippet/trace для backfillLegacyCoverage: метод без actor вызывает attachFullCoverage для всех Estimate.contract_id. Из-за metadata_changed выполнен ограниченный поиск app/routes/database/tests PHP: найдено только определение, вызывающих мест не найдено в этих областях. Удалены метод и единственный импорт EloquentCollection. Вместо него используется существующий проверяемый migration_plan/migration_apply; автоматическое восстановление полного состава не выполняется.
- Расширена migration_preserves_signed_act_lines_and_payment_history: старый CompletedWork и approved акт с pivot included_quantity=0.5/included_amount=60 дополняют современный signed акт. Полные атрибуты работы/акта/pivot совпадают до/после, act_work остаётся в отчёте; предыдущие сравнения современных документов, оплат и итогов выполнены.
- PG16 PASS1/12 finance-migration-legacy-fact-tests.log,01:45.603/166MB,68856 terminal0. PHPStan EstimateCoverageService PASS finance-retire-backfill-phpstan.log,3459 terminal0; php-l двух файлов/diffcheck PASS. Профили восстановлены; своих активных процессов нет.
- Следующий крупный блок: ручное распределение нераспределённого утверждённого выполнения. Начат поиск EstimateFinanceExecution; актуальный snippet report показывает modern lines/legacy pivot и документный остаток header-minus-mapped, ручной ledger ещё отсутствует. Coverage metadata_changed, перед дальнейшими правками проверить затронутые участки/потребителей. Полный перенос организации/экспорт/конкурентность и остальные требования исходного плана сохраняются. Деплоя нет.

## Хранение ручного распределения выполнения
- Добавлена миграция 2026_09_13_030000: estimate_finance_execution_allocations со stable UUID, scope org/project/estimate, allocation/act restrict FK, уникальной парой act/allocation, currency, nullable quantity decimal20,8 и net, gross20,2, version/condition_version/source_hash/source_snapshot/condition_snapshot/actor. История estimate_finance_execution_versions с уникальной версией и restrict FK сохраняет before/after/mutation/revision/actor. Ограничения БД: положительные версии, uppercase currency, валидный hash, неотрицательные объём/gross/net, net<=gross. Ноль допускает последующее снятие распределения с сохранением истории; неизвестные quantity/net остаются NULL.
- Новый test_execution_distribution_storage_preserves_unknown_values_and_fact_references проверяет NULL, дубль act/allocation, запрет физического удаления акта/условий/записи с историей, отрицательный объём/net>gross; исходный акт100 сохраняется при ledger60. PG16 PASS1/10 finance-execution-storage-tests.log,01:54.336/164MB,50986 terminal0. PHPStan migration PASS finance-execution-storage-phpstan.log,77839 terminal0;php-l2/diffcheck PASS;профили восстановлены, своих процессов нет.
- Следующий шаг — EstimateFinanceExecutionDistribution в существующих preview/save: permission financeedit+executionview+contractsedit, scoped estimate/act locks, revision/mutation replay, source hash, остаток акта по ВСЕМ сметам за вычетом нативно привязанных строк, version conditions/ledger, decimal расчёт и история без изменения актов. Затем reader/summary/project/Excel/UI и accepted-fact guard. Quantity нужно не выдавать за 0, когда распределена лишь сумма; неизвестный net не выводить из текущего НДС. Аннулированные документы исключать из подтверждённого факта с сохранением истории. Пока реализована только схема, API записи и экран отсутствуют; полный план не завершён, деплоя нет.

## Источник остатка утверждённого акта
- EstimateFinanceExecution.report принимает optional actId для ограниченного чтения, возвращает document.unallocated_amount_without_vat = подтверждённый net документа минус известный net нативно связанных строк. Если есть неизвестный net или смешанная валюта — NULL. Основной расчёт gross остаётся прежним, необязательный фильтр не меняет обычный отчёт.
- Новый внутренний EstimateFinanceExecutionSource.read(Estimate, ContractPerformanceAct) получает scoped eligible документ из Execution/Query, отклоняет draft/annulled/unknownside/currency/ошибочный остаток, возвращает нативный остаток gross/net, source_hash и snapshot акта, всех native lines либо legacy works/pivots + capacity. Локализованная ошибка. Это helper для будущего writer под его access checks и блокировками; отдельно HTTP не выставлен. Caller должен передать свежую модель акта под блокировкой, чтобы snapshot атрибутов был актуален.
- InitialPG finance-execution-source-tests.log: существующая legacy/multicurrency regression прошла, новый тест упал на попытке изменить утверждённую строку (BusinessLogicException, защита не отключалась). Исправлена подготовка теста: изменение строки только после перехода в draft, затем approved. Final source-only PG PASS finance-execution-source-final-tests.log,74320 terminal0; PHPStan двух productionfiles PASS finance-execution-source-phpstan.log,96827 terminal0;php-l3/diffcheck PASS. Проверяются gross120/net100 без строк, после native60 остатокgross60/netNULL, стабильность/изменение hash и отказ draft. Профили восстановлены, активных своих процессов нет.
- Writer/API/интеграция отчётов/экран ещё не реализованы. Продолжить с execution_distribution правилами и сервисом, общим source cap по всем сметам, сохраняемыми версиями/snapshots; не учитывать новый ledger в native capacity при записи (иначе двойной вычет). Полная цель, перенос/права/275436/8k/docs/main/CI/deploy остаются незавершёнными.

## Запись сумм ручного выполнения
- EstimateFinanceExecutionDistribution + executionRules в Save/Preview и dispatch FinanceService operation=execution_distribution. Access financeedit/canViewExecution, transaction scoped estimate lock/revision/shared receipt actor+hash replay, scoped act FOR UPDATE, source helper/hash; allocation scope/contract/currency/targetnotexcluded/version, editContracts. Ledger берётся по акту по ВСЕМ сметам; остальные строки вычитаются из native capacity. Gross cap, known net распределяется FinanceDecimal.allocate по changed amounts+remaining с сохранением неизменённых строк. Unknown net остаётся NULL. StableUUID/upsert500/history500/revision/receipt; акт не изменяется. source_snapshot компактный (source ID/capacity/hash без полного массива строк акта на каждую запись), condition_snapshot исходных условий.
- Пока API распределяет СУММЫ; quantity новых записей NULL, существующее quantity сохраняется. Поддержка ввода/проверки объёмов, reader/summary/project/export/UI и accepted-fact protection для этого ledger обязательны далее. Нельзя считать факт объёма0 по NULL. Не выкатывать этот промежуточный блок отдельно.
- PG16 test_execution_distribution_saves_exact_net_replays_and_limits_native_remainder PASS1/13 finance-execution-write-tests.log,01:38.252/166MB,47160 terminal0: из акта120/net100 распределено60/net50, samecommandreplay не создаёт историю, актattrs неизменны;120.01 rejected без изменения,120 accepted net100/stablekey/history2. PHPStan4productionfiles PASS finance-execution-write-phpstan.log,58203 terminal0;php-l5/diffcheck PASS. Профили восстановлены; своих процессов нет.
- Дальнейшие gates: две сметы/общий остаток, права/чужой scope/source stale/409/атомарный batch/0-clear/net rounding, native line/work locks для concurrent source edits, защита условий после ручного факта, распределённые объёмы. Проверить поведение существующих записей при изменении/аннулировании исходного акта и неизменность их снимков. Полный план остаётся активным, push/deploy отсутствуют.

## Общий остаток и защита ручного факта
- ExecutionDistribution блокирует native lines, а если их нет — completedWorks/pivot перед чтением source. PG16 shared-act regression PASS1/6 finance-execution-shared-tests.log,01:22.920/166MB,73491 terminal0: 0.03 gross/0.02 net делятся между двумя сметами, превышение остатка отклонено; без contracts.edit запись отклонена; финальные gross/net точно совпадают с документом и history2. PHPStan writer PASS finance-execution-shared-phpstan.log,47791 terminal0.
- EstimateFinanceAcceptedVolume.assertRetained теперь сначала вызывает assertManualRetained для item/resource targets. Batch query ledger+conditions+acts защищает идентичность key/contract/item/resource/currency. Активный известный объём задаёт минимум SUM(quantity); при активном gross с неизвестным quantity запрещено уменьшать текущий план до уточнения. Аннулированный акт не ограничивает минимум объёма, но запись с историей всё ещё нельзя удалить/перепривязать (FK restrict сохраняет историю и guard даёт понятную ошибку вместо SQL). Нулевые ledger-записи также сохраняют идентичность истории.
- Guard first test PASS1/5 finance-execution-guard-tests.log,13319 terminal0, но после уточнения поведения аннулирования выполнен свежий final: PASS finance-execution-guard-final-tests.log,47988 terminal0. Проверены delete/currencychange/reduction unknown, known quantity10 vs9, annulment позволяетquantity0, physical deletion послеаннулирования всё ещё отклоняется. PHPStan final PASS finance-execution-guard-final-phpstan.log,24663 terminal0 (initial77579 тоже terminal0);php-l/diffcheck PASS. Профили восстановлены, своих процессов нет.
- Далее: ввод и суммирование manual quantity вместе с native accepted volumes, защита исторической цены в Remainder, reader/summary/project/Excel/UI. Дополнительные scope/409/независимая concurrency/0-clear/source correction проверки ещё нужны; текущий shared test последовательный, не доказывает конкурентность. Полный исходный план/реальные275436/8k/docs/main/CI/deploy остаются незавершёнными.

## Объёмы ручного выполнения
- executionRules optional nullable quantity строка до8знаков. Writer сохраняет quantity в preview/ledger/history; отсутствие поля сохраняет прежний объём, amount0 без явногоquantity снимает объём в0 с сохранением stableID/history. ЯвноеNULL означает неизвестный объём, а не0.
- Новый EstimateFinanceExecutionQuantity.assertAvailable: batch conditions выбранныхitemIDs+contract+estimate, native modern lines либо legacy pivot толькодляactбезmodern, active manualledger всех актов; текущиеизменяемые строки заменяются в сумме. Проверки по allocation и группамitem/resource againstplannedquantity; если имеются другие неуточнённые positiveamount/NULLquantity, положительный новый объём требует уточнения. Группы ресурсов отделены отработ; проверяютсятолькоchangedgroups. Gross/net cap остаётся прежним. Ошибка переведена.
- PG16 PASS2/18 finance-execution-quantity-tests.log,42307 terminal0: два акта с60.12345678+39.87654322=100 точно,40второгоотклонено,clearamount0→quantity0/stableID/history3; предыдущийamountonly/replay/net тест прошёл. PHPStan3productionfiles PASS finance-execution-quantity-phpstan.log,14230 terminal0;php-l4/diffcheckPASS. Профили восстановлены; своих процессов нет.
- Remaining quantity gates: native+manual mixed regression, resources/multiple allocations/unknown batch order, scoped corrupt snapshots (native allocation_key следует валидировать по basis_type и совпадению estimate_item_id, сейчас только keylookup), independentconcurrency. Далее reader/Remainder/project/Excel/UI для ручного выполнения и все прочие требования полного плана. Деплой не выполнялся.

## Совместный учёт native и ручных объёмов
- ExecutionQuantity при отнесении native строки к конкретной allocation проверяет basis_type=contract_conditions, совпадение item_id в строке/условиях/snapshot, estimate_id и contract_id snapshot. Недостоверная ссылка не приписывает объём конкретному resource/allocation; нативный объём остаётся в группе самой позиции акта. Исторический факт не удаляется.
- Новый mixed regression: approved акт содержит modern quantity60 и legacy pivotquantity60 на ту же позицию; ручное41 на другом акте отклонено,40 разрешено и сохранено. Это проверяет отсутствие повторного учёта modern+legacy и совместный capnative+manual. PG16 PASS1/3 finance-execution-native-quantity-tests.log,28354 terminal0; PHPStan PASS finance-execution-native-quantity-phpstan.log,17664 terminal0;php-l2/diffcheck PASS. Профили восстановлены, своих процессов нет.
- Reader ручного ledger всё ещё нужен: добавить act_distribution строки/документные остатки, строгие org/project/estimate/contract scopes, source freshness без N+1, snapshot helper общий с writer. Source.read должен явно получать только native capacity, иначе после интеграции ledger получится двойное вычитание при записи. Затем unknownquantity в summary/UI, Remainder/historyprice, projectdedup/Excel/UI. Недостоверныйsnapshot negative/resource/multiplealloc/unknownbatch и независимая concurrency ещё не покрыты отдельными тестами. Полная цель и deploy остаются незавершёнными.

## Ручное выполнение в серверном отчёте
- Новый ExecutionSnapshot.make общий для Source и Ledger; сортирует native lines/works, сохраняет прежний snapshot/capacity/hash формат. Execution.report optionalincludeDistributions(defaulttrue), Source.read явноfalse: writer продолжает получать только нативный остаток и не вычитает ручнойledger дважды.
- ExecutionLedger.merge: один batch query всех ledger по разрешённымактам с conditions/estimates/items, scope org/project/estimate/contract сверяется; чужие/повреждённые записи не выводятся, документ требует проверки. Hash пересчитывается по уже загруженным act/lines/works безN+1. Active распределения добавляются как source_type=act_distribution со stablekey/version/conditions/resource/сохранённымизначениями. Документный remaining уменьшается на ledger по всем сметам, estimate amount только текущейсметы. При source hash/currency/scope/capacity проблеме подтверждённые суммы иquantityNULL, saved* остаютсядлядопустимыхстрок, requires_reviewtrue. Annulled/draft исключаются существующимnative reader; history сохраняется. Нулевые снятые записи не становятсяфактом.
- ExecutionSummary использует r:resource_id дляручныхресурсов, иначеi:item_id; existing NULLquantityhandling даётunknownaccepted/remaining. Новые UI/types/Excel metadata/полнаяprojectсверка пока не сделаны. Не считать весь интерфейс готовым.
- Расширен существующий writer regression отчётом: rowmanual60/docremaining60/summaryrevenue60, unknownquantityNULL; полное120даётremaining0; изменённыйдокумент→review/confirmedNULL/saved120;annulment→rows[]/history2. Совместно с shared writer regression PG16PASS2/33 finance-execution-ledger-tests.log,01:26.926/172MB,91095terminal0. PHPStan5productionfilesPASS finance-execution-ledger-phpstan.log,81151terminal0;php-l6/diffcheckPASS. Профиливосстановлены, своихпроцессовнет.
- Далее: ledger reader project/sharedscope/export/negativeorg/querycount/resource tests, понятные названияресурсов, UI/types/dialog, Remainder historicalprice. Guard/nativesnapshotnegative/unknownbatch/concurrency и остальныепунктыплана/275436/8k/docs/main/CI/deploy остаютсянезавершёнными.

## Ручное выполнение: проект и Excel
- 79d52257d: ProjectExecution сохраняет NULL, если подтверждённая сумма хотя бы одной сметы неизвестна. Общий акт и ручные распределения двух смет учитываются один раз.
- Excel строки актов включают идентификатор/версию распределения, сохранённые объём/net/gross и статус проверки. Неизвестный подтверждённый факт остаётся пустой ячейкой; сохранённая сумма доступна отдельно.
- PG16 final PASS1/22 finance-execution-project-export-final-tests.log (01:22.400,170MB,19737terminal0). PHPStan PASS finance-execution-project-export-phpstan.log; diffcheck PASS; три тестовых профиля восстановлены. Initial test ожидал NULL вместо пустой Excel-ячейки, исправлен только assertion.
- Следом UI/types ручного ledger, затем ввод распределения и Remainder historicalprice. Общая цель, перенос/реальная регрессия275436/полные проверки/документация/main/CI/deploy не завершены.

## Интерфейс ручного выполнения
- Типы FinanceExecutionRow расширены act_distribution/resource_id/stablekey/version/requires_review/saved*. Суммы документа и project estimate_amounts допускаютNULL согласно серверу.
- ExecutionPanel раскрывает источник ресурса по r:resource_id, название берёт из соответствующей строки. Ручное распределение отмечено с версией; требуетпроверки показывает сохранённый объём/сумму отдельно от подтверждённого прочерка. Native rows сохраняют прежнее отображение.
- Vitest4/4 PASS finance-execution-ledger-ui-tests.log (93502terminal0), tsc PASS finance-execution-ledger-types.log (66232terminal0), ESLint3files/diffcheckPASS. Регрессия проверяет раскрытие ресурса, NULLconfirmed, сохранённую100 и предупреждение; существующие access/history/8001UI tests такжеPASS.
- CBM admin generation2026-09-12T14:22:19Z: ExecutionPanel и CashDistributionDialog nottracked; estimateFinance.ts/estimateFinanceService.ts metadata_changed, выполнено ограниченное чтение. CashDistributionDialog прочитан1–210 как существующий образец UI для следующего execution distribution dialog; сервис методы previewCash/saveCash найдены60–67 (полное чтение методов ещё нужно). Дальше добавить типы/методы previewExecution/saveExecution + диалог amount/nullablequantity/candidates/paging/preview/version/retry и подключить приfinancialedit+contractsedit+executionaccess. Backend writer уже существует. Remainder/manual historicalprice, остальные проверки и полная цель остаются открытыми; деплоя нет.

## Форма распределения выполнения и версии снятых записей
- Backend6108d2dd8 ExecutionLedger возвращает distributions текущейсметы после scope guard до пропуска нулевых строк: act/allocation/stablekey/version/amount/nullablequantity. Нулевые снятые записи не становятся фактом, но дают правильнуюверсию дляповторногосохранения. Дополнительныхзапросовнет. PG16 PASS1/11 finance-execution-cleared-report-tests.log (77427terminal0,01:19.443/166MB): clear→metadata version2→reallocate sameID version3. PHPStanPASS finance-execution-cleared-report-phpstan.log68721terminal0,php-l2/diffcheckPASS;profilesrestored.
- Admin новый ExecutionDistributionDialog и типыestimateFinanceExecution. previewExecution/saveExecution используют существующиеfinancepreview/save. Кандидатытолькоnonexcludedmoderncontractconditions данногоactcontract/currency; amount/nullablequantity, существующиеверсии(includingcleared),50rows/searchpersistentchanges, previewglobalremaininggross/net, serverhash/conditionsversion/revision/UUID, inFlightref. Посленеопределённогоsaveошибки поляfrozen и повторяетсяточнотажеоперация;409/403blocked+reload,422разрешаетисправление/новыйUUID+новыйpreview. Unknownquantityпусто→NULL; clear requiresamount0/quantity0explicit.
- Workspace передаётeditтолькоприreport.can_edit+contracts.edit; executionavailablerequiredfordialog. Panelнепредлагаетдействиебезmetadataилиприneeds_review. No deniedsectionrequests.
- Tests initial20/20PASS (Dialog4+Panel4+Workspace12) finance-execution-distribution-ui-tests.log14063terminal0. После422recovery+permissiontest finalDialog5+Panel5PASS finance-execution-distribution-final-ui-tests.log83347terminal0. MSWреальныеservicecalls проверяютsourcehash/nullablequantity/clearedversion/replay500/403/409/422/8001paging. WorkspaceнеизменялсяпослеinitialPASS.
- tscinitial67128terminal1:duplicate amount/quantity fields spread. Исправлен fallbackcurrent[key]??defaults; finaltsc72177terminal0 finance-execution-distribution-final-types.log. ESLintinitial7filesPASS иfinal3changedfilesPASS;diffcheckPASS. Новыелогиdiagnosticonlyнеcommitted.
- Остатокmanualexecution: Remainder/publicAcceptedVolume историческиецены и объёмы, документныепереходы/история/корректировки, resource/scope/querycount/sourcefreshness/independentconcurrencynegativecases. Новыйдиалогблокируетneeds_reviewдокумент;процессуточненияизменённогоисточникаещёнужен. Fullgoal migrationallorg/export/actual275436/owncostcorrections/cashsections/8kresources/docs/latestmainCIdeployclient остаётсяоткрытой; productionнеизменён.

## Историческая цена ручного выполнения
- Backend cb29917ed: Remainder.acceptedFacts теперь использует общий Execution.report(Query.contracts) для native+manual подтверждённого факта; отдельное суммирование performance_act_lines убрано. Scoped allocation map проверяет contract/item/resource/currency для каждойссылки. NULLquantity/gross/net остаютсяNULL, apply запрещаетпересчётдо уточнения; известныеhistoricalamounts добавляютсякценеостатка. Snapshot/sourcefreshnessmanual наследуютсяLedger. Query+Execution dependencycycle нет, PHPStanPASS.
- В началеacceptedFactsфильтруютсяUUID черезStr::isUuid; legacy:N displaykeys не отправляются вUUIDSQL. Emptykeys/emptyallocations→[] безлишнегочтенияфакта. Это исправило large-readregression initial23024terminal2: invalidUUIDlegacy:1. Native/manualhistory testsinitialпрошли; finalвсенужныеповторены.
- Новыйmanualremainder test: approvedact120000gross/100000net,manualqty20,planqty100,новаяunitprice12000→1080000gross/900000net, historical120000/remaining960000;двойноесохранениенеизменяетакт/ledger. УточнениеquantityNULL→reportNULL/повторныйпересчётошибкабезизмененияплана.
- PG16finalPASS3/58 finance-manual-remainder-final-tests.log (58682terminal0,02:12.035/426MB): newmanual+existingnativehistoricalprice/accessredaction+large8001legacyreadquerycount. PHPStanfinalPASS finance-manual-remainder-final-phpstan.log43920terminal0;php-l2/diffcheckPASS;profilesrestored. Modern8001allallocationsсфактомload/concurrency/sourcecorruption отдельнопоканеисследованы.
- Admin744fa4eb FinanceAmountBasis.quantitynullable; FinanceAcceptedSummaryпоказывает«объёмнеопределён» вместоNumber(null)=0, warning приunknownqty/net/gross, требованиеуточнить передизменением. Vitest2PASS finance-manual-remainder-ui-tests.log75930terminal0; tscfinal13286terminal0 finance-manual-remainder-final-types.log (initial45979тожеPASS,перезапускпотомучтоUIизменёнпослезапуска);ESLint3PASS/diffcheckPASS.
- CBMbackendRemainderclasssnippetактуальный87lines,coveragemetadatachanged;traceclass0неполныйPHPgraph→boundedliteralcallsService148(report)563(apply). Execution/Query/Servicemetadatachanged→boundedsource. AdminFinanceAcceptedSummarynottracked→source23lines.
- Дальше: AcceptedVolume.publicquantities/contractprogress/nativeworkflowmanualquantitycoherence; manualsourceclarification/history/documentlinks, native forgedsnapshot/unknownbatch/resources/scopes/independentconcurrency tests. Wholegoal remains migrationallorg/export/actual275436/owncostcorrections/cashsections/8kresources/docs/mainCIdeployclient. Никакихproductionchanges/PR/push/deploy, goalнеcomplete.

## Совокупный принятый объём
- AcceptedVolume.quantities теперь добавляетmanualledger кmodernnative+legacyno-modern, groupedcontract:item,толькобезresource_id. Дополнительныйbatchqueryjoinacts/conditions/fact scopedorg/project/estimate/contract; approved/signednonannulledonly. Positivegross/NULLquantity→NULLaggregate; assertRetainedrejectsNULL иqtyнижеобщегопринятогообъёма. Resourceguardостаетсяотдельным.
- Новыйregressionnative20+manual30→50; candidate40отклонён,candidate50допущен;manualquantityNULL→unknown;annulmanual→20;foreignorg→[]. Совместносexistingmodern+legacydedup test PG16PASS2/7 finance-combined-volume-tests.log92527terminal0,01:25.711/170MB. PHPStanPASS finance-combined-volume-phpstan.log20081terminal0;php-l2/diffcheckPASS;profilesrestored.
- CBM AcceptedVolumeclassavailablebuttrace0, metadatachanged→boundedsource/literalcalls. App searchshowsAcceptedVolumeusedbyFinanceServiceonly. OperationalcontractprogressusesseparateContractEstimateOperationalProgress.prepare; current80linesread: actualCompletedWorkaggregate, PerformanceActLine+acteager, floatacting quantities; **manualledgerиlegacycompletedWorksещёнедобавлены**. Existingregressiontest_contract_progress_batches_queries_for_multiple_items verifies30items/query<=5/serializer0,actual20,approved5,reserved3,available12. Needintegrateallactquantitiesandnullablewithoutleakingdenieddata.
- ActingQuantityReservationService first115linesread: availableQuantities locksworkIDscontexttransaction, reservesmoderncompleted_worklines+legacypivot(no matchingcanonicalcompletedworkline) via scaledints. **ManualfinanceledgerhasnoCompletedWorkID: нельзяугадатьраспределениепоработам. Нужнаотдельнаяпроверкаобщегопринятогообъёмапозицииприпоследующихактах**; sourcehashinvariantsforActingWizard/ActReportWorkflow остаются. Earliercommentarywasintendednextscope, thisblockonlyfinancialretainedguardcomplete.
- NextOperationalProgress/actcreationpositioncap, sharedcalculation/resourceunknownquerycount/concurrency; allpreviousgoalremaining/migrationactual275436/owncostcorrections/cashsections/8k/docs/mainCIdeploystillopen. Productionunchanged.

## Выполнение в карточке договора
- Backend d3ee1ba42 ContractEstimateOperationalProgress.prepare получаетпринятуючастьчерезобщийExecution.report, groupeduniqueestimates, scopesorg/project/linkcontract, resourcesнеприбавляютсякродительскимработам. Estimateзагружаютсяbatch, ихчислодолжносоответствоватьlinkselseAuthorizationException. Query.contractsвыполненодинраздляproject, выбрантолькотекущийcontract. Unknownmanual/sourcechanged quantity→NULL→resourceavailableNULL. Нативныестрокиapproved/signed иlegacy+manual берутсяизобщегоreaderбездвойногоучёта.
- ИсходныйPerformanceActLine запросоставлендлярезервачерновиков; released/approvedисключены. РезервсуммируетсяFinanceDecimal, выводсовместимоroundfloat4. ActualCompletedWorkбезизменений. НетcanViewActs→нетзапросовкexecution/актам.
- Extendedmixedregression native20+manual30: serializeracted50/available50приactual100; unknownmanual→actedNULL/availableNULL; deniedacts→NULLиquerylogбезperformance_act/estimate_finance_execution. PG16PASS2/25 finance-contract-manual-progress-tests.log69401terminal0,01:26.681/172MB. Existing30itemsregressionquerybudget15(было5,добавленобщийreaderиscopes),serializerпо-прежнему0queries; типы5approved/3reserved/12availableсохранены. PHPStanPASS finance-contract-manual-progress-phpstan.log48026terminal0;php-l2/diffcheckPASS;profilesrestored.
- Admin ContractEstimateItemsTreeformatProgressприhasAccesstrue/NULL→«Неопределено», иначе«Недоступно». Встроках/сводкедоступразличается; warningпринятогоunknown/остатокнерассчитан. Vitest4PASS finance-contract-progress-ui-tests.log55418terminal0;tsc37841terminal0 finance-contract-progress-types.log;ESLint0errors,1существующеепредупреждениеuseEffectmissingloadline67(этотучастокнеизменялся);diffcheckPASS.
- ContractEstimateItemResourceужеnull-safe дляavailable/actedprogress/create_act, измененийнепотребовалось. CBMresource иtree metadatachanged, tree sumProgressgraphnotfound→boundedsource. No claimsallconsumerscomplete: **nativeactcreationpositioncapещёнеподключён**, existingActingQuantityReservationServiceonlyCompletedWorkIDs иmanualledgerнеимеетworkID. Нельзяраспределятьmanualфактпоработампредположением;нуженобщийконтрольпозицииприактах. ГруппировкаExecution.reportпосметамнеN+1позициям,нонагрузкумножествосмет/8001modernфактпоканеисследовали.
- Fullgoalremainingasbefore: nativeworkflowguard/concurrency/resources/clarification/history, migrationallorg/export/actual275436, owncostcorrections/cashsections/8k/docs/latestmainCIdeployclient. Productionunchanged,goalactive.

## Контроль объёма при обычных актах
- Backend f19c1f1c7: новый EstimateFinanceActQuantityGuard.assertFits(act,contract), требуетDBtransaction. Собираетсовокупныйобъёмcurrentact поitem:modernlines,либоlegacycompletedWorksеслиlinesнет; свободныеmanualбезestimateitemпропускает. Scopedmodernallocationпланgroupestimate/item/contract/org/project; resourcesотдельныенеучитываютсякакродительскаяработа. Получаетnative+manualпринятыеобъёмычерезAcceptedVolume.quantitiesиотклоняетunknownилипревышение. Безmodernусловийоставляетlegacyworkflowсовместимым. Мультиобъектныеактынеотклоняютсяпростоиз-занесовпаденияcontract.project_id: guardscopeпоact.project_id.
- AcceptedVolume.quantities новыйoptionalexcludedNativeActId исключаетcurrentactТОЛЬКОизnative/legacy,manualcurrentactнеисключается. Это предотвращаетсамоповторприdirectapprovedcreate и не теряет ручноераспределениетогожеакта.
- ПодключёнWizardпослесозданиястрок/пересчёта;Workflow.submitдоpending;Workflow.approveпослеrepriceдоapproved;ContractPerformanceActServicecreateпослесинхронизацииработдоacceptanceevent/updateпередзавершениемTX. СтарыйworkIDreservationнеудалён. Nativeguardпозиционный, не угадываетCompletedWorkIDдляmanualфинфакта.
- **Конкурентностьещёнеобеспеченаобщейблокировкой**: guardвTXнонетединойserializationсmanualwriter/conditionedits. Nativeentrypoints имеютразныйlockorder(act/contract/work),manualestimate/act/alloc; нужновыбратьобщийпорядок/lockиотдельнопроверитьпроцессами. Такжеperallocationнативныхsnapshot/ресурсы/стараясвязьнеподтверждённые/sourcefreshnessqty/multiproject/largemultismets—открытыеаудиты. Необъявлятьполнуюготовность.
- Sourcearchitectureguard AcceptedProductionCandidateContract.SOURCE_HASH обновлён на eb7f71af8bff629846e7c92ebb3fc46e10a15a5d92f0dc2d931a9428a6d0ea01; hashlistдополненновымActQuantityGuard иAcceptedVolume. Самапроверкасохранена. Hash=.NETIncrementalSHA256сырыхфайловспискавпорядке;namespaceimportsresolvedApp→app; .gitattributes*eol=lf. **ЛюбоеизменениеэтихдвухклассовтеперьтакжетребуетпересчитатьSOURCE_HASH**, какWizard/Workflow/ContractPerformanceActService. PHPStaninitialbootstrapdriftисправленэтимобновлением; final6productionfilesPASS finance-native-act-cap-final-phpstan.log68904terminal0.
- Mainregressionmixednative20+manual30,nextdraft60submit→ValidationException/statusdraft,точныйостаток50допускается,unknownmanual→nextguardblocked. PG16PASS2/21 finance-native-act-cap-retest.log01:25.248/172MB (pipeline70097firstpart). InitialfixtureNEXT-COMBINEDимелdefaultis_approvedtrue; добавленfalse, productionimmutabilityневскрывалась. Earlier34675bootstrapdrift/39629fixtureerror terminal; нетживыхпроцессов.
- FinancialBasiscompatibilitytestsобнаружилиустаревшиеfixturesactorlessattachAPI. Ихцель—immutablelegacyestimateversions: setupтеперьявносоздаётfinance_managed=falseContractEstimateItemбезсменыожидаемыхцен/НДС; in-memoryprecisionfixtureполучилorg/project/currencyиexplicitno-financeAllocations. Final7/122PASS finance-native-act-basis-final-tests.log81725terminal0,01:38.833/174MB. Initial7test5errorsнефинальнорезультат. php-lallchanged/diffcheckPASS;3profilesrestored.
- LauncherнепринимаетdirectoryTestPath; FeatureSuiteсFilterвсёравнозагружаетPestfilesиошибкаInvalidPestCommand. ИспользоватьотдельныеTestPathфайлыпоследовательночерезканоническийlauncher(никакойзаменыdriver/profiles). No needrerunsuccessfultestsбезизменений.
- CBMmetadatachangedforWizard/Workflow/ContractService/Candidate/PerformanceActLine/financialbasistest→boundedsource;ActQuantityGuardnottracked. No productionchanges/noPR/push/deploy. Fullgoalremainingasbeforeincludingmigrationactual275436/owncostcorrections/cashsections/resources8k/history/docs/mainCIclient, goalactive.

## Сериализация ручного и обычного выполнения — 2026-09-13

- Commit e8c6400b8. EstimateFinanceExecutionDistribution теперь блокирует scoped договор перед актом и чтением объёмов. Смета остаётся первой блокировкой. ActReportWorkflowService submit/approve блокируют договор до акта; договор определяется подзапросом по реальному act ID, повторное чтение акта ограничено этим contract ID. ContractPerformanceActService create/update также блокируют договор до акта/работ; update повторно ограничен contract ID и organization ID.
- Существующий EstimateFinanceService normalize уже блокирует договоры по ID; Wizard уже имеет contract-first. Новая правка закрывает проверенный сценарий manual writer против native acceptance, но не является доказательством общей конкурентности всех входов. Ещё проверить annul/sign/reject/reprice, legacy deletion, adapters/migration/ресурсы, платежи и собственные затраты, lock order callbacks и обе стороны гонки. Нельзя объявлять все операции конкурентно безопасными.
- PG16 regression: finance-contract-serialization-tests.log — 3 tests / 68 assertions, 01:32.908, 178 MB; session 42270 terminal 0. PHPStan 4 production files PASS finance-contract-serialization-phpstan.log, session 48566 terminal 0. php-l всех изменённых production/test PHP и diff --check PASS.
- Новый независимый process test test_manual_execution_waits_for_native_contract_volume_and_rechecks_after_commit: план 100, родитель держит contract lock, worker пытается распределить 60; pg_blocking_pids подтверждает реальное ожидание, родитель принимает native 50 и commit, worker получает 422, ledger не создан. Проверяет работу общего mutex и повторное чтение объёма. Принятие в родителе вызывает guard + model status update внутри TX, это не полный HTTP/Workflow approve тест.
- Worker EstimateFinanceSaveWorker печатает READY + backend PID, ловит ожидаемую ValidationException как 422. Существующие ограничения testing + pgsql + random most_phpunit DB сохранены. Прежний concurrent save/revision test совместим.
- Final concurrency PG16: finance-contract-serialization-parallel-clean-tests.log — 2 tests / 11 assertions, 01:30.265 / 172 MB; session 6575 terminal 0. Initial single test 1/7 PASS (14431); промежуточный 25762 exit2 был ошибкой cleanup test: history не имеет estimate_id. Исправлено scoped subquery execution_allocation_id; final cleanup удаляет только записи тестовой сметы/актов. Все процессы закончены, profiles restored 45433. Не повторять successful checks без новых изменений.
- AcceptedProductionCandidateContract.SOURCE_HASH теперь 62a8dbca3e182aa1e2082e2f9587900039bb3688ec7a3fe6655f2e7c79a778fc. Hash guard не отключён.
- CBM worktree project most-unified-finance-backend generation 2026-09-12T20:04:13Z. Service snippet получен; metadata_changed/not_tracked для затронутых файлов позволили bounded source reads. Новый reindex не запускался.
- Полная цель остаётся активной: требуется per-allocation native/resource correctness, история/уточнение исходников/ссылки, собственные затраты и денежные разделы, миграция всех организаций и actual 275/436, 8k полный сценарий, документация, latest main integration/CI/штатный деплой/клиент. Production не менялся, push/PR/deploy не было.
