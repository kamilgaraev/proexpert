# Реализация единой привязки смет: журнал работы

## Основание

Полный объём и приёмка: [согласованный план](unified-estimate-contract-finance.md).
Ни один этап пока не завершён. Деплой не выполнялся.

## Состояние на 12 сентября 2026

- Backend: ветка `task/unified-finance-20260912`, исходный main `a7c85b3a1`.
- Админка: одноимённая ветка, исходный main `6d90f0d1`.
- Обе рабочие копии изолированы от незавершённых задач в основных каталогах.
- CBM перечислил проекты, но содержательный поиск завершился `Transport closed`; исследование продолжено точечным чтением исходников нового checkout.

## Подтверждённые точки изменения

- `EstimateFinanceService::save` удалял и заново создавал строки распределений при каждом редактировании. Изменено на обновление существующей строки по стабильному ключу с сохранением её ID. Удаление отсутствующих строк пока остаётся старым; защита утверждённого факта ещё предстоит.
- `EstimateFinanceService::projectLinks` читал связи и распределения для каждой позиции отдельно. Чтение сгруппировано в два пакетных запроса; записи пока остаются построчными. Это ещё не доказательство выполнения требования нагрузки на 8000 строк.
- Добавлен регрессионный тест сохранения ID распределения и совместимой связи при изменении цены и повторном запросе.
- `ContractEstimateService::attachItems`, `detachItems`, `syncItems`, `updateCoverageVat` ещё используют самостоятельный путь изменения связей. Их предстоит сделать адаптерами единого сервиса с явным actor и проверками доступа.
- `EstimateFinanceAccess::editContracts` теперь проверяет `contracts.edit` и область организации/проекта для входящих и существующих затронутых связей. В том числе проверяется пустая замена, удаляющая договорные условия. Проверка вызывается в нормализации сохранения и обоих вариантах предпросмотра. Самостоятельные затраты без связей с договорами не требуют `contracts.edit`.
- `SaveEstimateFinanceRequest` ограничивает выбор 1000 целями и не различает освобождение от НДС и ставку 0%. Массовый выбор и явный налоговый режим ещё предстоят.
- История снимков условий реализуется в четвёртом блоке ниже. Редактирование остатка, факт актов/оплат, ручные затраты, перенос, новый единый интерфейс и единые сводки ещё не реализованы в этой задаче.

## Проверки первого блока

- `php -l` для изменённого сервиса и теста: успешно.
- PHPStan для `EstimateFinanceService.php`: успешно.
- `git diff --check`: успешно.
- Целевой `EstimateFinanceTest` через штатный PostgreSQL launcher: успешно, 22 теста, 98 проверок, 4 минуты 17 секунд.
- Для тестового запуска порт в трёх файлах временно согласован с правилами workspace: 55433 вместо старого 45433. Временные изменения возвращены после проверки; CI/CD не меняется.

## Следующая работа

1. Первый блок проверен; документы в `docs/specs` игнорируются правилами репозитория, при коммите включить только два документа этой задачи явно.
2. Составить карту потребителей сумм сметы/договора/проекта/Excel и старых команд привязки; привести к одному расчёту с регрессией из плана.
3. Расширить существующий финансовый контракт: режим НДС, стабильные версии и защита факта, права на договоры, массовый выбор и полноценный предпросмотр.
4. Реализовать единый пользовательский процесс, план/выполнение/деньги и миграцию согласно полному плану, затем штатный релиз и проверку клиента.

## Карта старых команд (проверенные исходники)

- `ContractEstimateItemController`: добавление позиций, удаление, изменение НДС через `ContractEstimateService`.
- `EstimateCoverageService::attachFullCoverage/syncCoverageItems`: синхронизация через `ContractEstimateService`, затем синхронизация журнала через `CompletedWorkFactService`.
- `EstimateCoverageService::detachCoverage`: прямое удаление `ContractEstimateItem`, обход сервиса привязки. Обязательно перевести на общий путь и защитить факт.
- `ContractFromEstimateService::create`: создание договора и привязка в одной транзакции, actor уже передаётся в наружный метод, но не в `attachItems`.
- `JournalContractCoverageService::ensureCoverage`: автоматическая привязка к договору журнала, actor отсутствует в сигнатуре. Потребует явного контекста и проверки финансовых прав, при сохранении совместимости API.
- `EstimateCoverageService::getCoverageForEstimate/getContractCoverageSummary` и `ContractEstimateService::getSummary/calculateContractEstimateTotal` самостоятельно суммируют совместимые связи. Это потребители будущего единого расчёта.
- `EstimateFinanceQuery::allocations` объединяет управляемые распределения и старые связи, причём старые условия возвращает как неизвестные. Перенос должен сохранять эту осторожность и не придумывать налоговый режим.

## Второй блок: права

- Добавлены четыре теста: запрет создания без права договора, запрет удаления пустой заменой, запрет предпросмотра сметных цен без права договора, разрешение собственных затрат без договора.
- PHPStan обоих изменённых сервисов и php -l сервисов/теста прошли.
- Общий запуск: 25 из 26 тестов прошли, один новый тест ошибочно ожидал начальную версию 0 вместо сохранённой версии после создания позиции. Исправлено сравнение с исходной версией; повторный целевой запуск прошёл (1 тест, 2 проверки). Исполняемый код после общего запуска не менялся.
- Временная настройка тестового порта возвращена; diff не содержит изменений тестовой инфраструктуры.

## Третий блок: режим НДС

- Добавлены nullable `vat_mode` и миграция; исходные данные миграция не пересчитывает.
- `EstimateFinanceTax` рассчитывает сумму без налога и с налогом через существующую десятичную арифметику, проверяет соответствие режима, базы цены и ставки. Режим `none` хранится без ставки; налогообложение по ставке 0% сохраняет явную нулевую ставку и отличается от освобождения.
- Старые API без `vat_mode` поддерживаются по исходным полям цены/ставки, без восстановления ставки из сумм. Старые связи с неизвестными условиями остаются неизвестными.
- Калькулятор больше не считает явно выбранное освобождение от НДС отсутствующей ставкой. Предусмотрены тесты независимых ставок сторон, повторного сохранения, освобождения и противоречивых условий.
- Админка: типы, преобразование ответа в форму и существующая форма условий передают явный режим. Выбор освобождения очищает ставку; при возврате к налогообложению ставка не подставляется автоматически. Полный новый процесс привязки ещё предстоит.
- После прерывания процессов результаты налогового блока не сохранились; процессы были проверены как отсутствующие и проверки перезапущены с файловыми логами.
- PHPStan финансового каталога и запроса прошёл; синтаксис нового расчёта и миграции прошёл.
- Backend: 29 тестов, 122 проверки прошли (6 минут 16 секунд, длительность связана с созданием полной тестовой схемы). Временное изменение порта возвращено.
- Админка: Vitest 7 тестов, ESLint и `tsc --noEmit` прошли. Backend-блок сохранён коммитом `4f6b8d380`; изменения админки также сохранены в task-ветке. Деплоя нет.

## Четвёртый блок: история условий

- Добавлены `condition_version` и отдельная таблица снимков до/после создания, изменения, удаления. История не зависит от существования текущей строки распределения; содержит автора, идентификатор операции и общую версию расчёта.
- `EstimateFinanceHistory` записывает историю пакетно внутри транзакции сохранения. Повтор одного mutation ID возвращает прежний результат без повторных версий. После удаления ключ нельзя повторно использовать для новой привязки.
- Необязательная `condition_version` проверяется наряду с общей revision. Отсутствие нового поля сохраняет совместимость старых клиентов; новая админка передаёт версию из ответа.
- Добавлен защищённый финансовыми правами GET `projects/{project}/estimates/{estimate}/finance/history`, постраничная выдача по 100 записей с `after_id`/`next_cursor`. Интерфейс просмотра истории ещё предстоит.
- Проверки: сохранность снимков после удаления, идемпотентность, устаревшая версия при свежей общей revision, запрет чтения без финансового права. PHPStan и синтаксис прошли; DB-набор прошёл: 32 теста, 135 проверок. Временный порт возвращён. Админка: ESLint и TypeScript прошли. Backend-коммит `6a7ee9359`, изменения админки сохранены в task-ветке.
- Попытка fast-индексации новой backend-копии в официальном CBM завершилась `Pipeline failed`. Граф основной копии не использовался как актуальные исходники этой ветки; далее применено точечное чтение.
- Снимки актов пока не связаны с версиями и защита принятого объёма ещё не реализована. Этот блок не закрывает требования плана по неизменности факта.

## Пятый блок: принятый объём (в работе)

- `EstimateFinanceAcceptedVolume` читает утверждённые/подписанные акты в области организации, проекта и затронутых договоров, группирует принятые объёмы по договору и позиции. Аннулированные акты исключены, включая случай сохранившегося флага `is_approved`.
- Источник объёма — строки акта; только для актов без строк используется старая таблица связи с выполненными работами. Это соответствует приоритету источников в `ContractPerformanceAct::recalculateAmount` и не складывает оба представления одного акта.
- Нормализация save/preview отклоняет удаление связи и объём ниже принятого. Документы и их суммы не изменяются.
- Добавлены тесты удаления/уменьшения подписанного объёма, освобождения после аннулирования и отсутствия ограничения от черновика. Общий запуск: 33 из 34 прошли; тест черновика выявил настоящий дефект: старый `is_approved=true` может сосуществовать со статусом draft. Расчёт исправлен на явные статусы approved/signed, флаг не превращает черновик в факт. Повторный запуск трёх относящихся к актам тестов выполняется. PHPStan и синтаксис ранее прошли, PHPStan после исправления перезапущен.
- Обнаруженная зависимость для следующей части: `PerformanceActFinancialBasisService::forCompletedWork` пока берёт цены/НДС из версии сметы, поэтому его нужно перевести на версии договорных условий с совместимостью старого факта.
- `ActReportWorkflowService::approve` блокирует акт, пересчитывает строки и затем блокирует договор. Требуется согласовать порядок блокировок и проверку актуального остатка с изменением привязки. Текущая проверка объёма сама по себе ещё не закрывает конкурентное утверждение.
- Не завершены в этом блоке: расчёт новых цен только на остаток, фиксация версии в акте, отдельная проверка старого представления актов и конкурентный сценарий. Не считать требование неизменности факта полностью выполненным.
- Повторная проверка завершена: `finance-accepted-retest.log`, 3 теста / 10 проверок успешно; PHPStan после исправления успешно.
- Дополнительный тест проверяет два представления одного акта и отдельный старый акт: принято 30, а не 45; чужая организация не получает эти объёмы. Штатный PostgreSQL launcher: `finance-accepted-legacy-tests.log`, 1 тест / 2 проверки успешно. Временный порт возвращён после проверки.
- Индексация task-worktree теперь завершилась успешно: `most-unified-finance-backend`, generation `2026-09-12T13:44:58Z`. Граф обнаруживает классы PHP, но не метод `forCompletedWork`; актуальный фрагмент класса прочитан через `get_code_snippet`. Метаданные покрытия помечены `metadata_changed`, исчерпывающие связи графа не предполагаются.
- Следующий блок: мастер `ActingActWizardService` и `ActingAvailabilityService` получают цену через `PerformanceActFinancialBasisService`. Утверждение `ActReportWorkflowService::recalculatePricedLines` пересчитывает сохранённую основу через `financialTotals->recalculateFromStoredBasis`, поэтому нужны снимок версии договорных условий и проверка её актуальности перед утверждением, а не только подстановка текущей цены в мастере.

## Шестой блок: договорная основа акта (в работе)

- `PerformanceActContractBasisService` читает условия в области организации, проекта, договора и позиции. Цена и НДС берутся из финансовой привязки; неизвестные условия не заменяются молча ценой сметы. Снимок содержит ключ, номер версии и сохранённые условия. Старые позиции без финансовой привязки пока сохраняют прежний путь по версии сметы.
- `PerformanceActFinancialBasisService` использует договорную основу при её наличии. При нескольких привязках требуется явный ключ; новый необязательный `selected_works.*.allocation_key` передаётся мастером в сервис. Полный выбор нескольких условий в интерфейсе и разбивка одного выполнения на несколько условий ещё не реализованы.
- Первый новый тест (8 проверок) прошёл: договорная ставка 5%, новая цена в следующей версии, предыдущий снимок не изменён. Остальные два не стартовали из-за контроля исходников отчёта: изменение мастера требует обновить `AcceptedProductionCandidateContract::SOURCE_HASH`. Контрольная сумма вычислена по указанному в самом контракте порядку файлов и обновлена; проверка целостности не отключалась.
- Повторные DB-тесты: session 41619, `finance-act-basis-retest.log`; PHPStan: session 30412, `finance-act-basis-phpstan.log`. Сначала проверить эти процессы. Порт в трёх тестовых файлах пока 55433, вернуть после завершения launcher.
- До готовности блока обязательны: проверка версии при утверждении и общий порядок блокировок; привязка принятого факта к конкретному ключу; цены только для неисполненного остатка; пакетное чтение условий; единицы в снимке; точное округление сумм; регрессии мастера актов. Частичная реализация пока не готова к выпуску.
- Повторный запуск основы акта завершён: 3 теста / 11 проверок успешно, PHPStan успешно. После этого добавлен `PerformanceActConditionGuard`, вызываемый при утверждении после блокировки договора и до пересчёта. Он отклоняет исчезнувшую/устаревшую версию и старую сметную основу, если позиция уже имеет договорные условия. Сохранение финансов теперь блокирует также существующие договоры, в том числе при пустом удалении.
- Добавлен тест настоящего `ActReportWorkflowService::approve`: изменение и удаление условий должны возвращать 409 без изменения акта/строки. Текущие процессы: DB session 32725 (`finance-act-guard-tests.log`), PHPStan session 85908 (`finance-act-guard-phpstan.log`). Предыдущие 41619/30412 завершены. Контрольная сумма отчёта пересчитана после изменения workflow. Изменения шестого блока ещё не закоммичены, временный порт вернуть после проверки.
- Session 32725 завершилась ошибкой инфраструктуры до проверок (4 теста, 0 assertions): PostgreSQL закрыл соединение, затем исчезла индивидуальная тестовая БД. Подтверждён новый контейнер `a8612128582b` и работающий PHPUnit другой задачи `pir-platform-backend` (PID 17196 на момент наблюдения). Не запускать наш launcher, пока чужой PHPUnit жив: он пересоздаёт общий контейнер. Session 85908 / PHPStan успешно. После этого добавлен дополнительный scope estimate/project в guard; повторная проверка всего пакета остаётся обязательной.
- Чужой PHPUnit завершился (проверены все php.exe с phpunit в командной строке). Повторный запуск нашего пакета: session 77343, `finance-act-guard-retest.log`. PHPStan пакетной загрузки: session 78610 завершена успешно, `finance-act-batch-phpstan.log`.
- Добавлена связь `EstimateItem::financeAllocations` и eager load для мастера и доступных работ. Resolver использует загруженные данные с повторной фильтрацией организации/договора/позиции; одиночные обращения сохраняют запрос. Дополнительно найдено: `Contract::getProjectIds` для multi-project каждый раз выполняет pluck, поэтому для полного отсутствия N+1 нужно использовать предварительно загруженные projects в resolver. Это ещё не исправлено. Общий тест 8000 строк не выполнен.
- Session 77343 завершена успешно: 4 теста / 17 проверок. Ошибки устаревшей/удалённой версии проверены через настоящий workflow, сохранность акта и строки подтверждена. PHPStan и diff-check прошли. Порт возвращён; пакет сохраняется в ветке без деплоя.
- Следующее выявленное ограничение: общий guard принятого объёма по договору/позиции допускает перенос этого объёма между двумя ключами одного договора. Теперь, когда акты сохраняют allocation_key, нужна дополнительная защита именно этого ключа перед сегментацией цены на принятую часть и остаток.
- Пакет шестого блока сохранён коммитом `b6556f8c7`. Полный процесс остатка остаётся незавершённым.

## Седьмой блок: факт конкретной привязки (в работе)

- Дополнен `EstimateFinanceAcceptedVolume`: принятые строки с `basis_type=contract_conditions` агрегируются по ключу привязки, договору и позиции. Ключ с фактом нельзя удалить, заменить другим ключом, перенести на другую позицию/договор или уменьшить ниже принятого объёма. Старый суммарный guard остаётся для совместимости актов без ключа.
- Новый тест: две привязки одного договора, факт на первой; перенос общего объёма на вторую отклоняется, уменьшение первой ниже факта отклоняется, после аннулирования перенос разрешён. Текущий DB launcher session 69007, `finance-allocation-fact-tests.log`. Временный порт 55433 вернуть после завершения. PHPStan запущен отдельно, `finance-allocation-fact-phpstan.log`.
- Проверки завершены: 3 теста / 13 assertions успешно, PHPStan успешно. Временный порт возвращён. Следующая часть — сохранение принятой стоимости и новых условий остатка: метод unit меняет цену оставшегося объёма, метод total задаёт общий итог связи, из которого вычитается принятая стоимость в соответствующей базе цены.

## Восьмой блок: стоимость остатка (в работе)

- Добавлены nullable JSON `accepted_basis`, `condition_basis` и `EstimateFinanceRemainder`. Сервис вызывается при нормализации после защиты объёма. Принятые суммы читаются только из утверждённых/подписанных неаннулированных строк с ключом условий. Для unit новая цена умножается на остаток; для total из общего итога вычитается принятая стоимость в выбранной базе. План содержит принятую стоимость плюс стоимость остатка. Акт читает новую цену из condition_basis, а не делит смешанный итог на весь объём.
- Тест: 20 из 100 приняты по 6000; новая цена 12000 даёт 1080000, повторное сохранение не начисляет заново; общий итог 1000000 оставляет 880000 на 80 единиц, цена нового выполнения 11000. Проверяется неизменность принятой строки и сумма совместимой проекции.
- Текущие процессы: DB session 2605 (`finance-remainder-tests.log`), PHPStan session 8332 (`finance-remainder-phpstan.log`). Порт временно 55433. Блок не закоммичен.
- Требования для дальнейшего завершения: старый факт без ключа требует уточнения/переноса, он пока не сегментируется; UI должен показывать принятую часть и условия остатка; смешанные ставки не представлять как одну ставку всей суммы; аннулирование требует пересмотра сохранённой сегментации; точное распределение округлений при частичном актировании; предел принимаемого объёма при утверждении, конкурентный сценарий; полный регрессионный пакет. Не считать весь остаток готовым по одному тесту.
- Первый PHPStan отметил три лишних аргумента scale у add/subtract (эти методы сохраняют десятичную точность без параметра). Аргументы удалены, повторный PHPStan session 43500; предыдущая 8332 завершена. DB session 2605 остаётся текущей.
- DB session 2605 завершена успешно: 2 теста / 20 проверок. PHPStan session 43500 успешно. Синтаксис сервиса/миграции и diff-check прошли. Порт возвращён перед сохранением блока. Это подтверждает unit/total и повторное сохранение в проверенном сценарии; остальные ограничения выше остаются открытыми.
- Backend-блок сохранён `dd205ae19`.

## Девятый блок: объяснение остатка в форме (в работе)

- Admin CBM `most-unified-finance-admin` успешно проиндексирован, generation 2026-09-12T14:22:19Z. В форму добавлены типы accepted_basis/condition_basis и краткая сводка сохранённой принятой части. Названия полей уточнены: общий объём/общая сумма привязки; поясняется применение цены единицы к остатку.
- Сводка пока использует сохранённый accepted_basis из ответа. После первого появления факта до редактирования привязки необходимо отдавать актуальную принятую часть в report/preview; это ещё требуется реализовать. Визуальная браузерная проверка и выбор нескольких условий в мастере актов не выполнены.
- ESLint session 98045 прошёл (только уведомление старого Browserslist, зависимости не обновлялись), tsc session 64566 / `finance-remainder-ui-tsc.log`. Изменения UI не закоммичены.
- Согласована независимая задача 01a095fb-70ad-7ca0-b5b6-abba579cdad0: она исправляет импорт labor_hours из is_not_accounted ресурсов и пустые Chip EstimateTableView/estimateItemHelpers, выпускается отдельно. Эти участки пока не менять, её правки затем принять из main. Сейчас тестовый PostgreSQL занят этой задачей; наши DB-запуски завершены. Не запускать launcher до сообщения об освобождении или проверки живых процессов.
- TypeScript session 64566 завершена успешно. После неё добавлены optional can_view_execution и пояснение недоступности факта; повторный ESLint запущен. Для актуальной принятой части report теперь читает acceptedFacts только при act_reports.view и маскирует accepted_basis/condition_basis без права. История также скрывает эти поля. При наличии права факт виден до первого редактирования привязки. Добавлены проверки актуального факта, отсутствия запросов к актам при запрете и маскировки истории; DB-запуск ещё требуется после освобождения контейнера. PHPStan session 51740 / finance-execution-access-phpstan.log.
- PHPStan 51740 успешно, ESLint 14055 успешно. Admin Vitest session 65129 / `finance-remainder-ui-vitest.log` выполняется. В задачу 01a095fb-70ad-7ca0-b5b6-abba579cdad0 отправлен запрос освободить контур после её DB-пакета; не пересоздавать контейнер в промежутке между её проверками без ответа.
- Vitest 65129 завершён: 7 тестов успешно. UI сохранён в task-ветке. Соседняя задача подтвердила, что её повторный DB-тест ещё работает; ждём явного освобождения. Наш backend-блок доступа/актуального факта пока не закоммичен и DB-проверки не пройдены.
- UI-коммит `1eb4cd8e`. Соседняя задача передала контейнер на один запуск и ждёт возврата для финального backfill-теста. Наш DB session 93708 / `finance-execution-access-tests.log` запущен, временный порт 55433. По завершении немедленно сообщить в задачу 01a095fb-70ad-7ca0-b5b6-abba579cdad0 и вернуть порт; до её финального теста больше не запускать launcher.
- Подтверждена карта старых attachItems через CBM trace inbound depth1: JournalContractCoverageService::ensureCoverage, ContractEstimateItemController::attach, ContractEstimateService::syncItems, ContractFromEstimateService::create (4 callers, без усечений). Актуальные snippets attach/sync/updateCoverageVat показывают прямое изменение совместимой проекции, не финансового источника. Адаптация этих путей остаётся следующим крупным обязательным блоком; не считать их объединёнными.
- Session 93708 завершена успешно: 1 тест / 39 assertions, в том числе актуальный факт до редактирования, маскировка метаданных без act_reports.view, отсутствие SQL-запросов к актам при запрете, маскировка истории. Контейнер передан обратно соседней задаче для её финального backfill. Порт возвращён перед коммитом.

## Десятый блок: адаптер старых команд (в работе)

- Backend доступа сохранён коммитом `5b2706219`. Соседняя задача закончила DB и освободила контейнер. Она сообщила main backend aa804fea82e6b54cc4d54c19fed6106273b14f7a (PR691) и admin 68078fdfdbcb6333334e1cbef34aaa8f986ce3f1 (PR374); перед нашим выпуском принять эти изменения из main. Сейчас ещё не подтягивались.
- `ContractEstimateFinanceAdapter::updateVat` преобразует старую команду в save финансового сервиса. Ставка — явно переданная либо уже известная на привязке; ставка сметы не читается. Сохраняются остальные распределения выбранных позиций, ключи управляемых привязок и общая защита прав/факта. Старые связи преобразуются с legacy_link_id, неизвестные условия не подтверждаются автоматически.
- `ContractEstimateService::updateCoverageVat` больше не пишет суммы проекции самостоятельно; требует явного actor. Контроллер передаёт пользователя и необязательный vat_rate, request проверяет ставку 0..100. Старый запрос без ставки допустим только при уже известной ставке; иначе нужна новая форма/уточнение. Её клиентская адаптация ещё не выполнена.
- Тест команды: независимость от ставки сметы, повторное включение без повторного начисления, сохранение allocation/link IDs, неизменная сумма другой стороны, выключение налога, запрет actor-less вызова. Процессы: PHPStan 17638 (`finance-vat-adapter-phpstan.log`); DB 65740 (`finance-vat-adapter-tests.log`), порт временно 55433. Блок не закоммичен.
- Открыты адаптеры attach/detach/sync, клиентские параметры НДС, передача mutation_id/revision и идемпотентность истории старой команды, полная проверка legacy/ресурсов/исключённых строк и старых тестов updateCoverageVat. Это пока только первый путь, не объединение всех API.
- PHPStan 17638 успешно. DB 65740 завершена ошибкой AuthorizationException до assertions: ContractEstimateService зарегистрирован singleton и был создан до mock AuthorizationService. Тест теперь forgetInstance перед получением сервиса; повтор DB ещё требуется. Порт возвращён. Контейнер передан соседней задаче для срочного guard-теста после сбоя её production backfill: production имеет approved_estimate_structure_is_immutable, её новая миграция исключает утверждённые/запечатанные сметы. Наш деплой не запускать до разрешения её выпуска, полный план всё равно ещё не готов.
- Admin EditCoverageVatDialog больше не выводит НДС из разницы сумм и не берёт ставку сметы: требует явного выбора none/exclusive и ставки (0 допустим). Сервис передаёт vat_rate. Независимый от принятого факта предварительный расчёт суммы в браузере удалён, показывается текущая сумма. Клиентские изменения не закоммичены и требуют проверок.
- Повтор команды НДС завершён успешно: finance-vat-adapter-retest.log, 1 тест / 13 assertions. Клиентские тесты EditCoverageVatDialog: 2 успешно, ESLint успешно. Восстановлена прежняя проверка соответствия организации/проекта в updateCoverageVat; тест чужой организации передаёт явного пользователя и ставку. Текущая проверка изоляции: session73700, finance-vat-isolation-tests.log. Проверка типов admin: session43393, finance-vat-typecheck.log. Остальные старые VAT-тесты пока требуют адаптации фикстур к явным условиям и actor, не считать весь пакет совместимости пройденным.
- Соседний выпуск завершён по сообщению его задачи: backend main c8e719b613461e32df9c940a61a4a0e3293374f8, deploy34700511852 success; admin deploy34699981932 success. Перед нашим выпуском всё равно сверить актуальный main и production. Его исправление миграции сохраняет approved/sealed сметы; не снимать этот guard в нашем переносе.
- Проверка изоляции завершена: 1 тест / 1 assertion успешно (finance-vat-isolation-tests.log). Временные порты возвращены; DB launcher завершён.
- Admin tsc --noEmit завершён успешно (session43393, finance-vat-typecheck.log). В старый API НДС добавлен необязательный revision и передача в общий save, чтобы клиент мог получить 409 при устаревшей версии. Расширен регрессионный тест: устаревшая версия не меняет сумму/режим/ревизию, актуальная сохраняется. DB session49266, finance-vat-revision-tests.log; порт временно55433. Клиент пока не передаёт revision, обязательное получение версии и mutation_id/idempotency старого API остаются открытыми.
- Проверки revision завершены: finance-vat-revision-tests.log — 1 тест / 17 assertions успешно; finance-vat-revision-phpstan.log — без ошибок. Временный порт возвращён. Следующий шаг: передача версии из UI и идемпотентность адаптера, затем адаптация оставшихся старых тестов и attach/detach/sync. Полный выпуск не готов.
- Связана версия UI/API: getContractCoverageSummary возвращает finance_revision вместе с существующей сводкой; EditCoverageVatDialog отправляет её в updateVat, без дополнительного запроса финансового отчёта. При отсутствующей версии сохранение блокируется с предложением обновить страницу. Тип поля optional для совместимости остальных потребителей. Тесты формы расширены на передачу версии и блокировку без неё; session52705, finance-vat-dialog-revision-tests.log. ESLint четырёх файлов и PHP syntax успешно. Идемпотентность legacy-команды, старые fixtures, остальные адаптеры остаются незавершёнными.
- Проверка формы завершена: 3 теста успешно (finance-vat-dialog-revision-tests.log). Последний полный tsc был до добавления поля revision; повторить вместе со следующим завершённым UI-блоком.
- Legacy VAT idempotency: API принимает mutation_id; адаптер использует ту же запись estimate_finance_mutations и атомарно дописывает adapter_command_hash в changes. Повтор проверяет actor, hash исходной команды и текущие финансовые/договорные права до возврата. Отличающиеся условия с тем же id дают409. UI хранит id по сигнатуре условий через useRef, при сетевом сбое повторяет его. Без mutation_id старый клиент по-прежнему получает новый id.
- PHPStan без ошибок. UI 4 теста успешно, включая повтор после сетевого сбоя. Первый DB тест упал на предположении revision+1: общий save и существующие обновления повышают revision больше одного раза. Исправлена проверка на неизменность ревизии и condition_version именно между первым сохранением и повтором. Повторный DB session79947, finance-vat-replay-retest.log; временный порт55433. Полный tsc запущен отдельно. Двойное повышение revision само по себе не нарушает optimistic lock, но единый контур ещё требует общего аудита наблюдателей.
- Replay НДС: повторный DB успешно 1 тест / 20 assertions (finance-vat-replay-retest.log); admin tsc session43057 завершён без ошибок. Порт остался55433 для следующего DB-блока.

## Одиннадцатый блок: старое удаление через единый сервис (в работе)
- Добавлен ContractEstimateFinanceAdapter::detach: выбранные условия текущего договора удаляются через finance.save, другие договоры сохраняются, общий guard принятого объёма и история применяются. ContractEstimateService::detachItems требует явного actor, проверяет организацию, загружает сметы по организации/проекту в транзакции и вызывает адаптер. Контроллер передаёт actor и пропускает стандартные ошибки прав/валидации/HTTP вместо500.
- PHPStan трёх файлов успешно. DB session2739, finance-detach-tests.log: новый тест сохранения другого договора и расширенный тест запрета удаления принятого объёма. Порт временно55433. Старый ContractEstimateServiceTest::detach ещё требует адаптации actor/fixture/проекций. Прямая EstimateCoverageService::detachCoverage, sync/attach и ресурсы ещё не переведены. Версия и идентификатор операции для detach ещё не передаются. Не считать все операции унифицированными.
- Первый detach DB успешно: 2 теста / 12 assertions, finance-detach-tests.log. Следующий путь EstimateCoverageService::detachCoverage также переведён на detachItems с явным actor; EstimateContractIntegrationService и HTTP-контроллер передают его, поиск договора ограничен организацией/проектом. Старое изменение журнала работ при отвязке удалено: финансовая команда не переписывает выполненный факт. Это требует общей регрессии интеграции журналов при финальном аудите.
- Выявлена сохранённая нулевая проекция без allocation: countedInCoverage учитывал её как позицию. Добавлен фильтр действующей связи для finance_managed через EXISTS allocations; идентификатор остаётся в таблице. Тест расширен проверкой count0 и отвязкой второй стороны через detachCoverage. Новый DB session96449, finance-unlink-tests.log. Порт временно55433. PHPStan integration3files успешно; модель синтаксически проверена. Общий UI/мобильные читатели проекции и остальные legacy tests ещё требуют аудита.
- Второй unlink DB успешно: 2 теста / 14 assertions (finance-unlink-tests.log). Старые тесты адаптированы: явный actor, авторизация, известные стороны двух договоров для VAT; ставка20 передаётся явно. При detach сохраняются2 строки совместимости, но countedInCoverage=0. Новая DB проверка3сценариев session14119, finance-legacy-compatibility-tests.log; порт55433.
- По текущим исходникам resolveChildrenForDetach захватывает один уровень parent_work_id и не r:ресурсы. Это остаётся отдельным незавершённым требованием при переводе legacy composition; не считать поддержку удаления всей ресурсной структуры законченной.
- Legacy compatibility завершена успешно: 3 теста / 11 assertions (finance-legacy-compatibility-tests.log). Порт возвращён45433. Блок VAT/replay и первые два пути detach готов к локальному коммиту; весь план и остальные legacy adapters не завершены.

## Двенадцатый блок: полный вложенный состав (в работе)
- resolveChildrenForDetach заменён обходом adjacency map всех parent_work_id в сметах договора. Один запрос дерева, далее очередь/seen в памяти, без запроса на каждую строку и без зацикливания при повреждённом графе. Расширен прежний тест третьим уровнем: три строки совместимости сохраняются, активное покрытие0.
- DB session70068, finance-nested-detach-tests.log; PHPStan session89536, finance-nested-detach-phpstan.log. Порт временно55433. Это покрывает вложенные EstimateItem; отдельные r:ресурсы в адаптере ещё нужно включить в состав и проверить подтверждение изменений состава.
- Вложенные позиции: DB успешно 1 тест / 3 assertions (finance-nested-detach-tests.log), PHPStan успешно. Добавлены r:ресурсы в detach по estimate_item_id с обязательным включением родителя в target_keys и confirm_resource_changes. Поиск смет дополняется financial allocations для ресурсной привязки без старой проекции. Тест: подрядчик только на ресурсе, заказчик на родителе; удаляется ресурсное условие, доход сохраняется. DB session97281, finance-resource-detach-tests.log; PHPStan запущен finance-resource-detach-phpstan.log. Порт55433. Проверка принятого факта именно ресурсного состава и вложенных смет с resource-only links требует дальнейшего аудита.
- Ресурсный detach DB успешно: 1 тест / 2 assertions (finance-resource-detach-tests.log), PHPStan успешно. Полная отвязка теперь собирает item IDs также из allocations; обход дерева определяет сметы объединением проекций и allocations, включая resource-only договор. Тот же тест переведён на публичный detachCoverage для проверки полного пути. DB session66270, finance-resource-unlink-tests.log; PHPStan session72257, finance-resource-unlink-phpstan.log. Порт55433, блок пока не коммитить до результата.
- Полный ресурсный unlink DB успешно: 1 тест / 2 assertions (finance-resource-unlink-tests.log), PHPStan успешно. Порт возвращён45433. Проверенный блок вложенных позиций и ресурсов сохраняется отдельно.
- Следующий attach/sync: граф inbound attachItems depth1,4 production callers (без truncation): JournalContractCoverageService::ensureCoverage, ContractEstimateItemController::attach, ContractEstimateService::syncItems, ContractFromEstimateService::create. ensureCoverage автоматически вызывает attach при отсутствии связи без actor. При переходе на единый процесс нельзя автоматически угадывать цены/НДС/направление из журнала; требуется сохранить поведение регистрации факта без создания молчаливых подтверждённых условий. Граф callers ensureCoverage неполон (0), нужен ограниченный source-поиск вызывающих обёрток и фактической цепочки.

## Тринадцатый блок: создание через финансовый сервис (в работе)
- ensureCoverage: ограниченный source-поиск по всему app нашёл только определение, явных вызовов нет. Не считать автоматическую регистрацию журнала доказанным активным путём; динамические вызовы не проверены. ContractFromEstimateService::create имеет actor и атомарный dossier replay guard.
- Добавлен adapter.attach: существующие условия сохраняются, новые корневые позиции получают выбранные сметные цены как договорную базу и явный none/exclusive VAT; неизвестная ставка при includeVat требует ввода. Исключённые строки пропускаются, включённые дочерние строки не добавляются второй раз. Отдельно выбранный дочерний элемент требует уточнения состава. Все финансовые права и квоты общего save сохраняются. Повтор attach существующей позиции не меняет цены/версии. Метод пока НЕ подключён к старым attach/sync/controller/create; это следующий обязательный шаг.
- PHPStan успешно, DB session47284 finance-attach-tests.log: сохранение договорной цены после изменения сметы и повторного attach. Порт55433. Неизвестные legacy стороны/налоги, размеры запросов, UI-предпросмотр и атомарные обе стороны остаются в полном плане.
- Adapter attach DB успешно: 1 тест / 5 assertions (finance-attach-tests.log). Теперь ContractEstimateService::attachItems полностью делегирует адаптеру и требует actor, больше не создаёт проекции напрямую. Контроллер передаёт actor/vat_rate и не превращает validation/auth/HTTP ошибки в500; request принимает vat_rate. ContractFromEstimateService передаёт actor и новый optional vatRate, но вызывающий DTO/controller ещё должен передать ставку.
- Тест переведён на публичный attachItems: session98542 finance-attach-api-tests.log. PHPStan session45649 finance-attach-api-phpstan.log. Порт55433.
- ВАЖНО: переход ещё НЕ завершён и не готов к коммиту/релизу. syncItems пока вызывает attachItems без actor и независимо удаляет проекции; нужно заменить на адаптер и протянуть actor/rate через EstimateCoverageService, EstimateContractIntegrationService и HTTP. Неиспользуемый ensureCoverage и многочисленные legacy тесты также требуют явного actor/новых ожиданий либо удаления недействующего пути. Прежние зелёные legacy тесты после смены attach больше не являются доказательством текущей совместимости.
- Публичный attachItems DB успешно1/5 (finance-attach-api-tests.log), PHPStan успешно. syncItems заменён: собирает удаляемые item IDs из проекций/allocations, вызывает общий detach и attach внутри одной транзакции с actor/rate. attachFullCoverage/syncCoverageItems/linkToContract/HTTP протягивают actor/rate. DB новый rollback test: при неизвестном НДС после попытки удаления прежние условия/ревизия сохраняются, при явной20% замена даёт240000. session DB записать по результату tools; finance-sync-tests.log. PHPStan session2079 finance-sync-phpstan.log. Порт55433.
- Открыто: старый backfillLegacyCoverage вызывает attachFullCoverage без actor; его нужно перевести в новый повторяемый перенос, а не угадывать пользователя. Неиспользуемый ensureCoverage и старые тесты ещё не адаптированы. Передача vatRate в создание договора из API пока не завершена. Полный набор legacy test после замены attach/sync не пройден.
- DB sync rollback: session79414 (finance-sync-tests.log), продолжать наблюдение за этим процессом, не запускать второй launcher до завершения.
- Sync rollback DB успешно: 1 тест / 5 assertions (finance-sync-tests.log), PHPStan успешно. Порт возвращён45433. CreateContractFromEstimateRequest принимает coverage_vat_rate отдельно от полей договора, controller передаёт её в сервис и пропускает стандартные validation/auth/HTTP исключения; PHPStan session86523 finance-create-vat-phpstan.log. UI создания договора ещё должен передавать поле.
- Открытый риск идемпотентности создания dossier: проверить, входят ли itemIds/includeVat/coverageVatRate в существующий request hash. Сейчас вызов create переиспользует dossier replay guard, который может не учитывать состав привязки. Не считать эту защиту завершённой без отдельного теста изменения состава/НДС при том же ключе.
## Проверка идемпотентности создания договора (следующий блок)
- Подтверждено source чтением ContractDossierCreationService::create: сначала возвращает любой ContractDossierSource с organization/source_type/source_id, затем договор по dossier_creation_key; сравнения запроса нет. Controller задаёт source_type=estimate/source_id=estimateId. Это мешает нескольким новым договорам из одной сметы и может считать изменённые условия повтором.
- ContractDossierCreationInput readonly содержит contract, key, document metadata/sourceLinks/sourceidentity; hash/context отсутствует. Не менять глобальную semantics всех источников без необходимости. Нужна защита именно создания из сметы: идентичность операции отдельно от идентичности исходной сметы, snapshot/hash состава и НДС, повтор с отличающимися условиями409, прежние источники/документы сохранить. Использовать существующий финансовый журнал команд либо минимальное расширение, без второго финансового контура.
- В tests/Feature/Contract/ContractDossierCreationServiceTest.php найден единственный targeted test_invalid_estimate_items_do_not_create_a_partial_contract (204–233 в индексе), мокирует ContractFromEstimateService dependencies, проверяет отсутствие частичного договора. Добавить сценарии повторов/другого состава/нескольких договоров, не выдавать этот тест за покрытие идемпотентности привязки.
## Четырнадцатый блок: операция создания договора из сметы (в работе)
- ContractFromEstimateService теперь принимает FinanceAccess, проверяет финансовые права и фиксирует запрос в estimate_finance_mutations (детерминированный uuid5 от estimate/key). Hash включает DTO договора, отсортированный состав, includeVat/ставку, документные параметры. Источник dossier для новых операций estimate_contract_creation/operationUUID вместо общей estimate/id; sourceLinks исходной сметы сохраняются. Разные ключи могут создавать разные договоры из одной сметы. Старые источники/документы не меняются.
- Повтор сравнивает actor/hash, права изменения договора и contract_id из receipt. Старый dossier replay без новой receipt возвращает409: состав прежнего запроса неизвестен, молча считать его совпавшим нельзя. После успешного создания/привязки receipt пишется в той же транзакции. PHPStan finance-create-replay-phpstan.log успешно. Нужны targeted DB тесты повторов, других условий, разных ключей, rollback и доступа. Старый тест invalid items обновлён под4й constructor dependency, ещё не запущен.
- Блок не проверен функционально и не готов к коммиту. Проверить также вызовы new ContractFromEstimateService за пределами известного теста, поддержку нового sourceType внешними читателями и прошлые ключи/перенос. Порт сейчас45433, активных DB запусков нет.
- Create invalid-input test успешно1/2 finance-create-invalid-tests.log. Найден и обновлён второй direct constructor в ContractSourceIntegrationTest. Интеграционный тест с настоящим PG receipt/dossier source (мок записи самих финансовых условий) успешно1/8 finance-create-replay-tests.log: перестановка items и20/20.0 повторяют договор, изменение состава/ставки конфликтует, другой ключ создаёт второй договор. Это не заменяет full end-to-end проверку создания фактической привязки/архива.
- Соседней задаче01a08688-81ff-7af0-8b69-da897d430cf8 передан DB-контур после завершения всех моих процессов. До сообщения об освобождении не запускать DB. По явной координации добавлен общий mutex Local\MostPostgresTests в launcher вокруг reset/up/tests с finally Release/Dispose, без CI изменений. Parser проверен, конкурентную проверку самогоmutex ещё провести. Порты возвращены45433.

## Вложенный состав старого sync (следующий блок, не завершён)
- Предыдущий goal-turn дал прогресс: DB проверки создания/replay сохранены. В этом продолжении обнаружен resolveWithChildren только первого уровня; заменён на один запрос id/parent_work_id и обход очередью с seen для всех уровней. PHP lint и APP_ENV=testing PHPStan по ContractEstimateService успешно (finance-nested-sync-phpstan.log). Без APP_ENV testing bootstrap падает storage_configuration_invalid; не подставлять фиктивные credentials.
- Новый test_sync_keeps_existing_conditions_of_deeply_nested_positions в EstimateFinanceTest пока НЕ ПРОШЁЛ. Первый запуск ошибался fixture: пытался менять parent_work_id после allocation, DB guard корректно отклонил. Второй запуск (session41792 завершён exit2) выявил запрет revenue для дочернего target (EstimateFinanceService::normalize примерно364). Fixture теперь создаёт иерархию до save и использует contractor cost для root/leaf, confirm_resource_changes=true. Эту последнюю версию ещё запустить. Не выдавать тест за успешный, не деплоить блок.
- DB окно передано задаче01a08688-81ff-7af0-8b69-da897d430cf8 под7регрессий ПИР. Все наши DB процессы завершены, порты45433 восстановлены. Mutex Local\MostPostgresTests фактически использован двумя нашими launcher, иных инфраструктурных ошибок не было. Дождаться освобождения соседней задачи или безопасно поставить очередь черезmutex после согласования.
- Дополнительно обнаружен старый calculateItemsTotal: суммирует все resolveWithChildren через calculateAmount, который не исключает parent_work_id и использует VAT сметы. Trace inbound единственный production caller EstimateCoverageService::validateContractAmount. Нужно перевести этот preview/validation на общий расчёт, иначе после расширения иерархии независимое суммирование ещё заметнее. Это обязательный незавершённый потребитель этапа1, не утверждать согласованность всех сумм.

## Общий предварительный расчёт привязки (в работе)
- ContractEstimateFinanceAdapter::rootKeys общий для attach и initialAmount. calculateItemsTotal теперь делегирует initialAmount: денежная арифметика FinanceDecimal, EstimateFinanceTax, только корневые неизключённые строки, явная ставка НДС вместо ставки сметы. Старый calculateAmount остаётся неиспользуемым и ещё требует удаления после проверки вызовов.
- vat_rate протянут в ValidateEstimateContractAmountRequest/controller/integration/coverage. UI потребителей validate ещё адаптировать; include_vat=true без явной ставки для новой привязки теперь422. Существующие привязки берут сохранённые суммы. Права preview и неизвестные исходные цены остаются в полном аудите.
- PHPStan всех6изменённыхproductionPHP успешно finance-preview-phpstan.log; php-l7файлов иgitdiffcheck успешно.
- Предыдущий nested test32770 завершён с composition guard: target_keys не включал непосредственного родителя leaf. Fixture теперь добавляет child key и сохраняет cost conditions. Новый тест initial_amount проверяет родителя/ребёнка/исключённую строку, VATсметы5 vsявная20, совпадение attach1200000, missingVATreject. Текущий общий DB session69250 finance-preview-tests.log (2теста), продолжать наблюдение; порты55433 пока НЕ восстанавливать до окончания. Не считать проверки успешными до результата.
- Пользователю честно сообщено: приблизительно60–70% полного плана ещё впереди, раньше темп был оценён оптимистично; полногоUI/факта/денег/переноса/деплоя нет.
- DB session69250 завершён успешно: 2tests/8assertions, 02:04.772, finance-preview-tests.log. Вложенные условия/ID/ревизия сохранены при sync; preview/attach суммы совпали, исключённая строка и включённый ребёнок не добавлены повторно; явныйVAT20 независим отVATсметы5. Порты45433 восстановлены, своих DB процессов нет. Следующая задача ПИР ставит QualityControlMobileTest черезmutex. Полный блок и весь выпуск всё ещё не готовы: UI vat_rate, старые fixtures, перенос, полныйплан открыты.

## Передача НДС создания договора в админке
- В admin CreateContractFromEstimateDrawer добавлены явные none/exclusive и поле ставки, без подстановки из estimate.vat_rate. При missing/nonfinite/outside0..100 отправка блокируется; 0% отправляется как exclusive/rate0, none как false/null. CreateProjectContractFromEstimate payload типизирован дополнительными include_vat/coverage_vat_rate. Старый validateContractAmount API-клиент принимает необязательный vatRate и передаёт параметр; текущий ContractAmountValidator всегда использовалfalse, не требует нового поля для просмотра сохранённого покрытия.
- Тест формы расширен: blankrateблокирует,0%передаётся, повторпослеошибки используеттотжеkey, обычныйnoneпередаётnull. Vitest2/2 успешно finance-create-tax-tests.log. ESLint4файлов успешно finance-create-tax-lint.log. TSC session28472 finance-create-tax-types.log ещё наблюдать. UI не закоммичен.
- ВАЖНО: это совместимость старой формы, не вся новая единая форма. Здесь пока только none/exclusive как в старом API; included в общемFinanceDrawer есть, объединениеещёвпереди. CreateContractFromEstimateDrawer всё ещё сам суммирует selectedItems для initialData/notes (обнаруженоgraphread); нужно заменить сервернымpreview, чтобы выбранные работа+ресурсы не увеличивали начальнуюсуммудоговора. Полныйe2e создание+финусловия+архив не выполнен. Backendне деплоить отдельно от завершенияcompat.
- TSC session28472 завершён exit0: finance-create-tax-types.log пустой, ошибокнет. Проверки текущих4adminфайлов завершены:2Vitest,eslint,tsc,diffcheckуспешно. СвоиDB/Nodeпроцессы завершены. Следующий приоритет: заменить клиентскую сумму selectedItems сервернымpreview и продолжить единуюформу, затем остальныеполныеэтапы.

## Серверная исходная сумма создания договора
- Новый EstimateFinanceSelection объединяет rootKeys/amount для adapter.attach/initialAmount и finance.preview. Операция source_amount расширяет существующий preview, принимаетitem_ids<=20000, возвращает netamount/count/currency/revision. Новый PreviewEstimateFinanceRequest отдельно отSaveRequest: save не принимает этуоперацию. ПроверкаFinanceAccessedit остаётся в сервисе. Проекции/условия не записываются.
- Удалён клиентский reduce selectedItems и выдуманная разбивка notes из CreateContractFromEstimateDrawer. estimateFinanceService.sourceAmount запрашивает существующий preview. Форма появляется после успешного расчёта; ошибки даютRetry, сменаselectionотбрасываетстарыйответ, безpreviewсозданиеблокируется. Начальнаясуммавалидируетсяfinite/RUB/nonemptycount. УсловияНДСпривязкиотдельны.
- Backend PHPStan5файлов успешноfinance-source-preview-phpstan.log,phplint5успешно,diffcheckуспешно. DB95838завершён:1test/8assertions,01:45.384,finance-source-preview-tests.log. Провереноsourcepreviewnet1m/count1беззаписей, совпадениеattachсНДС20%1.2m,исключениеchild/excluded. Это сервисныйтест,неfullHTTP/e2e.
- Frontend3Vitestуспешноfinance-create-preview-tests.log (serveramountvslocal9m,ошибка/retry,НДС0/repeatkey,existingdossier),eslintуспешноfinance-create-preview-lint.log. TSC93212finance-create-preview-types.log ЕЩЁ выполняется; продолжатьэтотhandle, не перезапускать. СвоиDBзавершены,порты45433восстановлены;контурпередансоседнейПИРзадаче.
- Открыто: HTTPnegative/sourcesaveguardtests, неизвестныесметныецены(вqueryпокаdefault0), целостныйsnapshotусловийприизменениисметымеждуpreview/create, типизированныйединыйUIсобеимисторонами/включённымНДС,весьостальнойплан. Не считать20000itemIdsрешениемвсех8kограничений: основныесохранениявсёещёограничены1000targets/5000lines.
- TSC93212 завершёнexit0,finance-create-preview-types.log пустой. Все запущенные проверки данного блока завершены; DB/node активных нашихпроцессовнет. Измененияbackend/admin пока не закоммичены. Следующийблок: полноценнаяединаяпривязкавместоразрозненныхформ,аудитстарыхfixtures/потребителей,акты/оплаты/перенос и весь remaining scope.

## Массовая подготовка обеих сторон в финансовом окне
- Добавлен admin FinanceBindingSetup в существующий EstimateFinanceDrawer: два договора по server-side side(revenue/cost), независимыеnone/exclusive/included/rate/подтверждениесостава. Создаёттольконовыеrootлинии,пропускаетexcluded/children/ужеимеющийсятотжедоговор. Серверный preview estimate_prices принимаетоднимзапросомвсе additions вместе с сохранённымиlines; приошибке local linesнеизменяются. Денежныхклиентскихвычисленийнет. Итоговыйsaveостаётсяобщиматомарнымsave.
- Это подготовка полных объёмов новых root-позиций по сметным ценам. Частичныеобъёмы/общаясумма/изменениеценредактируютсяниже существующим UI. Уже распределённые другим договорам объёмы проверяетсервер, при превышении previewотклоняется. Удобнаямассоваячастичнаяпривязка и массоваяцена/НДСостатков ещёне закончены.
- Новые2теста проверяютобестороны/0vsnone,сохранениесуществующейстроки,исключениеchild/excluded,unknownVATблокировку. Вместес7Workspaceтестами9/9PASS finance-binding-setup-tests.log. ESLint3файлаPASS finance-binding-setup-lint.log. TSC71853 finance-binding-setup-types.log ещёжив,дождаться.
- Затем workspace defaultbasis заменённаwithout_vat,действиепереименованоПривязатькдоговору,drawerПривязкакдоговорам/Сохранитьпривязки. Повторworkspace6/7прошёл,1ожидалстарыйзаголовокwithVAT. Fixtureобновлена:initialПлановаямаржа,послепереключенияРазницасНДС. Узкийповторsession40534 finance-binding-basis-tests.log. ESLintworkspace/drawer/testPASS finance-binding-workspace-lint.log. Не считатьповторпрошедшимдозавершения.
- DBнеиспользовалась. СоседняяПИРзадачасообщилаотретьемlauncherбезmutex worktreeestimate-revisions-backend;онасвязываетсясвладельцем. Не перезапускатьконтейнервручную,толькособщимmutex/координацией.
- TSC71853 завершёнexit0;узкийbasis40534успешен1/1(остальные6неперезапускались,онипрошливпредыдущемпакете). Всеактивныеfrontendпроверки завершены. Scopeполнойцелиоткрыт: договорнаявкладкаещёнеиспользуетединыйeditor,связьвыбораосновнойтаблицысbulkformнеполная,план/факт/деньги/миграция/деплойне завершены.
- Admin checkpoint commit8eec27e9 сохранёнлокально:10файлов,сервернаясуммасоздания/явныйVAT/подготовкаобеихсторон/defaultwithoutVAT. Непушен,неPR,недеплой. Backendизмененияостаётсянезакоммиченными;неоткатывать.

## Редактирование привязки из договора
- ContractEstimatesTab заменяет узкийEditCoverageVatDialog на ContractEstimateBindingEditor через «Изменить привязку». NestedProtectedAction требуетcontracts.edit+finance.view+finance.edit. НовыйeditorдополнительноchecksusePermissionsпередHTTP,загружаетобщийfinance.reportwithoutVAT,выбираетallocationsтекущегодоговора+financeSelectionrelatedparents/resources,сохраняявсеlinesпрочихдоговороввэтихrows. ПередаётданныевтотжеEstimateFinanceDrawer. onsavedзакрываетeditor/деревоиперезагружаетsummary,чтобыприраскрытиидеревозагрузилосьзаново.
- Loading/errorRetry/empty/nopermissionsпоказанывDrawerShell. AbortControllerотбрасываетстарыеответы. can_edit=falseсерверногорепортаоставляетобщийeditorreadonly. Полнаясервернаяпроверкаправназатронутыедоговорывsaveостаётся.
- Новые2MSWтеставыборки/сохраненияother-link/norequestприnopermission+старый1ContractEstimatesTabtestуспешны(3/3,finance-contract-editor-tests.log). ESLintнетошибок,1ранееимевшеесяwarninguseEffectloadSummarydependencyвContractEstimatesTab(finance-contract-editor-lint.log). TSCsession92167finance-contract-editor-types.log ещёжив;дождатьсятого жеhandle. DBнет,контейнерне трогал.
- Крупныеостаткинеуменьшать: нетendtoendизэтойкнопкичерезнастоящийsave/утверждённыйакт,общаявкладкасуммпокаlegacySummary,основнаявыборкасметыещёнесоединенасbulkeditor,8kUI/saveлимитыоткрыты,план/исполнение/деньги/перенос/деплойне завершены.
- TSC92167exit0,finance-contract-editor-types.logпустой;своихпроцессовнет. Editorдоговорнойвкладкипроверенлокально,неproduction. Измененияadminсохраненыотдельнымлокальнымкоммитом(смgitlog);непуш/неPR/недеплой.

## Выбор основной таблицы сметы → общая привязка
- EstimateDetailPage больше не открывает AttachToContractFromEstimateDialog по выбраннымпозициям: удаленыимпорт/состояние/старыйhandlerсобходомлокальныхitems/старыйbutton/modal. selectedItemIds иonBindingSavedпередаютсявEstimateFinanceWorkspace. ОбщаяtoolbarврежимеestimateпоказываетПривязатькдоговору(N)толькоприreport.can_edit;открываетfinanceSelectionselected IDsсродителями/ресурсами. Послеsavecallbackочищаетвыбор иперезагружаетсмету. Создание новогодоговораоставленоотдельнымдействием.
- AddedWorkspaceMSWtest проверяетвыборi2+childi3безпостороннегоi1,PUTpayloadtarget_keys,callbacksaved. Первичный8testspackage7PASS/1FAILиз-заPOSTmockвместоPUT;mockисправлен,узкийповтор93429 finance-table-binding-retest.log. Общийлогfinance-table-binding-tests.logоставитькакнеуспешныйпервичныйзапуск. ESLint3файловуспешноfinance-table-binding-lint.log. TSC79528finance-table-binding-types.log ещёнаблюдать.
- НетDBзапусков. Всеостальныетребованияполнойцели остаются: частиобъёмов/договорныесводки/платежи/факт/миграция/права/нагрузка/deploy. Не считатьтестпустьemptylinesсохраненияполнымe2eдоговора;этоинтеграциявыборасобщимредактором,финансысохраняетсуществующийsave.
- Узкийповтор93429PASS1/1;предыдущие7тестовнеперезапускалисьбезизмененийихлогики. TSC79528exit0. Кодсохранёнлокальнымadminкоммитом(смgitlog),непуш/недеплой. Активныхнашихпроцессовнет.

## Совместимость старых тестов привязки с едиными условиями
- ContractEstimateServiceTest передаёт пользователя в attach/fullCoverage, задаёт явное направление договора и ставку НДС. Проверки НДС используют ставку сметы 5% и договорную 20%, без неявного наследования.
- Включённый ресурс больше не ожидается как отдельная оплачиваемая проекция. Сумма работы 10 000 остаётся 10 000, ресурс сохраняет родителя в смете. Разные стороны договора допускают привязку одной позиции одновременно.
- Исторические вложенные связи для проверки удаления создаются напрямую; идентификаторы проекций остаются, countedInCoverage исключает удалённые. Исключённая старая строка с суммой 90 млн создаётся напрямую и не влияет на сводку/видимые позиции/валидацию.
- PostgreSQL launcher с Local\MostPostgresTests: finance-legacy-adapter-tests.log — 12 из 13 прошли, первый остановился на доступе к проекту. Fixture уточнён: is_archived=false, all_projects, сервис разрешается после actingAs. Целевой повтор finance-legacy-parent-retest.log успешен: 1 тест, 4 проверки. Остальные 12 не повторялись. PHP syntax и git diff --check пройдены. Порт профилей восстановлен на 45433, своих процессов нет.
- Следующий конкретный риск: EstimateCoverageService.attachFullCoverage и syncCoverageItems вызывают CompletedWorkFactService.syncJournalEntriesForContractEstimateCoverage. Тот вызывает syncFromJournalEntry, пересохраняющий/удаляющий CompletedWork. Нужно убрать побочный пересчёт исторического факта из команд условий и добавить регрессию; пока код этих вызовов НЕ изменён. CBM generation 2026-09-12T13:44:58Z, metadata_changed; точный snippet показал текущие вызовы, syncCoverageItems дочитан ограниченно.
- Новые изменения не выпущены. Полная цель, включая план/факт/деньги, миграцию, 8k, права, сверку production и штатный деплой, остаётся открытой.
## Защита выполнения от побочного пересчёта при привязке
- Удалены два вызова syncJournalEntriesForContractEstimateCoverage из attachFullCoverage/syncCoverageItems и неиспользуемая зависимость CompletedWorkFactService. Сервис журнала не изменён: обычные операции журнала сохраняются.
- Добавлен EstimateFinanceTest.test_coverage_commands_do_not_resynchronize_existing_execution: запрещает побочный вызов, проверяет неизменность атрибутов существующего confirmed CompletedWork после обеих команд и стабильность проекции.
- PHP syntax пройден. PHPStan первоначально не загрузил storage config в обычном окружении; штатный APP_ENV=testing повтор finance-coverage-history-phpstan-retest.log успешен, ошибок нет.
- Первый PostgreSQL тест finance-coverage-history-tests.log остановился на авторизации, до проверок поведения. Исправлена подготовка: сбрасываются ранее разрешённые singleton ContractEstimateService и EstimateCoverageService после тестового mock авторизации.
- Повтор ЕЩЁ ЖИВ: exec session63889, PowerShell PID27792, finance-coverage-history-retest.log. Launcher ждёт общийmutex; не перезапускать! На 20:38 подтверждён владелец другого активного прогона: php28504, launcher23016 estimate-rollback-backend. Соседний ПИР launcher1884 тоже ждёт. ПИР подтвердил движение очереди, не зависание. Проверить этот же handle до любых новых DB запусков.
- Временные профили сейчас55433 до завершения wrapper, finally восстановит45433 исходного checkout. При интеграции актуальногоmain привести к каноническому55433 без отдельной новой инфраструктуры. Полная цель и деплой не завершены.- Повтор finance-coverage-history-retest.log завершён exit0: 1 тест, 5 проверок, 01:44.927. Exec63889 терминальный, mutex освобождён, wrapper восстановил исходные порты. Первоначальная ошибка была в singleton тестовой подготовки. Проверка сохранности выполнения теперь подтверждена локально.
- Начата сверка чтения сумм: ContractEstimateService.calculateContractEstimateTotal использует SQL sum(amount) по countedInCoverage, getSummary — collection sum(amount); EstimateCoverageService.getContractCoverageSummary — collection sum(amount/amount_without_vat) проекций. Это ещё не прямой общий read model финансовых условий. Требуется объединение потребителей с учётом неизвестных условий и совместимости старых связей; никакой замены этих методов в этом пакете ещё нет.
## Суммы договора из финансовых условий
- Добавлен ContractEstimateItem.scopeWithFinancialAmounts: один grouped join по allocation projection ID, точные SQL SUM с сохранением NULL при неизвестной цене. Если allocations отсутствуют, используются старые значения проекции. Явная выборка колонок не допускает двойных amount aliases в SQL подзапросе.
- Подключено к getItemsForContract, calculateContractEstimateTotal (SQL SUM из подзапроса), getSummary, getCoverageForEstimate и getContractCoverageSummary. Чтение не пишет в БД и сохраняет ID. Финансовая quantity пока берётся из совместимой проекции.
- Новая регрессия test_contract_reads_financial_conditions_instead_of_stale_projection_amounts: финансовая сумма 1 млн, старая проекция 180 млн; все пять путей показывают 1 млн, ID тот же, физическая проекция остаётся 180 млн. PostgreSQL finance-summary-source-tests.log PASS 1 тест/7 проверок, 01:53.563, exec30046 завершён. PHPStan finance-summary-source-phpstan-retest.log PASS (первичный запрет collection sum исправлен SQL aggregate).
- Ограничения следующего пакета: старые сериализаторы sum/float ещё могут превращать неизвестные суммы в 0; нужно переносить полноту данных/null на API и UI, а не объявлять финансовую сводку полностью готовой. Требуются проверки legacy fallback, нескольких распределений/валют, bounded organization/project и нагрузки grouped join на 8k. Остальные потребители проекта/Excel ещё не все объединены. Новые изменения пока не коммит/не деплой. Своих процессов нет; портwrapper восстановлен.
## Неизвестные суммы в сводке договора
- Добавлен EstimateFinanceAmounts.sum: десятичное накопление и распространение null. getContractCoverageSummary использует его для gross/net и общего итога; неизвестный итог даёт null также для процента/остатка/перекрытия/средней/максимальной суммы. Количество позиций остаётся известным.
- Admin ContractEstimateSummary допускает null в денежных показателях. ContractEstimatesTab показывает «Требуется оценка», скрывает денежный progress при неизвестном проценте и не выводит ложное превышение. Старый EditCoverageVatDialog безопасно показывает неизвестную сумму и блокирует сохранение без базовой цены.
- PostgreSQL finance-summary-null-tests.log PASS2/19, включая предыдущую регрессию stale180млн и новуюunknown/nullvszero. PHPStan finance-summary-null-phpstan.log PASS. Frontend ContractEstimatesTab2/2PASS finance-summary-null-tests.log; EditCoverageVatDialog5/5PASS finance-vat-null-tests.log. TSC первоначально нашёл nullable в старомVATdialog, после исправления finance-summary-null-types-retest.log exit0. ESLint3основныхфайлов0ошибок,прежнийwarningloadSummarydependency. Все свои процессы завершены.
- Граница: null обработан в contract coverage summary и его UI. getCoverageForEstimate/getSummary/calculateContractEstimateTotal и другие потребители всё ещё требуют аналогичной обработки неизвестных сумм, UI дерева и общий проект/Excel не завершены. Legacy fallback и 8k нагрузка remain. Не считать полный план выполненным; деплоя нет.
## Неизвестное покрытие на стороне сметы
- getCoverageForEstimate использует EstimateFinanceAmounts.sum. validateContractAmount сохраняет неизвестную сумму вместо float0 и возвращает valid/difference/percentage_difference=null с локализованным сообщением о заполнении цен и налоговых условий.
- Admin nullable типы EstimateContractValidation/ContractEstimateCoverage. EstimateContractSection показывает «Требуется оценка». ContractAmountValidator выводит info без нулевых чипов/ложного расхождения. Старый LinkContractDialog показывает info, не предлагает подтвердить неизвестное значение как отклонение и не отправляет такое сохранение.
- PostgreSQL finance-estimate-null-tests.log PASS1/19; PHPStan finance-estimate-null-phpstan.log PASS. Frontend новый ContractAmountValidator1/1PASS, LinkContractDialog3/3PASS. TSCfinance-estimate-null-types.log сначала выявил4nullableместа в старомLinkDialog; после исправленияfinance-estimate-null-types-retest.log exit0, exec56476 завершён. Lint5начальныхфайлов0ошибок/прежнийwarningeffectvalidateAmount. Свои процессы завершены, diffcheckPASS, production не трогали.
- Ещё открыты getSummary/calculateContractEstimateTotal unknown handling, список/tree serializers, источники исполнения/денег, миграция, нагрузка и полный deploy. Этот пакет не означает завершения цели.
## Неизвестные суммы списка позиций и договорная цена
- ContractEstimateService.calculateContractEstimateTotal возвращает nullablefloat и SQL CASE COUNT/SUM, getSummary применяет общий null-preserving decimal sum. ContractEstimateItemResource сохраняет amount=null и добавляет contract_unit_price из суммы/объёма привязки через FinanceDecimal, не из сметной цены.
- ContractEstimateItemsTree сохраняет неизвестные итоги списка/раздела, копейки складывает целыми. Строка «Цена по договору» использует contract_unit_price (для старого API — сумму/объём), больше не показывает исходный item.unit_price как договорный. ContractEstimateItemLink.amount nullable.
- PostgreSQL finance-items-null-tests.log PASS2/32: известная договорная цена/неизвестный ресурс/nullable service summaries. При проверке resource установлен estimateItem=null, чтобы изолировать денежный контракт; это НЕ тест прав и полного ресурса с выполнением. PHPStanfinance-items-null-phpstan.log PASS, TSCfinance-items-null-types.log exit0. Frontend1known testPASS; unknown первоначально слишкомточныйtextmatcher, после regex целевой finance-items-null-retest.logPASS1, первый не повторялся. Lint0errors/priorloadeffectwarning.
- Важный следующий участок: ContractEstimateItemResource.toArray для каждого item вызывает getActualVolume/getCompletionPercentageForPlannedQuantity/resolveActingQuantities/contracts_count query. Пока не убраны per-row queries, не добавлены полноценная проверка прав факта и отдельный financial execution/cash report. Снимки CBM это подтвердили; следующее изменение требует batch read model и проверки no-forbidden-queries. Не выдавать current monetary tests за выполнение требований 8k/прав/факта.
- Своих живых процессов нет, DBmutex свободен, deploy отсутствует. Полный scope остаётся.
## Пакетная загрузка оперативного выполнения и права
- Новый ContractEstimateOperationalProgress.prepare: проверяет организацию и доступность проекта, принадлежность links договору, отдельно contracts.completed_works.view и contracts.performance_acts.view; закрытый источник не запрашивается. Разрешённые CompletedWork суммируются по item одним запросом, строки актов с документами загружаются общим пакетом. Аннулированные/отклонённые освобождают объём, approved/signed в актировании, остальные в резерве. Возможность действий проверяется contracts.edit / contracts.performance_acts.create.
- Index/attach контроллера готовят прогресс перед Resource; index не превращает AuthorizationException в500. getItemsForContract eager withCount countedInCoverage; attach eager section/unit/children/count. Resource больше не делает getActualVolume/resolveActingQuantities/contracts_count per-row queries; старый private resolveActingQuantities удалён. Без подготовленного прогресса отдаёт null/нет прав, без fallbackSQL. UI показывает Недоступно и info о правах, без ложных процентов, типы nullable.
- Сохраняется ОПЕРАТИВНЫЙ смысл CompletedWork.effectiveForSchedule (вне журнала включает draft/pending/in_review/confirmed, журнал толькоapproved). Это НЕ реализация финансового представления Выполнение, где требуются только утверждённые документы. Отвязанные от договора CompletedWork не назначаются автоматически по единственной связи; нужен будущий явный нераспределённый факт.
- PostgreSQL finance-progress-tests.log PASS2/19, exec99023 завершён: отсутствие SQL к закрытым sources, 30позиций, не более5queries подготовки и0при сериализации; confirmed20/approved5/draftreserve3/annulled7=>available12. Нетпроверки8k/глубокихsections/всехстарыхпотребителейResource. Финансовые accepted snapshots/legacyactpivot отдельные и не заменены этим reader.
- PHPStan4filesPASSfinance-progress-phpstan.log (после него удалён только неиспользуемый private метод/импорты); TSCfinance-progress-types.log exit0; frontend3/3PASSfinance-progress-ui-tests.log; lint0errors/priorloadwarning. Все свои процессы завершены. Новые изменения безproduction/deploy. Остальной полный план открыт.
## Проверка чтения 8001 позиции
- Добавлен test_large_estimate_reads_without_queries_per_position: пакетная подготовка 8000 новых позиций + исходная, 8001 legacy проекция, загрузка списка, оперативного прогресса, Resource сериализация, сводка договора и finance.report.
- finance-large-read-tests.log PASS1/7, 02:33.843 с подготовкой схемы, peak424MB, exec50979 терминальный. Проверено8001строк в списке и отчёте; сериализация не добавилаqueries; весь измеряемыйreadпакет<=40queries; сумма1,800,000/8001позиций правильна. Это synthetic fixture, is_manual=false, без полногоExcelimport/browserDOM/массовогоsave, без всехвариантовиерархий/ресурсов. Не выдавать за всю8kприёмку.
- Следующий обязательный участок: SaveEstimateFinanceRequest ограничивает target_keys1000, lines5000 (также prepare_resource_items/resource_mappings1000). Простое повышение лимита без проверки Laravel wildcard/distinct validation и массовыхwrites недостаточно. EstimateFinanceService.validateInput использует in_array(target_keys) в цикле — для большого сохранения нужен аудит сложности/пакетов. Лимиты пока НЕ изменены.
- EstimateFinanceQuery.targets использует items.contains(id) для представления каждого ресурса и повторный pendingResources/hash по ресурсу; потенциальный CPU N² на ресурсных сметах, хотя в данномfixture нетэтихресурсов. Графgenerationстарый; точные snippets были получены и coverage metadata_changed отмечено.
- Своихпроцессовнет,DBmutexсвободен. Цельне завершена,production/deploy отсутствуют.
## Продолжение: построчная валидация больших привязок
- FinanceInputValidation проверяет массивы по одной строке без глобального разворачивания wildcard-правил. Уникальность проверяется множествами (UUID без учёта регистра, integer ID в числовой форме). Ошибки сохраняют путь конкретной строки; при первой ошибочной строке запрос отклоняется. Непроверенные поля вложенных объектов удаляются.
- FormRequest validator/validated/safe и EstimateFinanceService preview/save используют общий механизм. Source preview также проверяет большие списки без wildcard distinct. Лимиты target_keys подняты до 20000, lines/total_line_keys до 40000. prepare_resource_items/resource_mappings пока по 1000: массовое преобразование ресурсов ещё не завершено.
- finance-input-validation-tests.log PASS4/17, 01:50.118 вместе с подготовкой схемы, peak220MB: 8001 позиция/16002 строки условий, сохранение condition_version, удаление лишних полей, некорректная конечная строка, UUID-дубликат другого регистра, malformed/oversized payload, FormRequest/source preview. После добавления safe() выполнен узкий finance-input-safe-tests.log PASS1/6, 01:26.575, peak158MB. Оба launcher-процесса терминальные; профили восстановлены на исходный порт45433.
- PHPStan3 изменённых production-файла PASS (finance-input-validation-phpstan.log, APP_ENV=testing). Первый запуск без APP_ENV остановился на production StorageServiceProvider bootstrap; конфигурация хранилища не менялась. git diff --check PASS.
- Это проверка входного контракта, НЕ доказательство сохранения 8001 позиции в БД. Следующий участок: N² in_array в normalize/save, пакетная запись allocations/history/projections и реальный большой save с атомарностью/повторным запросом. Полный финансовый факт, перенос, приёмка и деплой остаются обязательными. Цель не завершена.

## В работе: пакетная запись привязок и триггер версии
- НЕ завершено и НЕ закоммичено: EstimateFinanceService записывает/удаляет allocations пакетами500, новые записи через insert (не upsert неизвестного ключа), существующие через upsert с сохранением id/created_at. Проекции contract_estimate_items upsert по contract_id+estimate_item_id с сохранением id/notes/created_at; ссылки allocations проставляются одним коррелированным update. В normalize/save ключи выбранных позиций и общей суммы проверяются множествами.
- FinanceInputValidation.sanitize приводит UUID mutation/line/total keys к нижнему регистру: keyBy результатов PostgreSQL UUID теперь совпадает с вводом другого регистра и не позволяет обойти проверку чужой сметы. Новые тесты rollback-on-history-error и uppercase-other-estimate прошли во втором прогоне.
- Выявлен row-trigger guard_estimate_finance_links: на каждую проекцию UPDATE estimates finance_revision++ (16002 лишних изменений одной строки). Добавлена новая миграция2026_09_12_160000_batch_contract_estimate_revision_trigger.php: statement-level AFTER INSERT/UPDATE/DELETE с transition tables, distinct estimate IDs и упорядоченным обновлением. Остальные source guards не изменены. down восстанавливает прежний row-trigger. В central save итоговая revision теперь locked initial+1, внутренние изменения входят в одну операцию.
- Первый finance-large-save-tests.log завершён exit1: большой save прошёл ограничение<300queries, но assertion revision=1 ошибочен; остальные2 (split/replay, accepted remainder price)PASS. Второй finance-large-save-retest.log завершён exit1: на прежней схеме получено24004 вместо ожидаемых8002 (8001 начальная +16002 проекции+1). Малые rollback/uppercase проверкиPASS. После этого подготовлены финальные service revision+trigger изменения; не считать прежние прогоны проверкой новой миграции.
- PHPStan final3files PASSfinance-large-save-phpstan-final.log; php-l миграцииPASS. Новая проверка прямого bulk insert/update/delete проекций ожидает ровноодно увеличение revision на statement.
- АКТИВЕН launcher exec session66779: finance-large-save-final-tests.log, фильтр6тестов test_large_atomic_save|test_batch_save_rolls_back|test_uppercase_allocation|test_legacy_projection_batches|test_new_price_applies_only|test_customer_and_split_cost. Последний лог: ожидание общего PostgreSQL-контура другой задачи. Это живой процесс; НЕ перезапускать. Временные3профиля сейчас55433, wrapper finally сам восстановит45433. Проверить этот session черезwrite_stdin до новых DBзапусков/commit. Никаких своих другихпроцессовнет.
- Следом подтвердить весь большойsave (8001позиция/16002условия, повтор, invalid-last-line, обновление с сохранениемIDs/history), затем commit толькоуспешныйблок. Полныйфинансовыйфакт, migrationстарыхданных,8kresources/frontend,productionregression,доки/деплой всё ещёне завершены.
- CBM targets в EstimateFinanceQuery актуальныйsnippet15-67 показывает contains(id) на каждый ресурс и повторный pendingResources+hash; после текущего блока заменить на индекс id и однократныйhashнаitem с отдельнойпроверкой. Query пока НЕ менялся.

## Проверка пакетной записи завершена
- finance-large-save-final-tests.log: PASS6/70, 06:34.353 с подготовкой схемы/фикстуры и несколькими большими командами, peak534MB. exec66779 завершён exit0. Три временных профиля восстановлены на45433. Своих работающих процессов нет (обнаруженный после завершения другой php23196 не принадлежит этому launcher; не трогать).
- Доказано:8001позиция/16002условия обеихсторон реально записаны; первыйsave<300SQLqueries, всеprojectionIDs проставлены; replayбезновыхhistoryrows; превышение объёма в последнейстроке не меняетrevision/history; updateсохраняет16002projectionIDs и создаётследующуюверсию условий. В отдельныхтестах ошибка непосредственно послеSQLзаписиhistory откатываетallocations/projections/history/revision/receipt; UUIDдругойсметывверхнемрегистре отклоняется безизмененияисточника.
- Новая migration statement-trigger проверена: прямые bulkINSERT/UPDATE/DELETE проекций увеличиваютrevisionна1заstatement; central save увеличиваетrevisionна1завсюоперацию. Регрессии splitvolume/replay и accepted-remainderpricePASS нафинальномкоде. PHPStan3filesPASS и gitdiffcheckPASS.
- Время6:34 не является временем одногоHTTPsave: включаетbootstrap,8000bulkfixtureстроксисходнымitemrowtrigger,несколькокрупныхsave/replay/ошибку и5малыхсценариев. ОтдельнуюпроизводительностьHTTP/браузера не измеряли. Большаяфикстураплоская,is_manual=false,безреальногоExcelimport иресурсныхиерархий; полная8kприёмка остаётсянеподтверждённой.
- Блокготовклокальномуcheckpoint. Production/PR/deployне выполнялись. Следующее:ресурсы Query.targets (индексitems иоднократныйsourcehash); затемнезавершённыеплан/факт/оплаты/миграцияданных/приёмка/документация/деплой.

## Ресурсы: устранены повторные обходы
- EstimateFinanceQuery.targets: индекс IDs всехпозиций вместоcontainsдлякаждогоресурса; pendingResources/hash вычисляютсяленивоодинразнаработу ииспользуютсядляеёресурсов. Ресурсы безsourcehashневызываютвычисление; разныеработыимеютраздельныеданные.
- finance-resource-query-tests.log PASS3/16,01:31.948сbootstrap,172MB: независимыехеши двухработ,устаревшие/актуальные/nullhashресурсы,явнаяподготовкалегасиресурсов иизменениеисточника,запретзакупкимиррорногоресурсадоразрешенияпредставления. PHPStanQueryPASS, gitdiffcheckPASS. exec6492терминальныйexit0,профиливосстановлены.
- Следующийкрупныйэтапфинансовогофакта: CBMнайдены PaymentTransaction(immutable,statusCOMPLETED,amount,currency,reverses_transaction_id,payment_document_id,org/project,refunds) и PaymentDocumentEstimateSplit(document_id,item_id,amount,quantity...). PaymentDocumentService.processEstimateSplits создаётплановыеsplitдокумента,аНЕдетализациюконкретнойтранзакции. Ихнельзяскладыватьскаждойоплатойпополнойсумме. ExistingPaymentDocumentEstimateLifecycleTest.test_refund_reverses_estimate_item_payment_projection показывает1000оплата/400возврат=>paid_amount600 иitem.actual_quantity6: actual_quantityэтогоконтураНЕсчитатьутверждённымвыполнением.
- Графpaymentsкандидатыпрочитанычерезsnippet+coverage(metadata_changed): app/BusinessModules/Core/Payments/Models/{PaymentTransaction,PaymentDocumentEstimateSplit}.php; Services/PaymentDocumentService.processEstimateSplits(1397-1445), tests/Feature/BusinessModules/Core/Payments/PaymentDocumentEstimateLifecycleTest(174-205). Требуетсяпродолжитьtraceпотребителей/прав/поляPaymentDocument иfactreport; покаэтонавигационноеисследование,финансовыйфактнеподключён. Полнаяцельне завершена,деплоянет.

## Выполнение: первый серверный слой источников
- Добавлен EstimateFinanceExecution.report и существующий finance.report/show расширен view=plan|execution (поумолчаниюplan; cash ещёнеподдерживается). Executionвозвращает available/rows/documents. Planполяпокаостаютсявобщемответе: frontendпереключатель,агрегатывыполнения/остатки,проект/Excel ЕЩЁ НЕ реализованы. Не выдавать за готовыйэтапфакта.
- Источники: ContractPerformanceAct approved/signed, annulled_at null, contract org/project и act project проверены. Подгружаютсястрокиактасitem; старыеcompletedWorks/pivot используютсятолькокогдасовременныхlinesнет. Вrowsтекущейсметы попадаюттолькострокис еёitemID; фактыдругихсметнепереносятся. Документотдаётсвоюсумму,суммутекущейсметы,остатокбезлюбойпозициисметы; отрицательныйостатокилиразныевалюты помечаютсяneeds_review. Строкиразныхвалютсохраняютсяотдельно,безпересчёта.
- Net строки извлекаетсяизcontract_conditions base_unit_price толькоеслисовпадаютcontract/item/estimate/currency, значениевалиднаядесятичнаястрока; иначеnull. СуммаактабезНДСберётсяизегоявногоненулевого amount_without_vat (либо0приобщемнуле), илиизполностьюизвестныхснимковстрокприсовпадениисуммы; историческийdefault0приненулевомgrossневыдаётсязаизвестныйnet. Известныйnetшапкиавтоматическинастрокинеразносится.
- EstimateFinanceAccess.canViewExecution теперьпринимаетprojectId, проверяет OR contracts.performance_acts.view / act_reports.view сproject+orgcontext. Это соответствует routes/api/v1/admin/project-based.php performance-actsreadmiddleware и ActReportingModule permission. report/history обновлены. Безправ rows/documents=null available=false и никакихзапросовкактам. Старыйtestaccepted-remainder callbackобновлёнзапрещатьобаактоправа (самэтотстарыйтествэтомпрогоненеисполнялся).
- finance-execution-report-tests.log PASS2/21 (ранняяверсия); финальный4testнаapproved/draft/annulled/unallocated/rights/projectcontext/legacyfallback/mixedcurrency PASS4/31 finance-execution-report-final-tests.log,01:53.215,174MB. Послеуточненияnetшапкиузкий finance-execution-header-net-tests.log PASS1/10,01:25.625,164MB. PHPStan4filesPASSfinal,послепоследнейправкиExecutionодинфайлPASSheader-net. gitdiffcheckPASS. Всеexecпроцессытерминальные,профиливосстановлены45433.
- Остаётсянаэтомслое: агрегатырядов/разделов/валют иостатки,ссылкиUIнаисточник,доступныеконтракты/снимкисогласованностьприконкурентныхактированиях,старыйis_approvedбезканоническогостатуса(неучитываемавтоматически),ручное распределениеостатковидругиетестыизоляции/нетдетализации/нетповторногоподсчёта. Неизвестноедействительно остаётсяnull. Общаяцельне завершена; cash/owncosts/migrationolddata/real275436/docs/deployвпереди.
- CBM backendgen13:44:58старый: новыйExecution, Remainder иActing/PerformanceActContractBasisService неtracked; snippetsнеполны. Использованыточечныечтенияпослепустогопоиска иcoverage. Нужнаоднаиндексациязатронутогоbackendworktreeпослепакета; неиндексироватьвсеосновныепроекты.

## Итоги выполнения по позициям, разделам и валютам
- Добавлен EstimateFinanceExecutionSummary: агрегаты источников с устранением повторов, раздельные валюты, итоги родительских разделов, остатки и превышение объёма по каждой связке договора и позиции. Неизвестные цены, направления и объёмы сохраняются как отсутствие данных; известная часть суммы доступна отдельно.
- Отчёт execution использует агрегатор только при наличии права просмотра актов. Чтение источников сохраняет неизвестный объём как null.
- Проверки: finance-execution-summary-tests.log PASS 2 теста / 23 утверждения; finance-execution-summary-integration-tests.log PASS 5 / 75; finance-execution-summary-final-phpstan.log PASS. git diff --check PASS. Тестовые профили восстановлены (не входят в diff).
- Следующий блок: отображение выполнения в админке. Деньги, собственные затраты, ручное распределение факта, перенос, полная сверка потребителей и деплой ещё не завершены.

## Контракт выполнения в админке
- В типы добавлен discriminated union available для выполнения, документы, строки, агрегаты валют/разделов/позиций и остатки. estimateFinanceService.report принимает пятым аргументом view (plan по умолчанию, execution явно), сохраняя существующий AbortSignal.
- MSW-проверка src/services/estimateFinanceService.test.ts PASS 1: параметры view/basis и отсутствие доступа без ложных нулей. ESLint трёх файлов PASS; npx tsc --noEmit PASS (exec27253 терминальный exit0). Коммит админки feat[admin]: описать контракт отчёта выполнения.
- UI ещё НЕ подключён. CBM FinanceWorkspace найден, snippet содержит текущую основную функцию, но конец обрезан из-за смещения строк. Добавлять выбор План/Выполнение внутри режима Доходы и расходы, типизированный компонент выполнения с детализацией и проверенными переходами к актам. Excel пока поддерживает только план: нельзя выдавать плановый экспорт за выполнение. Старые разделы cash/owncosts/migration и остальные условия полной цели остаются обязательными.

## Экран подтверждённого выполнения
- Подключено План/Выполнение в FinanceWorkspace к report(view). Новый EstimateFinanceExecutionPanel показывает итоги по валютам, явные неизвестные значения, остатки по договорам и позициям, поиск, фильтр разделов с подразделами и их агрегаты, акты/нераспределённые суммы и диалог исторических строк с condition_version. Объёмы по 50 строк на страницу (проверено 8001), без запросов на строку.
- Без права выполнения выводится unavailable, не нули. Переход /acts/:id только при act_reports.view (проверен App.tsx guard и permissions.ts); имеющим только просмотр актов договора доступны строки разрешённого отчёта без запросов к общей карточке.
- Целевые Vitest PASS12 (3 новых component +9 workspace, включая MSW переключение view/возврат к plan); ESLint4files PASS; npx tsc --noEmit финальный exec55850 PASS exit0, предыдущий12818 тоже terminal PASS. gitdiffcheck PASS. Проверка в реальном браузере ещё не проводилась.
- Excel execution пока отключён в UI, потому что экспорт поддерживает только plan. Это НЕ готовый выпуск: следующий шаг общий execution export. CBM нашёл EstimateFinanceExport.workbook/download (class snippet актуален, метод контроллера export найден); download пока вызывает report безview, workbook толькоplan. Надо расширить тот же экспорт: summary/positions/sections/documents/sources/remaining без независимого пересчёта, права, unknown=null, недоступные данные явные. Не создавать второй финансовый контур. Существующий тест workbook находится EstimateFinanceTest.php:1218. Остальные обязательные cash/owncosts/manualfact/migration/clientregression/docs/deploy не завершены.
- Ограничения текущего UI для дальнейшей доводки: таблица документов и диалог источников пока без пагинации; выбор позиции показывает источники всех её договоров, требует ясных подписей стороны/договора; детальное действие ручного распределения пока не реализовано. Финальный экспорт и проектные потребители должны быть согласованы до релиза.

## Экспорт выполнения сметы
- EstimateFinanceExport.download/workbook принимают view=plan|execution. Тот же маршрут контроллера передаёт view; выполнение получает finance.report(..., view), без независимого пересчёта. Шесть листов: итоги, позиции, разделы, объёмы, акты, строки актов. Сохраняются null, валюты, остатки, ID источников/allocation_key/condition_version, формулоподобный текст явно строковый. Нет доступа — явная запись без строк/документов. НДС выбранной базы подписан, gross/net источников отдельными колонками.
- UI Excel теперь передаёт текущие view/basis, имя файла выполнения отдельное; без available кнопка отключена. Сервисный MSW-тест проверяет параметры, Blob URL, имя и освобождение URL. Vitest service+workspace PASS11; ESLint3files PASS; tsc exec34116 PASS. Админка коммит feat[admin]: выгружать выбранное представление выполнения.
- Backend workbook tests PASS2/20 finance-execution-export-tests.log, 01:38,164MB (exec50002 terminal). Интеграция реального отчёта актов + старый plan/project/Excel regression PASS2/25 finance-execution-export-integration-tests.log,01:40,174MB (exec13013 terminal). Профили после launcher восстановлены45433. PHPStan PASS (exec37210 terminal), запуск APP_ENV=testing php -d memory_limit=2G vendor/bin/phpstan ... --memory-limit=2G: без APP_ENV ошибочно загружалась production-проверка конфигурации хранилища; без memory_limit упиралось в128MB. Никакие ключи хранилища не подставлялись, никакие production-команды не выполнялись. php -l4changed PHP files PASS; diffcheckPASS.
- Ограничение до следующего блока: execution export для project без estimateId пока возвращает validation error (не подставляет plan). projectReport пока толькоplan. Надо расширить проектный отчёт единым выполнением с корректной агрегацией по валютам/источникам и недублированием много-сметных актов; затем разрешить project execution export. Отдельно требуется одинаковость контрактного экрана и сводок.
- Полная цель по-прежнему не завершена: cash/returns/advances/manualundistributed/owncosts, повторяемый перенос вместо broken backfillLegacyCoverage, фактическая регрессия275/436, полная8kресурсы/большие UI документы, документацияworkflow, интеграцияmain/штатныйдеплой/клиентскаяпроверка. Ничего не деплоилось, не отмечать цель выполненной.

## Проектное выполнение и общий акт нескольких смет
- EstimateFinanceProjectExecution объединяет отчёты по уникальному ключу строки act_id/source_type/source_id, договорные условия по allocation key, цели/разделы по ID. Акт один на проект с estimate_amounts; повтор отчёта не увеличивает объём/сумму. Общие итоги через тот же EstimateFinanceExecutionSummary, не сложение произвольных UI сумм.
- projectReport(..., view) и проектный HTTP-метод принимают execution. Для агрегации сначала доступны строки/разделы, затем удаляются из кратких ответов; plan сохраняет прежнее раннее освобождение строк. Нет доступа — available=false, rows/documents=null, включая пустой проект.
- Проектный Excel execution теперь разрешён: использует projectReport(execution), добавляет общий итог; лист актов заполняется из объединённых документов, общий акт одной строкой со списком смет. Отдельная смета сохраняет прежние листы.
- Проверки: финальный комбинированный unit/workbook PASS3/30 finance-project-execution-final-tests.log,01:37,168MB; интеграция реального projectReport(execution) + старого plan/project/Excel PASS2/29 finance-project-execution-integration-tests.log,02:12,174MB. PHPStan4files PASS finance-project-execution-final-phpstan.log. php-l4files PASS, diffcheckPASS. Все backend launcher terminal, временные профили восстановлены45433.
- В админке подготовлены project(view) и типы execution/estimate_amounts; MSW3tests и ESLint3files PASS. tsc обнаружил implicit this в тесте скачивания из прошлого блока; исправлен this:HTMLAnchorElement, повтор сейчас exec6692 (проверить перед frontend commit). UI ProjectEstimateFinance пока толькоplan. CBM snippet этой функции прочитан целиком и актуален (29–233): компонент Accordion с загрузкой service.project, tax switch, plan-таблицей/expandcontracts и download. Следующий UI шаг — выбор view, выполненные итоги/акты из data.execution, переход к деталям сметы, экспорт с view. Типы/сервис уже поддерживают.
- Незавершённые риски: согласованность чтения одного акта при конкурентном аннулировании между чтениями разных смет, округление итогов на границах нескольких смет, дополнительные проверки project rights/no-act-query. Остальные полные требования cash/owncosts/manualfact/migration/клиент275436/docs/deploy остаются, ничего не выкатили.
- Cash groundwork: CBM PaymentTransactionService.refundPayment snippet136–269 актуален, coverage metadata_changed. Возврат — отдельная COMPLETED транзакция с отрицательной amount, reverses_transaction_id и теми же payment_document_id/org/project/currency; не переписывает original transaction. Проверяет сумму доступного возврата, уменьшает document.paid_amount и запускает synchronizeFinancialProjections. Плательщик/получатель в новой refund строке не копируются: направление надо определять через документ/исходную транзакцию/стороны договора, не по пустым полям refund. Отрицательную сумму нельзя повторно инвертировать. Этот код только прочитан, не менялся.
- Финальный tsc после исправления тестового this PASS exec6692 exit0. Frontend project API/types зафиксированы; все собственные процессы завершены. Следующий шаг — UI ProjectEstimateFinance с view и источниками выполнения (описание/snippet выше), затем cash. Обязательные остальные требования остаются активными; деплой не выполнялся.

## Проектный экран выполнения
- ProjectEstimateFinance переключает view plan/execution при чтении/экспорте, сохраняет plan-таблицу, execution отображается отдельным ProjectExecutionTable. Итоги смет и проекта берутся из execution.summary; общий акт одной строкой, список смет, общий/распределённый/нераспределённый gross. Документы по50настраницу. Диалог показывает части смет и строки источника; общая карточка акта только act_reports.view, ссылки смет только PERMISSIONS.ESTIMATES.VIEW (проверены реальные App.tsx маршруты /acts/:actId и /projects/:projectId/estimates/:id). Недоступный execution не показывает plan или ложные нули, Excel отключён.
- Финальный Vitest ProjectEstimateFinance PASS2, включая MSW переключение API, две сметы/один общий акт/проектный итог и запрет доступа. ESLint3files PASS. tsc exec49095 terminal PASS. diffcheckPASS. Новый UI в реальном браузере пока не осмотрен. Все свои процессы terminal. Коммит admin feat[admin]: показать выполнение по сметам проекта.
- Дальнейшая UI доводка: источники в диалогах пока без пагинации; ProjectExecutionTable делает поиск имени сметы в data.estimates на источник (заменить индексом при оптимизации крупных документов); конкретный вид execution не сохраняется в ссылке на смету. Обязательные cash/owncosts/manualfact/migration/права/клиентскаярегрессия/доки/деплой всё ещё не завершены.
- Cash groundwork дополнен: ContractPaymentDocumentService.createPaidContractPayment создаёт документ через PaymentDocumentService.createFromContract, затем registerPayment; metadata.contract_payment_type различает advance/другие. getPaymentsForContract выбирает PaymentDocument invoiceable_type=Contract::class и invoiceable_id=contractId, но для нового finance reader обязательны явные org/project scopes. PaymentDocument.transactions — hasMany по payment_document_id. Эти snippets актуальны, только прочитаны. Дальше проверить createFromContract и источники документов по актам (invoiceable polymorphic), канонические права чтения платежей и транзакций, сохранить отрицательный знак возвратов.

## Денежные источники связанных договоров
- Новый EstimateFinanceCashSources выбирает документы по invoiceable Contract/ContractPerformanceAct, строго в org/project; дополнительно проверяет реальные contract IDs в области estimate. Транзакции только completed в той же org и project (legacy nullproject допустим при проверенном документе), пакетная загрузка. Pending/чужие транзакции не учитываются. Возврат сохраняет отрицательную amount/reverses_transaction_id; валюты отдельно. Документ paid_amount не подменяет подтверждённые транзакции. Legacy paid_amount>0 без истории отмечается payment_history_missing.
- Направление из договора; противоречие invoice.direction incoming/outgoing со стороной договора даёт unknown+direction_requires_review. Не гадаем по автору. Cash report scope=linked_contracts, НЕТ автоматического распределения суммы на смету/позиции. Для чтения требуются одновременно payments.invoice.view и payments.transaction.view с org/project context. Маршруты Payments/routes.php подтверждают оба базовых права; запрещённый report не делает payment queries.
- EstimateFinanceService.report поддерживает view=cash (проект и export пока cash НЕ поддерживают). Добавлены cash payload и канонический type в adminservice. Источники из document invoiceable act прочитывают только act.id/contract_id для контекста платежа, не суммы/содержимое акта.
- Backend tests cash PASS3/18 finance-cash-sources-tests.log,01:39,172MB: частичные400+200-100=500 при recorded_paid600, отдельные USD10, исключены pending/другаяorg/другойproject; актовая связь/unknown direction/legacymissing; denied payments no queries. PHPStan3files PASS finance-cash-sources-phpstan.log; php-l3files PASS; diffcheckPASS. exec20509 дождался общегоmutex и завершёнexit0, профили45433 восстановлены. exec93695 terminalPASS.
- Админка: report(view=cash) typed sources/documents, MSW service PASS4 и ESLint3filesPASS, tsc47058PASS до UI. EstimateFinanceWorkspace добавляет Деньги; новый CashPanel показывает договорные источники/документы, аванс/возврат, предупреждение отсутствующей истории,50строк/страница. НДС-база пока не меняет денежный факт; Excel cash отключён до реализации. Важно: это только список договорных источников, НЕ законченный денежный анализ/распределение. Workspace Vitest PASS10 inclcash, ESLint3UI filesPASS; финальный tsc54239 ещёидёт (проверить передfrontendcommit).
- Дальше обязательно: денежные агрегаты с дедупликацией транзакций и раздельными валютами, платёжные привязки по позициям с cap оплаченной суммы/точным округлением/возвратами/повторами/историей; ручное распределениенеразнесённогофакта через общийpreview/save, project/export; собственные затраты и различие expense vs money. Existing PaymentDocumentEstimateSplit — план документа, его полная сумма не фактическая оплата; synchronizeEstimateItemsPaymentProgress использует paid_amount/document.amount ratio, обновляет procurement actual_quantity (НЕ выполнениеработ). Нельзя использовать эту проекцию как принятые объёмы.
- Cash риски для доводки: explicit flags для расхождения recorded_paid_amount/ledger, валюты документа/транзакции и malformed refund; доступность документов напрямую по estimate без contract и другие invoiceable типы; детальные ссылки на платёжный документ (App.tsx только /payments, внутренний механизм открытьdocumentещёнепроверен), распределение legacy splits без переписывания истории. Полная миграция/275436/8k/доки/деплой и прочие критерии цели остаются обязательными, ничего не выкатили.
