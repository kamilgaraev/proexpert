# Документация по интеграции модуля графиков работ

## 📚 Документы

### 1. [Полная инструкция по API](./schedule-management-api.md)
Подробное руководство по интеграции с API модуля графиков работ:
- Полное описание всех эндпоинтов
- Структура данных с примерами
- TypeScript типы
- Примеры кода для всех основных операций
- Рекомендации по визуализации
- Обработка ошибок

**Используй, если:** нужна полная информация об API и примерах реализации

### 2. [Quick Start Guide](./schedule-management-quickstart.md)
Краткая шпаргалка для быстрого старта:
- Минимальные примеры кода
- Ключевые поля для визуализации
- Формулы для расчета позиций
- Чеклист интеграции
- Типичные проблемы и решения

**Используй, если:** нужен быстрый старт или справка по ключевым моментам

### 3. [AI Prompt Templates](./ai-prompt-template.md)
Готовые промпты для AI-ассистентов:
- Генерация компонента Gantt-диаграммы
- Функции загрузки данных
- Утилиты обработки данных
- Хуки/композаблы для состояния
- Тесты
- Оптимизация производительности
- Адаптивность

**Используй, если:** хочешь использовать AI для генерации кода

## 🚀 Быстрый старт

### Шаг 1: Получи данные
```typescript
const response = await fetch(`/api/v1/schedules/${id}?format=gantt`, {
  headers: { 'Authorization': `Bearer ${token}` }
});
const { data } = await response.json();
```

### Шаг 2: Отобрази задачи
- Плановые полосы из `data.tasks[].gantt_bar.planned`
- Фактические полосы из `data.tasks[].gantt_bar.actual`
- Прогресс из `data.tasks[].progress_percent`

### Шаг 3: Нарисуй зависимости
- Координаты из `data.dependencies[].gantt_line`
- Цвет: красный если `is_critical`, синий иначе

### Шаг 4: Добавь текущую дату
- Вертикальная линия в позиции `data.gantt_meta.current_date`

## 📖 Структура данных

```
ScheduleGanttData
├── tasks[]              # Иерархия задач
│   ├── gantt_bar        # Данные для полос
│   │   ├── planned      # Плановая полоса (серая)
│   │   └── actual       # Фактическая полоса (синяя)
│   ├── ui               # UI метаданные (цвета, видимость)
│   └── children[]       # Дочерние задачи
├── dependencies[]       # Зависимости между задачами
│   └── gantt_line       # Координаты для линий
└── gantt_meta          # Метаданные (timeline, current_date)
```

## 🎨 Цветовая схема

| Элемент | Цвет (HEX) | Описание |
|---------|-----------|----------|
| Критическая задача | `#DC2626` | Красный |
| Обычная задача | `#8B5CF6` | Фиолетовый |
| Прогресс 100% | `#059669` | Темно-зеленый |
| Прогресс 75-99% | `#10B981` | Зеленый |
| Прогресс 50-74% | `#3B82F6` | Синий |
| Прогресс 0-49% | `#EF4444` | Красный |
| Текущая дата | `#3B82F6` | Синий |

## 🔗 Эндпоинты API

### Organization-based
- `GET /api/v1/schedules` - Список графиков
- `GET /api/v1/schedules/{id}?format=gantt` - Данные для Gantt
- `POST /api/v1/schedules` - Создать график
- `PUT /api/v1/schedules/{id}` - Обновить график
- `DELETE /api/v1/schedules/{id}` - Удалить график

### Project-based
- `GET /api/v1/admin/projects/{project}/schedules` - Список графиков проекта
- `GET /api/v1/admin/projects/{project}/schedules/{schedule}?format=gantt` - Данные для Gantt

## 🛠 Примеры реализации

### React
См. [schedule-management-api.md](./schedule-management-api.md) раздел "Примеры использования"

### Vue
Используй Composition API и те же паттерны из примеров

### Vanilla JS
См. примеры в [schedule-management-api.md](./schedule-management-api.md)

## 📋 Чеклист интеграции

- [ ] Настроена аутентификация
- [ ] Реализована загрузка данных (`?format=gantt`)
- [ ] Построена иерархия задач
- [ ] Отображены плановые полосы
- [ ] Отображены фактические полосы с прогрессом
- [ ] Нарисованы зависимости
- [ ] Отображена текущая дата
- [ ] Показаны отклонения (`+5 д`)
- [ ] Применены цвета из `ui`
- [ ] Реализовано разворачивание/сворачивание
- [ ] Добавлена обработка ошибок
- [ ] Протестирована производительность

## ❓ FAQ

**Q: Как получить данные для Gantt?**  
A: Используй `GET /api/v1/schedules/{id}?format=gantt`

**Q: Почему зависимости не рисуются?**  
A: Проверь, что `gantt_line` не null и используй координаты `from`/`to`

**Q: Как отобразить иерархию?**  
A: Используй `parent_task_id` и `children[]`, применяй отступы по `level`

**Q: Какой цвет для критических задач?**  
A: Используй `ui.bar_color` или `#DC2626` для критических

## 📞 Поддержка

- Полная документация: [schedule-management-api.md](./schedule-management-api.md)
- Быстрая справка: [schedule-management-quickstart.md](./schedule-management-quickstart.md)
- AI промпты: [ai-prompt-template.md](./ai-prompt-template.md)

При возникновении вопросов обращайтесь к команде бэкенда.

