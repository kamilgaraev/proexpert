type: object
properties:
  name: { type: string }
  email: { type: string, format: email }
  password: { type: string, format: password }
  password_confirmation: { type: string, format: password }
  role_slug: { type: string }
required: [name, email, password, password_confirmation, role_slug] 