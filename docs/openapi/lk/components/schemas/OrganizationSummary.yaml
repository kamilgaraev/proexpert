type: object
properties:
  id: { type: integer }
  name: { type: string }
  organization_type: { type: string }
  is_holding: { type: boolean }
  hierarchy_level: { type: integer }
required: [id, name, organization_type, is_holding] 