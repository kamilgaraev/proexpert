/logs/material-receipts:
  post:
    tags: [Logs]
    summary: Логировать поступление материалов
    security: [{ bearerAuth: [] }]
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/schemas/StoreMaterialReceiptRequest.yaml'
    responses:
      "201":
        description: Записано
        content:
          application/json:
            schema:
              $ref: '../../lk/components/schemas/ApiResponse.yaml'
/logs/material-write-offs:
  post:
    tags: [Logs]
    summary: Логировать списание материалов
    security: [{ bearerAuth: [] }]
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/schemas/StoreMaterialWriteOffRequest.yaml'
    responses:
      "201":
        description: Записано
        content:
          application/json:
            schema:
              $ref: '../../lk/components/schemas/ApiResponse.yaml' 