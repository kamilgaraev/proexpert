type: object
properties:
  project_id: { type: integer }
  material_id: { type: integer }
  supplier_id: { type: integer }
  quantity:
    type: number
    minimum: 0.001
  usage_date:
    type: string
    format: date
  invoice_number:
    type: string
    maxLength: 100
    nullable: true
  invoice_date:
    type: string
    format: date
    nullable: true
  photo:
    type: string
    format: binary
    nullable: true
  notes:
    type: string
    maxLength: 1000
    nullable: true
required: [project_id, material_id, supplier_id, quantity, usage_date] 