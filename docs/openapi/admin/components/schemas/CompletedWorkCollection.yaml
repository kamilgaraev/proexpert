type: object
properties:
  success:
    type: boolean
    example: true
  message:
    type: string
    example: "Список выполненных работ получен успешно."
  data:
    type: array
    items:
      $ref: './CompletedWork.yaml'
  meta:
    type: object
    properties:
      current_page:
        type: integer
        example: 1
      from:
        type: integer
        example: 1
      last_page:
        type: integer
        example: 8
      links:
        type: array
        items:
          type: object
          properties:
            url:
              type: string
              nullable: true
              example: "http://localhost/api/v1/admin/completed-works?page=1"
            label:
              type: string
              example: "&laquo; Previous"
            active:
              type: boolean
              example: false
      path:
        type: string
        example: "http://localhost/api/v1/admin/completed-works"
      per_page:
        type: integer
        example: 15
      to:
        type: integer
        example: 15
      total:
        type: integer
        example: 120