description: Ошибка авторизации - пользователь не аутентифицирован
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string
          example: "Unauthenticated."
        success:
          type: boolean
          example: false