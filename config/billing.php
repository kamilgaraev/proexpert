<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Default Subscription Limits
    |--------------------------------------------------------------------------
    |
    | Здесь определены лимиты по умолчанию для различных ресурсов,
    | если у пользователя нет активной подписки или лимиты не заданы в плане.
    |
    */

    'default_limits' => [
        'max_users' => 3,
        'max_foremen' => 1,
        'max_projects' => 1,
        'max_storage_mb' => 100,
        'max_contractor_invitations' => 5,
    ],

    /*
    |--------------------------------------------------------------------------
    | Testing Mode Configuration
    |--------------------------------------------------------------------------
    |
    | Настройки тестового режима для открытого бета-тестирования.
    | При включенном тестовом режиме все новые пользователи получают
    | начальный баланс при регистрации.
    |
    | Чтобы ВКЛЮЧИТЬ тестовый режим - измените 'enabled' на true
    | Чтобы ОТКЛЮЧИТЬ - измените 'enabled' на false
    |
    */

    'testing' => [
        // Включить/выключить тестовый режим (true = включен, false = выключен)
        'enabled' => true,
        
        // Начальный баланс в копейках (3000000 = 30,000 рублей)
        'initial_balance' => 3000000,
        
        // Описание тестового баланса в логах
        'description' => 'Тестовый баланс при регистрации (бета-тестирование)',
        
        // Метаданные для аудита
        'meta' => [
            'type' => 'testing_mode_grant',
            'reason' => 'open_beta_testing',
            'auto_granted' => true,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Payment Gateway Configuration
    |--------------------------------------------------------------------------
    |
    | Настройки платежного шлюза
    |
    | Доступные драйверы:
    | - 'mock' - тестовый шлюз (для разработки и бета-тестирования)
    | - 'stripe' - Stripe (требует настройки ключей)
    | - 'yookassa' - ЮKassa (требует настройки shop_id и secret_key)
    | - 'robokassa' - Robokassa (требует настройки merchant_login и password)
    |
    */

    'payment_gateway' => [
        // Тип шлюза (сейчас используется mock для бета-тестирования)
        'driver' => 'mock',
        
        // Webhook URL для обработки асинхронных уведомлений от платежного шлюза
        // TODO: Реализовать webhook endpoint для production шлюза
        'webhook_url' => null,
        
        // Секретный ключ для верификации webhook подписи
        'webhook_secret' => null,
        
        // Настройки для различных шлюзов (заполнить при подключении реального шлюза)
        'stripe' => [
            'key' => '',
            'secret' => '',
        ],
        
        'yookassa' => [
            'shop_id' => '',
            'secret_key' => '',
        ],
        
        'robokassa' => [
            'merchant_login' => '',
            'password1' => '', // Пароль #1
            'password2' => '', // Пароль #2
            'test_mode' => true,
        ],
    ],
];